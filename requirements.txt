# Canonical ideation-only runtime dependencies.
# The ideation-only runtime import closure is pure standard library (ticket 13);
# this contract intentionally pins no packages. Retained legacy/downstream
# dependencies live in requirements-upstream.txt.
