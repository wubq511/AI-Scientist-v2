# Canonical ideation-only runtime dependencies.
# httpx is the sole production HTTP transport dependency for DeepSeek execution (ticket 01).
# Retained legacy/downstream dependencies live in requirements-upstream.txt.
httpx==0.28.1
