# 工作日报 - 2026-09-03

## 今日概要

凌晨完成 local-ranking formal 003 r1 live 校准与独立复核，正式结束 DeepSeek evaluator ladder，经 Robert 批准以 BM25 冻结为 v1 排序合同。上午进入 ideation pipeline 实现阶段：完成 implementation spec、拆出 13 张票据，并一口气实现并关闭 Ticket 01-07、09（Ticket 08 待验收），全天 54 个 commit，自动化测试从 258 增长到 510 项全绿。全天零真实模型调用、零外部网络，全部为离线确定性验证。

## 完成的工作

**formal 003 实验收尾（执行拓扑调整 → live 校准 → 独立复核 → BM25 决策）**
- 完成 prepare-only 阶段：冻结 profile v2.2、构建 660 文件无 secret execution bundle，Windows 离线验证全绿（处理了 MAX_PATH 与 verifier cwd 两处环境缺陷，v1 archive 被 v2 取代并如实记录 deviation）。
- Robert 批准执行拓扑调整：authenticated calls 改由 Mac 从 frozen bundle 发出，Windows 只做无凭据 offline replay（amendment v1.0）。
- Kimi 执行 r1 live：两路 48 valid/1 deterministic invalid/0 exhausted，usage 远低于上限；Codex 独立复核 PASS（含 Mac 无凭据 replay + Windows 只读 hash 核验）。
- Codex 复核发现 derived summary 的 receipt cost aggregate 误记为 null（实际 48 份均为 `"0"`），以 tracked correction 修正文档、不覆盖已验证 evidence identity。
- 新建 `windows-executor` skill 沉淀 Windows 传输/验证/清理流程（端点不落库，缓存于 repo 外 mode-600 文件）。
- Robert 批准最终建议：停止 evaluator ladder，BM25 为 v1 parsimony default，关闭 ticket 021。

**ideation pipeline 规格与票据映射**
- 写 single implementation spec（56 条 User Stories、Testing Decisions 覆盖 Validation Matrix 九层），Robert 批准两个用户级 workflow seam + 三个既有注入点。
- 12 票草案经 fresh-context 审查收紧为 13 票（拆出 model-fixable、non-success terminal、suspend/resume、Evidence Chain 四闭环），关闭 ticket 030。

**Ticket 01：可测试 ideation 地基（已关闭）**
- requirements 拆三文件（runtime/dev/upstream）、import closure 双层 allowlist、CLI baseline hash 固化；两轮双轴 code review 零 finding；Python 3.13.7 clean venv 258 passed。

**Ticket 02：Workshop 批准边界（已关闭）**
- 三阶段流水线（prepare/validate/approve）与 8-token 泄漏门禁，双轴 review 两轮（单体拆分、TOCTOU、跨 Target 绑定漂移修复）；288 passed。

**Ticket 03：Target Reference Corpus（已关闭）**
- lifecycle 隔离模块 + 权威证据政策（237/237 targets 全量预处理 100% 通过）；修复 `validate-all` 相对路径解析缺陷；298 passed。

**Smoke case 双模型独立盲审**
- gemini-3.8-flash 与 glm-5.3-flash 独立 9 项语义审查 100% 共识 → Workshop 正式 `approved`，真实 case 两大核心输入（Approved Workshop + Approved Corpus）齐备。

**Ticket 04：Run admission（已关闭）**
- run_store/pricing/admission 三模块 + `new-run` 子命令，exact-`yes` 交互批准、九步 fail-closed preflight；验收自查发现并修复 `./x` 路径归一化绕过缺陷；333 passed。

**Ticket 05：BM25 检索（已关闭）**
- scoped retrieval + Audit Release Gate（canonical payload 先落盘留痕才释放）；对抗性审查修复 6 处漏洞，其中 segment_id 跨论文碰撞为 Critical（修复为 paper_id 前缀全限定）；363 passed。

**Ticket 06：DeepSeek adapter（已关闭）**
- 参数白名单 fail-closed、16 枚举 Failure Taxonomy、Usage 四项不变量、失败 attempt 也计费、wire bytes 原生落盘；对抗性审查加固 7 处（Audit Release Gate、header 大小写、Retry-After 等）；474 passed。

**Ticket 07：sealed run controller（已关闭）**
- IdeationController generation/reflection 循环、7 字段校验、说谎检测（UNRETRIEVED_PAPER）、run 级 backstop、终端封印；对抗性审查 6 处（admission 篡改防御、跨 generation 重名排斥、dual chain verification 等）；489 passed。

**Ticket 08：模型可修复动作（状态 review，待验收）**
- 12 类 model-fixable 错误白名单、统一脱敏反馈回灌、预算耗尽优雅终局；495 passed。

**Ticket 09：非成功结果封印（已关闭）**
- hygiene patterns 前置门禁、failed seal 三类封闭 reason_kind、backstop 由异常逃逸改为 seal failed 合同变更；验收时独立端到端 CLI 场景产出 `terminal_outcome=failed` seal；510 passed。

## 实验结果

| 实验 | 关键指标 | 数值 | 对比 Baseline | 备注 |
|------|---------|------|-------------|------|
| formal 003 r1 live | stable_count | 20/24 | 阈值 22/24 | `stop_profile_failed` 合同终止态，未达阈值 |
| formal 003 r1 live | stable_directional_count | 13 | - | DeepSeek Pro/high 与 Pro/max 均为 20/24，ladder 耗尽 |
| formal 003 r1 live | 有效调用 | 48 valid / 1 invalid / 0 exhausted | 上限 48/192 | invalid 为 provider 端 `finish_reason=length`，合同内重试成功 |
| formal 003 r1 live | usage | rolling 7→12% / weekly 36→42% / monthly 42→45% | - | 全 ok |
| 全量 pytest | passed | 258 → 510 | 每日滚动增长 | 全部离线确定性测试，零模型费用 |

## 遇到的问题

- ✅ 已解决：Windows MAX_PATH 导致 tar 静默跳过 591 个深路径文件 → 短前缀目录 + `strip-components=1` 解包
- ✅ 已解决：verify-bundle.py 用调用方 cwd 解析相对路径 → 改为 chdir bundle root，保证与 Mac 冻结值逐字节一致
- ✅ 已解决：r1 summary 的 receipt cost aggregate 误记 null → Codex 复核发现实际 `"0"`，tracked correction 修正文档，不覆盖 evidence identity
- ✅ 已解决：`./x`、`x/` 绕过 pathlib 归一化检查 → 解析前拒绝非归一化形状（RED→GREEN 实证）
- ✅ 已解决：BM25 段落 segment_id 跨论文碰撞导致集合折叠为 1 → paper_id 前缀全限定
- ✅ 已解决：Ticket 07 初版 `_execute_generation` 返回值被忽略导致 hygiene seal 后继续跑（测试捕获）→ 检查返回值立即返回
- ⚠️ 已知非阻塞：repo-wide Black 14 个既有 legacy/downstream 文件不合规，非今日改动引入，未伪装成通过

## 今日决策记录

- **决策**：v1 排序合同冻结为 BM25(k1=1.6, b=0.5)，E5 仅作 diagnostic challenger
- **原因**：holdout direction inconclusive + operational evaluator qualification 失败后应用 parsimony tie-break；不续跑已耗尽的 DeepSeek ladder
- **替代方案**：继续 E5/其他 evaluator 采样（边际信息价值低，违反预注册纪律）；被否决

- **决策**：formal 003 authenticated caller 从 Windows 调整为 Mac，Windows 只做无凭据 offline replay
- **原因**：Windows 对远端 API 推理无算力加速价值，credential boundary 更干净
- **替代方案**：原 Windows live 执行合同（需解决 live secret delivery）；被 amendment 取代

- **决策**：13 票拆分 `01→02→03→04→{05,06}→07→08→09→10→11→12→13`，05/06 唯一并行分支
- **原因**：原 12 票中 model-fixable/terminal 与 resume/isolation 责任过粗，拆出四个闭环；未扩大功能范围

- **决策**：Ticket 09 中 run 级 retrieval backstop 由抛异常改为 seal terminal `failed`
- **原因**：025 规定 terminal failure 必须产生 seal，异常路径违反「每个 run 都有显式终局」的 spec story 45

- **决策**：非交互 stdin 一律 fail closed，`--non-interactive` flag 从 CLI 移除
- **原因**：024 只允许七个参数，交互性由环境自然触发而非参数选择

## 明日计划

- Ticket 08（model-fixable actions）等待 Robert 验收关闭
- 实现 Ticket 10（suspend/resume）：resume 路径、run suspension 语义与证据重建（blocker 已解除）
- 按 DAG 继续 Ticket 11-13（Evidence Chain trust/export、expand-contract、全量 handoff 验证）
- 真实 smoke case 的 run admission 需要 Approved Workshop 已就绪（今日已 approved），可考虑真实 smoke admission 验证

## ⚠️ 异常与风险

无重大异常。执行与验收纪律保持如一：所有票据均经双轴 code review + 对抗性审查 + 全量回归后关闭；今日实现全部为零模型调用、零网络费用。