# 工作日报 - 2026-08-31

## 今日概要

Local ranking 原型（ticket 021）完成一个完整的实验闭环并遭遇关键否定结果：上午落地 v1.2 AI-qrels 并建立 development/holdout 两套 consensus；中午前在 Windows 完成 Stage A/B 全套 development comparisons，冻结 BM25 与 E5 两个 finalists 及全部参数；下午通过 Windows 10-run + Mac 3-run 双平台 determinism gate 后解封 holdout，**三套 qrels 的 winner 方向不稳定，formal result = inconclusive，当前没有 evidence-backed production winner**。随后撤回「以 BM25 保守默认收尾」的过早建议，完成 v1.4 fresh evaluation 的对抗性审查、预注册与全部 fresh 输入准备（12 cases/24 queries/166 papers）；深夜裁判资格认证连续两次因 evaluator contract 缺陷失败，定位根因后冻结 v1.1 qualification protocol。当天 26 个 commits，全部与 session 记录互相印证。

## 完成的工作

- **AI-qrels consensus 建立（v1.2 落地）**：已批准设计落地为 fail-closed `ai_judge` harness，冻结 Kimi/DeepSeek actual-model bundles 并校正错误的 Codex provenance；独立 Codex judge-C（`gpt-5.6-sol`/`xhigh`）完成 development 与 sealed holdout 分歧裁决，development consensus 176/176 PASS；新增确定性 `diagnose-agreement`（kappa、boundary、grade gap、support agreement）。
- **Windows Stage A/B development 执行**：从 clean commit 建立 immutable checkout，经三次失败 attempt 修复链（详见「失败的尝试」）后完成全部正式运行——
  - Stage A（3 套 qrels × 13 arms）：BM25 为三套一致的最佳 lexical mechanism，E5 nDCG@5 全部显著高于 lexical，resource gates 全过；BM25 冻结 `k1=1.6,b=0.5`（maximum regret 0.000524）。
  - Thread probe：16 threads 与 1/4/8 的 12/12 payload hashes 完全相同，corpus build 加速 7.75x，后续 Windows formal evidence 采用 16 threads。
  - Stage B：BM25 `title_weight=1` 三套全优冻结；E5 按 primary/Recall/catastrophic-miss 联合证据冻结 weight 1（如实记录 judge-A 上 0.0076 的 nDCG trade-off）；phrase bonus off/on payload byte-identical 被否决；RRF-k10/k60 被 E5 全面支配被否决；cap 3/5 指标完全相同，按 payload bytes tie-break 冻结 cap 3。
  - **Finalists 冻结**：BM25 `k1=1.6,b=0.5,title_weight=1,phrase=false,cap=3` 与 E5 pinned model `title_weight=1,cap=3`。
- **Determinism 双平台验证**：Windows 10-run scoring-only replay 两个 finalists 的 payload/score hashes 10/10 唯一；Mac stable 3-run 的 payload 与 Windows byte-identical、BM25 score bytes 跨平台一致（E5 raw floats 有平台差异但排序与 observable payload 不变，符合预设 gate）。
- **执行拓扑固化**：Windows 固定承担 bulk evidence/calibration/replay/holdout，Mac 固定承担 controller/编排/单次排序；Windows 不可用时正式 run fail closed 而非静默转移。已写入根 `AGENTS.md` 与 protocol v1.3。
- **Holdout 解封与 inconclusive 判定**：按 receipts 解封三套 holdout qrels（160/160 judgments、12/12 queries），顺序 A→B→consensus 完成正式运行；qrels-stability gate 失败，未生成 12-query card，未调参/重跑/改 judge；旧 holdout 永久标记 spent/diagnostic-only。
- **v1.4 fresh evaluation 设计与预注册**：完成一手文献对抗性审查（评测构念、holdout 复用、judge 偏差、统计单位、promotion burden），方案收缩为「直接匿名比较 query + 两组 exact top-3 payload」；实现 fresh operational sampler、formal-input path、四路 tool-less 镜像 judge bundles、closed-schema validator 与 bootstrap reducer；primary 只比 frozen BM25/E5，union/full-corpus 仅作 ceiling 诊断。
- **Fresh 输入全链准备**：机械选择 fresh batch（12 cases、222 fresh candidates、8/8 clusters、strata 4/4/4、spent overlap 0）；Workshop 经三轮 draft 迭代（draft-001 因泄漏被语义复核主动拒绝，draft-003 通过）；166 篇 reference corpus 完成 abstract-only 检查；K2.7 隔离 session 起草 24 条 queries，`approved_as_is`、rewrite 0；formal input 物化 12 cases/24 queries/166 papers/187 segments，E5 token 边界全过，双 attempt byte-identical。
- **裁判资格认证（至当日结束未通过）**：完成 DeepSeek V4 Flash 裁判适配研究（确认 alias 路由到 `deepseek-v4-flash-0731`、浮动别名风险）；qualification-001/002 连续失败后完成两轮 contract 修复（公开 20-500 quote 长度约束；废弃 exact-quote gate，改为 visible segment reference + support statement 的 v1.5 evaluator overlay），冻结 qualification protocol v1.1。

## 实验结果

| 实验 | 关键指标 | 数值 | 对比 Baseline | 备注 |
| --- | --- | ---: | --- | --- |
| Judge A/B agreement（dev，176 条） | exact / linear-weighted kappa | 71.59% / 0.6561 | — | 1↔2 boundary 3.98%、gap≥2 1.14%；**both-relevant exact support 0/34** |
| Stage A relevance（3 套 qrels） | E5 nDCG@5 | 0.8555 / 0.8782 / 0.8657 | 三套均显著高于 best lexical | BM25 为三套一致最佳 lexical；resource gates 全过 |
| BM25 参数稳定性 | maximum regret | 0.000524 | `b=0.75` 的 0.002658 | 冻结 `k1=1.6,b=0.5` |
| Title weight（BM25） | nDCG@5（A/B/C） | 0.7528 / 0.7207 / 0.7118 | weight 0/2 | 三套均最高，冻结 weight 1 |
| RRF fusion | E5 − best RRF-k10 nDCG@5 | +0.0183 / +0.0702 / +0.0718 | RRF-k10/k60 | Recall 同步占优，fusion 否决 |
| Output budget | cap3 vs cap5 ranking metrics | 完全相同 | tie-break：payload bytes | cap 3（BM25 8,092→5,216 B，E5 7,777→5,084 B） |
| Windows 10-run determinism | payload hash identity | 10/10 identical | — | scoring-only，qrels 移出 boundary |
| Mac 3-run 跨平台 | payload bytes | 与 Windows byte-identical | — | E5 raw floats 有差异，排序/payload 不变 |
| **Holdout（3 套 qrels）** | BM25/E5 nDCG@5 | A 0.7646/0.7270；B 0.8406/0.8752；C 0.7997/0.8080 | winner-direction stability gate | ❌ **inconclusive**：A→BM25、B→E5、C 低于 margin |
| 测试套件 | pytest passed | 43 → 81 | 前一日 43 | Python 3.13.7/3.14.6 双通过；Black/Ruff/compileall/diff-check 全过 |

## 遇到的问题

- ✅ 已解决：Judge-C 首次 finalize 把 73-item partial adjudication 误当完整 qrels → 新增独立 `local-ranking-adjudication-qrels-v1.0` schema + 逐字段 subset 校验，失败 attempt 保留。
- ✅ 已解决：output budget 实现偏离获批协议（`total_segment_cap` 固定 6）→ schema 改为只接受 paper cap 3/5、每篇 1 segment，在任何 formal run 前修复。
- ✅ 已解决：Windows `stage-a-001` 因 Git autocrlf 把 lock 转 CRLF 导致 hash mismatch → 关闭 autocrlf + 传输 exact lock bytes。
- ✅ 已解决：Windows worker stdout 默认 GBK 无法编码真实输入中的 `U+2009` → `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8`/parent 显式 utf-8，回归测试覆盖。
- ✅ 已解决：cold-start 与 corpus-build 双重计罚 E5 → 修正测量边界为「spawn 到 scorer ready」，corpus preparation 由独立 gate 负责。
- ✅ 已解决：Workshop draft-001 泄漏具体材料/机制 → 语义对抗复核主动拒绝，draft-002/003 逐步抽象后通过；失败 bytes 保留。
- ✅ 已解决：fresh sampler 因某 stratum 无 cluster representative 在任何输出前 fail-fast → selection v1.0.1 强制每 stratum 至少一个锚点；未消耗 formal batch。
- ✅ 已解决：input approval validator 把 protocol version 硬编码为 v1.1 → 按 selection 类型分流（operational 要求 exact v1.4）。
- ✅ 已解决（定性）：qualification-001（validator 隐藏 20-500 quote 长度约束）与 qualification-002（模型仅一个括号复制误差被拒）连续失败 → 根因是 evaluator contract 缺陷而非裁判能力：约束公开化 + exact-quote gate 判定为构念错位并废弃，v1.5 overlay 冻结。
- ⚠️ 持续风险：A/B both-relevant exact support agreement 为 0/34，evidence-span 选择不稳定——synthetic qrels 永远不能宣称 gold，winner 结论必须过三套 qrels same-direction gate。
- ❌ 未解决（已转化为新方案）：holdout qrels-stability gate 失败，formal result=`inconclusive`；不允许多数票/重跑/调参修补，唯一出路是已批准并预注册的 v1.4 fresh evaluation。

## 今日决策记录

- **决策**：holdout inconclusive 后不写 Resolution、不进 production、不强选 winner；BM25/E5 保留为 frozen candidates，旧 holdout spent/diagnostic-only。**原因**：三套 qrels direction 不稳定时任何多数票或修补都会破坏 fail-closed 可信度。**替代方案**：多数票决、追加 judge、重跑择优（均被预注册规则禁止）。
- **决策**：撤回「停止 v1.4、以 BM25 为保守默认」的建议，转向 AI-consumer-first fresh paired evaluation；不再把 pointwise synthetic qrels 或 Robert 的跨领域判断作为默认最终真值。**原因**：Robert 质疑后复核确认「已批准实验完整执行」≠「科学问题已解决」，原建议在 evaluation construct 上走了捷径。**替代方案**：直接收尾（被撤回）、继续用旧 qrels 体系追加判断（旧 holdout 已 spent）。
- **决策**：执行拓扑固化为 Windows=bulk evidence executor、Mac=controller/编排/fallback，Windows 不可用时正式 run fail closed。**原因**：从第一性原理区分 bulk 成本、日常单次排序、跨平台 correctness 与远端依赖风险；Mac 只需 canonical payload 一致，不要求 raw float 逐位相同。**替代方案**：按临时可用性漂移 workload（被否）。
- **决策**：v1.4 统计单位固定为 12 cases，不把 24 queries 伪装成独立样本；E5 晋级需同时过 `Delta≥1/3`、one-sided exact `p≤0.05` 与 resolution/strata/group/catastrophic gates，否则只保留 BM25 parsimony default 且不宣称 superiority/equivalence。**原因**：双镜像判断在同 case 内不独立；预注册门槛防止看到结果后移动球门。**替代方案**：query 级统计（伪独立）、事后阈值。
- **决策**：废弃 exact-quote evidence gate，改为 visible `side/paper/segment` reference + 20–500 scalar support statement。**原因**：segment 已由 bundle hash 固定，quote substring 只测复制精度、不证明语义支持；一个括号的复制误差不应淘汰模型的 setwise 判断能力——删除它提高的是 construct validity。**替代方案**：继续 exact quote 并「重试直到抄对」（选择性成功路径，被否）。
- **决策**：query author 使用 K2.7，与裁判 panel（Kimi K3 / DeepSeek V4 Flash）角色错开；同一模型的两个位置顺序必须在互不共享上下文的 fresh sessions 运行。**原因**：避免 author/judge 角色重合与镜像 pair 上下文污染；任一 position flip 或 content choice 不一致记 `unresolved`，不得强行决胜。**替代方案**：同 session 镜像、第三票多数决（均被否）。
- **决策**：后续 Windows formal evidence 采用 16 threads（1-thread portable CPU FP32 path 保留）。**原因**：端到端瓶颈为 corpus build（7.75x 加速），四档 canonical payload 完全相同，无 observable drift。**替代方案**：8-thread（warm p95 最低但 build 更慢，收益不抵成本）。

## 失败的尝试

- **「停止 v1.4、BM25 保守默认」的建议被 Robert 质疑后撤回**——当天最重要的方向修正：已批准 matrix 没有漏跑，但停止建议在 evaluation construct 上走了捷径；随后完成补救调研并把方案改为 fresh topics 上的 AI-consumer-first paired evaluation。
- **Holdout 正式运行得出 inconclusive**：三周的 qrels/development 体系未能在 holdout 上分出 winner；按预注册规则不修补，全部 evidence 保留为 diagnostic。
- Windows `stage-a-001/002/003` 三次失败 attempt（CRLF lock bytes、GBK stdout、cold-start 计时边界），均 immutable 保留、不热补丁，从 clean commit 重建至 `stage-a-004` 全通过。
- Judge-C 首次 finalize 被拒（partial adjudication 误判）、Workshop draft-001 被拒（答案泄漏）、fresh sampler 首次运行 fail-fast——三个 fail-closed 机制都按设计在输出前拦截，未污染正式证据。
- qualification-001/002 两次裁判资格调用失败：分别暴露隐藏约束与构念错位的 gate；两次都在只消耗 1/4 调用时暂停，不允许 repair/retry，根因修复后全量重跑。

## 明日计划

- 提交 qualification v1.1 后启动 qualification-003：Kimi K3 与 DeepSeek V4 Flash 各两个 orientation 的 fresh tool-less sessions，过 24/24/mirror/side/cross-model/6-item grounding 全部 gates。
- Qualification 通过后进入 fresh formal evaluation：报告 exact prompt bytes、context 可行性、调用/retry/成本上界并取得授权后执行正式四路调用。
- 若 fresh paired evaluation 仍无材料性差异，按预注册 parsimony policy 采用 BM25 工程默认（不宣称 relevance superiority）。

## ⚠️ 异常与风险

- 停滞检测未触发：当日 26 commits（≥3）、实验结果充分、无 🆘 问题；26 条 commits 全部与 session 记录互相印证（`4218e60` 属 [13:23] holdout 工作块，`3d04d32` 为 8/30 日报入库的 meta commit）。
- **最大风险：当前没有 evidence-backed production winner。** Holdout inconclusive 后，v1.4 fresh evaluation 是唯一已批准路径，且其结论按预注册严格限于该 consumer/prompt/query/corpus 分布，不能外推。
- **裁判 panel 独立性受限**：deepseek-v4-flash 与 kimi-k3 属不同 model families，但若均由 Kimi Code 编排则不能宣称 two-provider independence；DeepSeek alias 为浮动名称，正式 evidence 只能记录 registry/响应元数据与时间戳。
- **Evaluator contract 缺陷当日两度导致资格调用失败**（隐藏约束、构念错位 gate），虽已定位修复并冻结 v1.1，但 qualification-003 尚未运行，panel 准入仍未证实。
- Session 文件 `2026-08-31_07-09.md` 跨午夜：其中 03:54 及之后的记录（qualification-003 规模修复及其后工作）对应 9/1 的 commits（`dc18caf` 起），已排除在本日报之外，归入 9/1 日报。
