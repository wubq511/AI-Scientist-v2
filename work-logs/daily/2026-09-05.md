# 工作日报 - 2026-09-05

## 今日概要

三条主线并行推进：① Proposal 002 比较矩阵从凌晨死锁修复、slot 1 零产出根因诊断出发，完成「末轮强制收敛 + slot 更替」两层修复的编码实施，并在深夜落地批内预授权与幂等重入，矩阵自动化执行基座就绪；② AI 评审体系 wayfinder ticket 01/02/03 全部完成离线实现，真实六次 smoke（DeepSeek + Kimi）全链通过，slot 1/2 双评审与 pair 1 AI 判定已入 vault；③ 矩阵真实推进：slot 1 替换 run 归账、slot 2 完成、pair 1 判定 challenger 胜。全天 48 commits，测试 766 → 896 全绿（11 个测试数节点）；生成端 ledger total 0.45 CNY；评审台账 0.47 CNY（slot 2 单条 0.24 + pair 0.23，Kimi 零边际）、smoke 全程 DeepSeek 披露 1.62 CNY（含失败尝试计费），远低于 5.00 重审批阈值与 30.00 硬上限。

## 完成的工作

**比较矩阵故障链修复（Proposal 002）**
- 凌晨修复矩阵死锁：`evidence/ideation-runs/` 补入 `.gitignore`，解除「export 产生 untracked 文件 → 下一 slot DIRTY_WORKTREE 拒绝」的死循环（`b3dbfaa`）。
- 诊断 slot 1 零产出根因：run `966d0fdc` sealed success 但 `idea_count=0`——模板/仪器缺陷（末轮指令无强制 finalize）而非模型故障；实际支出修正为 0.10 CNY。
- 从第一性原理设计两层修复并经 Robert 确认：Layer 1 controller 末轮强制收敛（同 op attempt-2 纠正重问）；Layer 2 slot 更替（quarantine write-once + forfeited spend 计入预算 + reservation seq 链 + pin supersede）。spec 发布（`docs/agents/comparison-convergence-slot-replacement-spec.md`，`a111102`）。
- 编码会话（TDD）完成 commit 1–4：gate 4 不可达代码修复、末轮收敛（控制器 + resume 重建四类中断点）、slot 更替（ledger v1.2.0→v1.3.0、quarantine、pin supersede），双轴 code-review 修复 4 项判断发现（`bf92ea0`/`2d61d6a`/`dec4058`/`a02d8cd`），全量 787 passed。
- 深夜完成矩阵自动化基座：Robert 批准规格修订后实现批内预授权（write-once schema，fail closed 不回退交互提示，`4586ea5`，896 passed）+ 幂等 reservation 重入（接受 governed epoch commit，`22bbe25`/`316dbc4`/`bdb3156`）。

**AI 评审体系（wayfinder ticket 01/02/03）**
- ticket 01 单条评审：pinned 模板 `ai-review-prompt-single-v1` + authoring contract v2 + `ai_review.py`（匿名材料包/import/validate + 证据卡 md/html 双渲染）+ 19 项测试，离线验收关票（`e3c3a84`/`6c26507`）。
- ticket 02 双评审 + 成对盲评：异 model family 隔离评审与逐维共识、确定性匿名 pair（双向盲泄漏扫描、盲映射还原）、执行 config write-once + supersede 留痕、31 项测试（`e1159c1`/`65f1381`）。
- ticket 03 接入 comparison：协议 manifest write-once 注册、AI 双通道 ingestion（混用 fail closed）、vault `ai-verdicts/` 独立通道、迁移核验工具、独立评审费用台账、8 个 CLI 子命令 + 40 项测试 + 双轴 code-review 修复 8 项发现（`196b566`→`3706b05` 共 12 commits），883 passed。
- 真实六次 smoke 全链通过：DeepSeek 侧一次过 + 两次真实失败驱动模板修订为 `single-review-v2`；Kimi K3（Robert 选定 `k3-256k`）三次串行一次全过；14 次物理调用台账披露（DeepSeek 1.6224 CNY off-peak，Kimi 订阅零边际），报告 `docs/research/ai-review-real-smoke-evidence.md`。

**矩阵真实迁移与推进**
- Robert 三项决定落账：评估协议修订注册（manifest `8dc34cfa…`）、slot 1 处置选定 AI 双评审（暴露材料以 development/diagnostic 身份参与）、评审费用不设固定总额上限（逐次确认 + 台账 + 合并披露）。
- 真实迁移启动：pin worktree `d733ffed`、六文件双侧哈希核验、ledger recency same_state；slot 1 替换 run `1143a894` 归账（actual 0.08 + forfeited 0.10）。
- slot 2 全链完成：run `8a136ac4` sealed success（1 idea）、双评审 complete_resolved（0 conflict）、pair 1 盲评四方向全过、AI verdict 入 vault——**challenger（cross-domain）胜**。
- 修复 pair 盲评误报：idea 正文内 corpus paper id（declared-grounding 自引）不再视为臂泄漏，secrets 移除 paper_id（`73e1091`/`99ee349`），884 passed。

**其他**
- 安装 prompt-engineer skill（主文件 + 6 份 references）至 `.agents/skills/`（`879a878`）。
- 4 号日报提交入库（`545638b`，14:40）。

## 实验结果

| 实验 | 关键指标 | 数值 | 对比 Baseline | 备注 |
|------|---------|------|-------------|------|
| 真实 smoke 缺陷检出 | seeded 缺陷 F1 检出 | 4/4（双模型双方向） | — | ab/ba 换位一致，均引用误述句与 C003 矛盾句 |
| 引用核验 | single 31/31、pair 37/37 | 0 分歧 / 0 误报 | — | 强制逐字核验 |
| prompt v1→v2 | AUDIT_STATEMENT_REF_REQUIRED 拒绝率 | v1 2/2 失败 → v2 1/1 通过 | v1 两次独立失败 | 机检规则正确，模板显著性不足 |
| Kimi 网关超时诊断 | 非流式 ~300s 被掐 | 3 失败/1 压线成功；流式 351.8s 成功 | — | 非限流，是 nginx 300s 硬超时 → 流式方案 |
| slot 1 替换归账 | actual | 0.08 CNY | forfeited 0.10（966d0fdc） | 2 attempts，terminal success |
| slot 2 归账 + 评审 | actual 0.13 CNY；aggregate complete_resolved | 七维全 consensus、floor clean | — | 评审台账 0.47（单条 0.24 + pair 0.23，Kimi 0.00） |
| pair 1 AI 判定 | overall/fit → challenger | content_2 胜、intrusion equal | baseline 臂 | 盲评 stable，position_flip 0 |
| 测试套件 | 全量 pytest | 766 → 896 passed | 单日 +130 | 11 个测试数节点全绿 |

## 遇到的问题

- ✅ 已解决：矩阵死锁（export 文件 tracked 阻断下一 slot）→ `.gitignore` 一行修复，最小可逆不碰合同代码。
- ✅ 已解决：slot 1 零 idea 且 gate 4 不可达（`EVALUATION_ARTIFACT_MISSING` 永不可能触发，零 idea run 会在 packet 构建爆炸）→ 诊断坐实后两层修复。
- ✅ 已解决：smoke single attempt1 输出为空（max_tokens=8192 被 reasoning 独占，已计费）→ 统一 32768。
- ✅ 已解决：prompt v1 两次触发引用审计锚定规则 → 修订 `single-review-v2` 并在合同文档披露。
- ✅ 已解决：Kimi 首调 504（slot 2 second 首调，网关超时）→ 三次复核证明 300s 硬超时非限流，流式固定为 Kimi 评审方案。
- ✅ 已解决：config 占位 model id（`kimi-k3`）与实测 `k3-256k` 冲突 → `register-review-config --supersede` 留痕修订。
- ✅ 已解决：pair 盲评 paper_id 误报 → secrets 移除 paper_id + 回归测试。
- ✅ 已解决：Kimi 预检误报 9 处 A↔B 互换 → 检查工具排布错误，非模型错误，以实际渲染请求复查为 0。
- ⏳ 待推进：slot 3–8 启动与 pair 2–4 盲评（预授权与幂等重入已就绪，slot 3 未启动）。

## 今日决策记录

- **决策**：Layer 1 用 controller 强制末轮收敛，而非改 prompt 文案。
  **原因**：slot 1 已实证「prompt 保证」是概率保证；profile 模板是被测变量必须冻结。**替代**：模板加 finalize 指令——已证伪，且改模板污染被测面。
- **决策**：forfeited spend 计入 30 硬上限与 5 重审批阈值；ledger 语义扩展为「forfeited 按 run_id 唯一、同 slot 可多条、全部计入总额」。
  **原因**：否则失败循环可绕过预算。**替代**：不计入——被否。
- **决策**：编码与迁移分会话（编码会话不执行迁移、不 push）。
  **原因**：迁移涉及私有证据处置与 Robert TTY，必须留在本源会话。**替代**：编码会话一并迁移——风险高。
- **决策**：slot 1 处置定为 AI 双评审（路径 a），暴露材料以 development/diagnostic 身份参与，Promotion 裁决明示权重。
  **原因**：人工评审不在手边且 AI 通道已验证；runbook 原文「人工+AI 混用」与 reducer 混用 gate 矛盾，经核实是文档错误，代码符合 spec。
- **决策**：评审费用不设固定总额上限，授权边界 = 逐次调用前确认 + 台账逐笔记账 + 每 slot 合并披露。
  **原因**：Robert 决策；量级依据 smoke 实测（4 对全 AI 评审 DeepSeek 预期 ≈2.3 CNY，最坏 ≈4.6）。显著超量级暂停汇报。
- **决策**：幂等 reservation 重入接受 governed epoch commit（而非仅当前 pin）。
  **原因**：reservation 写于 4586ea5、HEAD 已至 22bbe25，硬比较会自锁；与 quarantine 的 admission-commit 规则一致。**替代**：严格当前 pin——自锁不可行。
- **决策**：Kimi 评审一律流式（网关 300s 超时非限流）；DeepSeek 保持非流式 + json_object。
- **决策**：审计引用（`AUDIT_STATEMENT_REF_REQUIRED`）机检而非纯 prompt 约束、记录再验证用「文本哈希 ↔ 导入记录 ↔ 记录 judgments」三方比对、AI 材料包检索节选含非 grounding 全量论文（保证 `retrieval_release_record: complete` 审计声明为真）。

## 失败的尝试

- smoke single attempt1（max_tokens=8192 被 reasoning 独占 → 空 content）：失败 payload 已归档 `/tmp/ai-review-smoke/failed-attempts/`，按 runbook 如实记账。
- prompt v1 两次真实调用被机检拒绝：规则正确、提示显著性不足；改为 v2 后一次通过——验证了「修订即新版本」路径，而非放宽规则。
- 首次 slot 1 数据演示失误：忘记 cd 进 /tmp 副本，CLI 以 repo 为 cwd 将派生文件写入真实工作区 evaluations 目录——即查即删，未动 Robert 的 brief/draft；教训：CLI 用 cwd 做 workspace root。
- PLAN 内部矛盾两处（forfeited 条数语义、`max_attempts` 触碰「模块只有两个」字面）：按诚实记账原则解决并写入 Proposal 002 修订案偏差记录。

## 明日计划

- 全自动通道执行 slot 3–8（批内预授权 + 幂等重入就绪；串联纪律：先双评审再 ingest 再下一 slot）。
- pair 2–4 盲评 + AI verdict 入 vault，评审费用台账逐笔记账。
- 矩阵 8/8 完成后 final reduction，Promotion 裁决（含 pair 3/4 若 incomparable 的处置）。
- 跨日边界说明：5 号文件末尾两条「2026-09-06 00:xx」记录中，批内预授权与幂等重入的 4 个 commits 于 5 号 23:17–23:47 提交（已计入本日报）；Kimi 限流复核、slot 3 首跑与替换、slot 4–8 执行、pair 3/4 盲评的记录时刻在 6 号 00:xx 前后，归 6 号日报覆盖。

## ⚠️ 异常与风险

- 无严重异常：commit 48 条、实验产出充分、无 🆘 问题、无跨天未决问题。
- 已消除风险：00:29 预告的「tracked 写入冻结」纪律在 pin 基准（`d733ffe`）创建后未再实际约束主区——14:17 起 ticket 01–03 与 smoke 的开发/文档提交均在主区正常进行；22:xx 生成端迁入 pin worktree（AI-Scientist-v2-pin，HEAD 固定 d733ffe）后与主区彻底解耦，slot 2 自 worktree 执行无 pin 冲突。
- 预算风险低：生成端 total 0.45、评审台账 0.47 + smoke 披露 1.62，均远低于 5.00 重审批阈值；评审费用无硬上界属 Robert 既定授权边界（逐次确认 + 台账）。
- 诚实边界已披露：smoke 弃权路径未真实触发、无专家 gold 不报准确率；Convergence 修复的诚实后果已记录（末轮前无检索的 run 将以 RETRIEVAL_BACKSTOP_FAILED 封 failed，slot 1 形态不受影响）。

## 交叉验证备注（git 有记录、session 文件未覆盖）

- `d733ffe`（10:58）重冻结：frozen spend-ledger hash 更新至 v1.3.0（迁移序列前置步骤，后续用作 pin worktree 基准 `d733ffed`）——无对应 session 条目。
- `dcb0438`（00:14）proposal 002 plan gate approval：内容属 4 号 session 记录（`2026-09-04_12-44.md`），提交落 5 号 00:14，4 号日报未含该提交，故补注于此。
- `d5ab680`（14:17）AI-assisted ideation evaluation spec 与 tickets 文档：ticket 01 领取前的前置规格提交，未在 session 相关 Commit 中列出。
- `a1c53c7`（16:57）回填 session log commit SHAs：13:18 session 回填动作的提交自身。
- `b32b11c`/`c5d8621`/`efcf426`/`871a813`（20:21–20:24）：smoke 报告交付后的文档修订（数据治理说明拆分与撤销、smoke session log、map 决策行）——属已记录 smoke 工作的提交收尾。