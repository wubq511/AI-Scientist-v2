# 工作日报 - 2026-08-30

## 今日概要

双线推进且都有实质收敛：上午关闭 ideation-pipeline 的 10 张规划票（024/034/025/032/026/038/037/027/029/028），新建 4 份契约文档（validation matrix、promotion gate、canary/scale gates、delivery & handoff），030 完成决策但按批准的关闭条件故意保持 open；wayfinder 规划阶段基本收尾，frontier 只剩 021/030/035/036。下午到深夜推进 local ranking 原型（ticket 021）：完成 Windows dense 环境 smoke 与 runtime evidence 强制、12 cases 正式输入获批、E5 512-token 长度政策校准、24 条 queries 隔离起草与批准、formal input 物化；深夜因 Robert 否定 336 条人工 qrels 的执行设计，重新调研并提出 AI judges 的 v1.2 方向（待批准）。当天 13 个 session 文件对应 24 个 Git commits，全部可互相印证。

## 完成的工作

- **Wayfinder 规划票批量关闭**（10 张，全部经 grilling 逐题批准后落盘）：
  - 024 safe ideation entry：原地改造 `perform_ideation_temp_free.py`，六参数 new-run CLI，九步 fail-closed preflight（末道为估价批准），versioned 价格表（显式修订 022 的每运行快照），跨时段费用上界恒按高峰价。
  - 034 minimal runtime dependency：`requirements.txt` 变为本 fork canonical 最小集，上游 29 包全量清单改名保留，dev 工具独立，三文件精确 pin。
  - 025 control flow / failures / resume：模型可修复错误统一回灌（废除静默 break），环境失败挂起可 resume、契约失败终止封存的封闭二值表，原子写入与三层 controller events；发现并修订 023 orphan-artifact 规则与 resume 的冲突。
  - 032 IdeaBench evaluation fidelity：纯定性结构化对照，排除 benchmark 可比性主张；评估证据走 post-seal 独立 Evaluation Artifact，不进 run 的 Evidence Chain。
  - 026 idea quality rubric：runtime 四条 evidence-backed 检查（grounding 门槛、Declared Grounding 说谎检测、payload hygiene terminal、run 内去重回灌）+ post-seal 七项定性条目。
  - 038 declared grounding contract：扁平字符串数组参数、per-generation eligibility（收紧 026）、封闭三错误码、gate 固定优先级（hygiene 最先且 terminal）。
  - 037 post-seal evaluation artifact contract：每 finalized idea 一份 canonical JSON，封闭 enum + 非空 rationale、禁止数值分，write-once + 线性 supersedes 链。
  - 027 validation and test matrix：新建 `docs/agents/validation-matrix.md` v1.0，九层矩阵 + 阈值三类固化法，018/019/024/026 的委托阈值全部收口。
  - 029 optimization promotion gate：新建 `docs/agents/promotion-gate.md` v1.0，双 gate（Plan Gate 前置全部裁量权）、分维度 Regression Budget、Design Epoch 证据可比性规则。
  - 028 canaries and scale gates：新建 `docs/agents/canary-and-scale-gates.md` v1.0，三波结构（smoke 1 → canary 12 → 终波），¥30 硬上限，终波改按批批准（修订 031）。
  - 030 delivery and handoff：新建 `docs/agents/delivery-and-handoff.md` v1.0（交付走既有 `/to-spec` → `/to-tickets` → `/implement` 链、report 落 committed `docs/research/`、narrative 落 private `docs/task/`）；决策完成但 status 保持 open，待 spec/票单经真实使用验证后关闭。
- **Session 记录纪律修复**：`record-session-progress` 新增强制回填步骤（写入新记录前必须回填可验证的占位条目，时间戳+内容双重吻合）；派子代理对 14 个已提交 log 回填 commit SHA。另修复 `record-session-progress` 与 `daily-work-summary` 两个 skill 因 frontmatter 缺 `name` 字段在 Kimi Code 清单中静默丢失的问题。
- **Local ranking 原型**（ticket 021，含跨午夜延续工作）：
  - Windows dense 环境就绪：pinned `intfloat/e5-small-v2`（revision + weight SHA-256 双固定）下载校验，fresh Python 3.13.7 venv + per-platform hash lock，两次独立 replay 逐 query payload hash 完全一致。
  - Runtime evidence 强制：protocol 新增 frozen `environment_lock`，candidate metrics 直接携带 environment/model bytes 与 lock hash；Python socket deny guard 实现进程级网络隔离（typed `NETWORK_ACCESS_DENIED`），RRF 资源用明确标记的 conservative source-plus-fusion composition。
  - 正式输入获批：`inputs-007` 12 cases（development/holdout 各 6、分层 2 small/2 medium/2 large）、168 篇 references、12 份 Workshop 全部通过确定性校验与语义审批，`input-approval-001` 按 exact hashes 批准；protocol 升级 v1.1（scope 固定为 title + publisher abstract，全文明确为独立 v2 comparison）。
  - 长度政策校准：一手核验 E5 512-token 边界性质后，批准「只对超限摘要做所有 arms 共用的 deterministic zero-overlap full-coverage segmentation」，禁止 silent/head truncation；以 addendum 冻结，不动已 hash 绑定的 v1.1 bytes。
  - Queries 隔离起草与批准：隔离 query-author session 只读 Approved Workshop packet（逐文件 SHA-256 校验），起草 24 条 queries（12 broad + 12 focused）并冻结为可审计 manifest；controller 按 delegated authority 原样批准，不改写任何 bytes。
  - Formal input 物化：`formal-input-005/006-replay` 两个 fresh process 目录 byte-identical；生成自包含 blind qrels HTML 表单（本地自动保存、完整性互斥检查、导出封闭 schema），经真实 Playwright 浏览器验证。
  - AI qrels 方向调研：完成 NIST/TREC、UMBRELA、LLMJudge 等一手调研（`docs/prototypes/local-ranking-ai-qrels-research.md`），形成 v1.2 提案——两个隔离 AI judges 全量判断 + 第三 judge 盲裁分歧 + A/B/consensus 三套 qrels 做 winner-sensitivity gate，Robert 只做 12 条匿名 pairwise 效用选择。

## 实验结果

| 实验 | 关键指标 | 数值 | 对比 Baseline | 备注 |
| --- | --- | ---: | --- | --- |
| Windows dense smoke（E5-small-v2, CPU FP32） | cold p95 / warm p95 / build / peak RSS | 5.298 s / 28.195 ms / 0.180 s / ~423 MB | protocol v1.0 gates | 两次独立 attempt 逐 query payload hash 完全一致；model 134 MB、isolated env 837 MB |
| 长度政策 probe（12 cases / 168 papers） | full input tokens min/p50/p95/max | 67/323/567/1101 | 512 model-input 边界 | 11 篇超限分布在 5 cases；shared segmentation 得 180 segments（157×1、10×2、1×3），max=512，loss/overlap/reconstruction failure 全为 0 |
| Formal input 物化 | cases / queries / papers / segments | 12 / 24 / 168 / 180 | approved length probe | 两个 fresh process byte-identical；segment records 与 probe 的 168 条 identity 全部精确相等 |
| Ranking harness 测试 | 测试通过率 | 43/43 | 当日从 19 增至 43 | Mac ambient 3.14.6 与 reference 3.13.7 双通过；Black/Ruff/compileall 全过 |
| Tracker 收敛 | tickets / closed / open | 37 / 28 → 33 / 4 | 29 日为 36/24/11（注） | 当日关闭 10 张、新建 2 张（037/038 随后当日关闭）；frontier 剩 021/030/035/036 |

注：tracker 计数口径含当日新建票；frontier 四张中 030 为故意保持 open，035/036 待 canary，021 在原型阶段持续推进。

## 遇到的问题

- ✅ 已解决：Kimi Code 缺失两个 skill 的 slash 命令 → 根因是 SKILL.md frontmatter 缺 `name` 字段被静默丢弃（Codex 容错从目录名推导故不受影响），各加一行修复；需新开 session 生效。
- ✅ 已解决：多条 session 记录的 commit SHA 永远停留在「待提交」→ 根因是记录先于提交写入、自引用不可能，且无回填纪律；skill 新增强制回填步骤堵复发，存量 14 个 log 由子代理回填、主 session 抽查后入库。
- ✅ 已解决：023 的 orphan final artifact → corrupt 规则会把 rename→event 的微秒级崩溃窗口放大成整个 run 不可 resume → 经批准修订为 resume 时 quarantine + 记录 incident。
- ✅ 已解决：初版长度 probe（`length-001/002`）未 fail-closed 校验 tokenizer hash/依赖版本 → immutable 保留并由 hardened `length-003/004` supersede。
- ✅ 已解决：blind qrels HTML 表单的 favicon 404 与 grade 回切时 visual/state 不一致 → 由 `formal-input-005/006` supersede，Playwright 真实浏览器验证通过。
- ✅ 已解决（方向性）：Robert 否定「本人完成 336 条英文 qrels」的执行设计（负担不合理且领域经验不适合作唯一 gold）→ 重新调研后形成 v1.2 AI judges 提案；v1.1 在批准前仍是唯一 approved protocol，不能提前实施。
- ⚠️ 反复出现：`rtk pytest` wrapper 多次误报 no tests collected（跨 29/30 日多个 session），均立即回退原始 `python -m pytest` 验证；属 wrapper 观测问题，但已成固定干扰源。
- ⚠️ 后台 research 子代理两次未交付（一次窗口内未返回被中断、一次因账户用量额度失败）；主 session 均改用一手资料直接完成核验，未把二手结论当权威。
- ⏳ 未解决：正式 development/holdout ranking 仍被 v1.2 批准与后续 qrels/stability gate 阻塞；035/036 待 canary；030 待 021 关闭后的 to-spec 流程。

## 今日决策记录

- **决策**：费用合同改用 versioned 价格表（显式修订 022 的每运行快照），跨时段上界恒按高峰价、实际费用按 attempt 开始时刻所在时段结算。**原因**：官方费率固定仅分峰谷，快照机制无必要；高峰上界使跨时段无法击穿批准额。**替代方案**：每运行快照（022 原案）。
- **决策**：失败分级为「环境失败挂起可 resume、契约失败终止封存」封闭二值表。**原因**：前者保留已承诺证据且不浪费已完成 generation 的费用，后者由 023「sealed 不可 resume」自然承接。**替代方案**：一律终止（浪费证据与费用）。
- **决策**：评估只做纯定性结构化对照，禁止数值分/总分，证据走 post-seal 独立 Evaluation Artifact。**原因**：面试任务不要求复现官方指标，数值分暗示 benchmark 可比性；独立 artifact 保持「生成过程永远接触不到答案」不变式。**替代方案**：本地描述性统计（依赖成本与误读风险换不来决定性收益）、官方评估器复现（需新 provider 合同且仍不可比）。
- **决策**：Grounding 用声明式链接 + 说谎检测，eligibility 收紧为 per-generation。**原因**：唯一能让 grounding 超越「至少搜过一次」的 evidence-backed 手段；run 级校验会放行「瞎猜命中」的声明。**替代方案**：最小门槛重申、事后文本匹配。
- **决策**：优化类变更走双 gate（Plan Gate 前置全部裁量权 + Promotion Gate 判执行），预算外维度意外回退也自动 reject。**原因**：允许看到结果后移动球门会把 pre-register 纪律掏空；只盯 pre-registered 维度会漏真实回退。**替代方案**：单 gate 事后审批。
- **决策**：Canary 预算从次数改为 ¥30 硬上限，终波规模按面试需要 + 实测成本决定（预期 ~50），不承诺全量 237。**原因**：Robert 明确面试任务不需要全量；硬阻断清零 + 每个已执行 canary case 最终 success 才许扩量。**替代方案**：原 ticket 的 scaling toward all 237 框架。
- **决策**：交付走既有 `/to-spec` → `/to-tickets` → `/implement` skill 链而非自建实现 ticket map；spec/票单落点从默认 `.scratch/` 改落 `docs/wayfinder/ideation-implementation/`。**原因**：Robert 纠正；本 repo `.scratch` 未 ignore 且 handoff 要求票单可审计。**替代方案**：自建 ticket map（被否）。
- **决策**：长度政策用所有 arms 共用的 deterministic shared segmentation，原始 abstract 永久完整保留，禁止 dense-only 或 all-arm silent/head truncation。**原因**：512 是完整 encoded sequence 的 model-input 边界而非 raw abstract cap；head truncation 会删除 1,931 tokens / 8,458 source chars 且只惩罚 dense arm；统一切碎 168 篇会在无 qrels 支持时扩大 segment-count bias。**替代方案**：head truncation、all-window、长上下文模型（同时改变模型族与成本，仅在 shared segmentation 出现 decision-changing failure 时另立预注册 challenger）。
- **决策**（待批准）：qrels 改走「AI exhaustive topical qrels + multi-judge sensitivity + 12 条 Robert blind pairwise utility」（v1.2 方向）。**原因**：一手证据支持 AI qrels 用于 run-level 系统比较（TREC 2024 UMBRELA 与人工 qrels 高相关），但不支持单个 AI judge 精确区分相近 finalists；336 条人工判断超出合理个人负担。**替代方案**：single-AI gold、继续全人工、pooling/LARA（均被否）。

## 失败的尝试

- 「Robert 本人完成 336 条英文 qrels」的 v1.1 执行设计被 Robert 否定——authoritative-human 假设在规模与领域匹配上都站不住，被迫回到评测目标重新推导，产出 v1.2 提案。这是当天最重要的方向修正：此前的 formal input 物化与 blind qrels 表单仍有效（176/160 条判断的形式不变），但判断者从人换成隔离 AI judges。
- 后台 research 子代理两次未交付（窗口内未返回、账户额度失败）；主 session 直接核验一手资料完成，研究文档均标注了证据边界。
- 初版 runtime 报告曾把 Hugging Face Git blob ID 误当 weight SHA-256（29 日深夜发现，30 日随环境准备修正为官方 LFS metadata）。
- `length-001/002` 与 `formal-input-001~004` 均因校验或 UI 缺陷被 supersede——immutable attempts 全部保留，hardened 版本重新取证，未原地修补。

## 明日计划

- 等 Robert 对 v1.2 AI qrels 方向的批准；批准后按隔离要求启动 fresh projectless judge tasks（只读最小 frozen packet），生成 A/B/consensus 三套 qrels 并过 winner-sensitivity gate。
- 完成 qrels stability gate（每 split 重标 15%、至少 30 条）后运行正式 development comparison；holdout 导出保持 sealed 至 finalists/参数冻结。
- 021 关闭后按 030 契约启动 `/to-spec` → `/to-tickets` → `/implement` 交付链；030 由该 session 补写 Resolution 并关闭。
- 035/036 维持 open，随 canary 阶段共用 12-case Canary 推进。

## ⚠️ 异常与风险

- 当日 commit 数 24（≥3）、有实验结果产出、无 🆘 问题，未触发停滞告警。24 条 commit 全部可与 session 记录互相印证（6 条 ranking 相关由跨午夜的 `2026-08-29_23-41.md` 覆盖）。
- **v1.2 未批准是当前最大阻塞**：ranking 原形的所有数据 blocker 已清零，唯一剩余的是 qrels 判断者方案的方向决策；若三套 AI qrels 导出的 winner 方向不一致，结论必须为 `inconclusive`，且两个 Codex 隔离任务是 same-provider judges，不能宣称跨模型族 consensus。
- **成本数字未实测**：028 契约中的单 run 费用（低谷 ¥0.5–1.0、最坏 ¥2–4）为估算，已标注 smoke 首跑后按实测重校准；`16fde5b`（v1.2 研究）于 31 日 07:11 入库，属 30 日工作成果。
- `rtk` wrapper 观测失真已连续两日出现，验证纪律是立即回退原始命令；若其覆盖范围扩大需重新评估。
