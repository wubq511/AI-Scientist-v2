# 工作日报 - 2026-09-02

## 今日概要

Atomic v2.3 的 forced-tool 输出方案从合同冻结（11:40）到实现、bounded canary、三轮 formal-readiness 加固、evidence ledger 修订全部闭环，晚间 23:22 冻结 formal 003 的 prepare-only 合同：`call-021` 连续四次 schema-invalid 的系统性失败被证实并绕开，canary 在 tool transport 下 first-valid PASS。同日完成 9-01 日报生成与跨午夜 session 补录（55 commits 全部归属）。Fresh `pro-max-calibration-003-tool-output` 的 live 授权按合同留待次日申请。

## 完成的工作

- **9-01 日报与 session 补录**：生成 `work-logs/daily/2026-09-01.md`，55 个 commits 逐条归属（27 条 session 显式引用、16 条 freeze/record、2 条 meta、10 条晚间工作补录）；向 `2026-08-31_07-09.md` 追加四段补录记录，并经本地 Codex 会话文件（34MB rollout JSONL）校订，纠正「Pro/max r1/o1 待次日执行」为「20:40 冻结后立即启动并跨午夜运行」的事实错误。9-01 日报未上传 Notion（环境无 Notion 工具，已提示手动复制；commits `2331041`/`5eaed9a`）。

- **Pro/max continuation 修复与终结**：第一性原理复核失败门槛，删除 rationale 800 字符上限（`d87e311`）、扩展 invalid-only retry ceiling 2→4（`510f6c2`），冻结修正后的 calibration 与 continuation 合同（`f5433cb`/`cd2ffff`）；对 `call-021` 重新调用 attempts 3/4（其他 23 个 first-valid calls 未重发，request SHA-256 核验一致；旧 Pro/max run 保持 spent，未事后追认其 votes）。两次均 HTTP/SSE 传输成功但返回损坏 JSON，消耗 15,615/8,083 tokens，判定为系统性 finalization 失败并停止该路径（记录 commits `65195ac`/`c81a050`）。

- **Structured-output 方案设计与获批**：对照 DeepSeek Thinking Mode/JSON Output/strict Tool Calls 文档与 LangChain/Instructor 实践交叉验证后，形成「semantic judgment 与 serialization 分离」方案——保持 Pro/max reasoning，改用 forced `submit_judgment` tool call，本地六字段 validator 接管 canonical JSON。Robert 批准后冻结为 v2.3 合同，并生成 Kimi Code/Kimi K3 实现 handoff（commits `559e4ff`/`3137ea4`/`33deb1b`）。

- **授权边界扩展与 canary 执行**：Robert 批准扩大 Kimi 执行边界——实现、完整验证、clean commit、prepare-only hashes 就绪后可直接运行 bounded canary（1 个 synthetic forced-tool probe + 最多 4 个 spent `call-021` attempts，总 ≤5 calls，first-valid stop）。Kimi 实现 `fa2409f`（8 files，+1254/-99，156/156 双解释器）后 canary PASS：synthetic probe valid；`call-021` attempt 1 transport + semantic 双 PASS（16,675 tokens，其中 reasoning 12,636），共 2 个 physical calls（commits `1f66d3c`/`10c9069`/`7309dcb`/`59dd8ca`/`fa2409f`/`f6a6343`）。

- **Codex 独立复核与 formal-readiness 三轮加固**：不采信 agent 自报，全部 hashes 重算、raw SSE 重提取、artifacts 重放。复核后冻结 v2.3 repair 合同（`091b331`），并按用户指示撤回曾进 `AGENTS.md` 的协作分工规则——Codex 研究/决策/冻结/验收 + Kimi 经 handoff 执行有界任务的分工本次有效，但用户明确其不适用于未来任务，故保持 delegation session-scoped 而不成为长期规则（`8ce4a2c`）。发现 6 个 blocker（manifests 漏 `response_submission`、真实 v2.2 prompt replay `HASH_MISMATCH`、版本族混收 legacy receipt、response-only smoke 可 PASS、lone surrogate 抛 `UnicodeEncodeError`、runner/profile 版本滞后）→ Kimi 修复 `c4bbb2e`（187/187）；再发现 4 类缺口（runner current-family gate、smoke 证据完整性、usage/cost 账本、canary hash 可覆盖）→ 补修 `562942f`（196/196）；随后构造 metadata tamper probe 证实自洽删减的证据仍可 PASS，冻结 v2.3.2 合同（commits `6f4d020`/`cd4b04d`/`90d0f4f`/`739ea57`/`3c56550`）。

- **Evidence ledger 实现与验收**：Kimi 按 v2.3.2 实现（55 个负例先红后绿，251/251）；Codex 复核后用对抗性 probes 补齐 Kimi red run 中被 TypeError 遮蔽的 E3 实证，并按授权直接修复 3 个小缺口（transport evaluator exact binding、共享 UTF-8 decoder、writer allowlist 单源）。全程无 live call、未读取 credential、未动 frozen artifacts。终态 255/255 双解释器，full Ruff 恰剩 7 个已披露 untouched baseline（commits `5a915d0`/`4772e1f`/`8ea6443`）。

- **Formal 003 preparation 合同冻结**：Robert 批准进入下一阶段并要求后续中大任务继续经 handoff 交 Kimi 执行；23:22 冻结 prepare-only 合同（`local-ranking-atomic-formal-003-preparation-contract-v1.0.md`，commit `326f928`）——两份 source bundle、六路 current manifests、fresh artifact root、usage snapshot、profile 与 Windows execution bundle 逐文件 ledger 全部列为 exact preparation outputs；live model call 分离授权。顺带修正文档中 profile「只绑定 Pro/high」的旧表述为「绑定调用方明确请求的一种 Pro/high 或 Pro/max evaluator」。执行代码基线固定 `8ea6443`。

## 实验结果

| 实验 | 关键指标 | 数值 | 对比 Baseline | 备注 |
|------|---------|------|-------------|------|
| `call-021` continuation（json_object transport） | 结果 | attempts 3/4 均 `INVALID_SCHEMA` | attempts 1/2 同样 invalid | 各 15,615/8,083 tokens；四次上限耗尽，无 attempt 5 |
| `call-021` 累计（r1/o1 + continuation） | physical calls / tokens / response IDs | 27 / 207,216 / 27 | — | usage rolling/weekly/monthly `9/32/40%` → `10/32/40%` |
| Tool-transport canary（forced `submit_judgment`） | 结果 | synthetic valid；`call-021` attempt 1 valid | 旧 transport 四次全 invalid | 2/5 calls；attempt 16,675 tokens（reasoning 12,636）；usage `0/32/40%` → `0/33/40%`；first-valid stop |
| 测试套件 | 全量 passed | 终态 255（双解释器） | 当日从 135 逐级上升 | 135→156→187→196→251→255 |
| Ruff（full） | findings | 恰剩 7 个 untouched baseline | canary 前披露 11 个 | 本任务修掉 4 个（FURB188/RUF059/SIM117/PERF402） |

## 遇到的问题

- ✅ 已解决：`call-021` 系统性 schema-invalid（传输成功、内容损坏、自我指令泄漏）→ 换 forced tool transport，canary attempt 1 valid。
- ✅ 已解决：formal readiness 6 个 blocker → `c4bbb2e`（17:44）逐项修复并复验。
- ✅ 已解决：v2.3.1 四类缺口（自洽证据删减、family 门缺失等）→ `562942f`（19:05）。
- ✅ 已解决：v2.3.2 evidence-ledger 缺口（ledger 无法证明绑定实际发送的 request、自洽删减的成功/失败证据可 PASS）+ 3 个小修 → `5a915d0`/`4772e1f`/`8ea6443`（22:35）。
- ⏳ 未解决（按合同等待）：fresh `pro-max-calibration-003-tool-output` live 调用未授权——prepare-only 合同已冻结，待 Codex 冻结 bundle/profile/usage/r1 budget 后向 Robert 单独申请。

## 今日决策记录

- **放弃继续提高 exact-request retry ceiling**：四次独立 transport-success/schema-invalid 足以判定系统性 finalization 失败，继续重复没有合理信息增益。
- **首选 forced `submit_judgment` tool transport**（Robert 批准）：semantic judgment 与 serialization 分离，六字段 schema 放 tool arguments；替代方案为按 DeepSeek 官方要求补 JSON example 的 prompt-only 方案（留作唯一 fallback，canary 机器失败才启用）。
- **Kimi 授权边界扩大**：实现验证后可直跑 5-call disposable canary（synthetic 先于 stress、串行、硬停止）；独立复核保留在正式 144 logical / 576 physical evidence 边界。Fresh calibration 与 fallback 仍单独授权。
- **Ruff gate 改差分门槛**：本次涉及文件必须 zero-error；7 个 untouched baseline findings 完整披露但不阻塞，避免为实验无关清理扩大 scope。
- **不重跑成功 canary**：formal metadata/validator 修复必须保持 tool schema、prompt、HTTP request 三组 hashes 不变；任一 bytes 改变才重新评估 probe。
- **账本缺口修复不引入模型质量门槛**：只验证执行身份与 evidence ledger 完整性，不改变 rubric/packet/model/effort/retry/semantic gates。
- **Profile 冻结 144/576，live 授权只给 r1 mirror pair（48/192）**：r1 FAIL/incomplete 停止，PASS 也不自动进入 r2/r3；pre-registered scientific gate 不变。
- **9-01 日报视角严格限定 9-01**（commits `dc18caf`–`98c7431`）：Pro/max 原 run 与其 continuation 属 9-02 范围，不混入；补录写进跨午夜文件并标注「2026-09-02 补录」。

## 失败的尝试

- **DeepSeek Pro/max + `response_format=json_object`（`call-021` attempts 3/4）**：与有效 `call-020` 的 raw SSE/finish reason/visible content 对照复核，确认 stream parser 未丢字段（`finish_reason=stop` 正常、无 token truncation、reasoning 通道正常），失败发生在 provider final structured submission——损坏 key + 自我指令泄漏。判定该 transport 组合系统性失败，弃用。
- **JSON Output 官方合同与 strict Tool Calls 调研结论（研究性否证）**：DeepSeek 官方要求 prompt 内嵌完整 JSON 示例（当前 prompt 只有字段规则）；strict tool calls 依赖官方 beta endpoint 而 OpenCode Go 不承诺透传——二者均不采用，前者降级为唯一 fallback。

## 明日计划

- Kimi 执行 formal 003 prepare-only：六路 preparation、usage/profile freeze、Windows bundle verification 后停止交回。
- Codex 独立复核 preparation 证据后，向 Robert 申请 r1 mirror pair live 授权（上限 48 logical / 192 physical）。
- 授权后冻结执行顺序与 r1 early-stop budget，启动 fresh `pro-max-calibration-003-tool-output`。

## ⚠️ 异常与风险

- 无重大异常。停滞检测：30 commits（≥3）；有实验产出；无 🆘 问题；无跨日未决问题。
- 需要留意：DeepSeek Pro/max 的 JSON-object transport 系统性失败已绕开但未根治（tool transport 下行为尚待 fresh 003 大规模验证）；formal 003 live 授权仍悬置，属合同内等待而非阻塞。