# Session 记录 - 2026-09-05 03:31

**[03:31] Proposal 002 remediation 编码实施完成（handoff 编码会话）**

**工作内容：**
- 按 `/tmp/ai-scientist-v2-handoff.hrEPFn/`（HANDOFF.md + PLAN.md）以 TDD 完成比较矩阵收敛保障与 slot 更替机制的编码实施，边界=commit 1–4 + 全量验证，不执行迁移序列。
- **勘察阶段**：确认 PLAN.md 全部 7 条关键事实；发现 PLAN 未写明的重建不变量冲突——`rebuild_resume_plan` 的 per-op 校验禁止"final state 之后再出现 attempt/第二个 final state"，而纠正调用要求复用同 op attempt 2。设计决定：显式放宽该不变量（仅允许一次 post-final 纠正延续），并以"2-final op ↔ 对应轮首个 outcome 必为 final_round_correction"的交叉校验封住滥用面。
- **Commit 1（`bf92ea0`）**：gate 4 修复。`ingest_comparison_result` 的 coverage 检查由 `is None` 改为 `if not coverage`（`_evaluation_coverage_for_run` 只返回 bool，`EVALUATION_ARTIFACT_MISSING` 自引入不可达）。TDD：新增 `run_zero_idea_sealed_run` 合成助手（响应脚本对 Layer 1 两侧稳定）+ 零 idea run 全链路 ingest 红测试，抽 `_synthetic_matrix_parts` 共享夹具。
- **Commit 2（`2d61d6a`）**：Layer 1 末轮强制收敛。controller 生成循环扩一个 virtual iteration（`range(start, num_reflections+1)`）：末轮 model-fixable 结果记录封闭 outcome `final_round_correction`（末轮搜索 = 新错误码 `FINAL_ROUND_FINALIZE_REQUIRED`，检索不再执行），同 op attempt 2 发起纠正重问（`execute_round` 新增 `max_attempts` 参数禁用内部传输重试，物理 attempts ≤ 2）；closure `_record_fixable_error` 统一 12 处调用点；arm 条件含 `corrective_attempt == 1`（attempt 2 已被传输重试占用则记普通 model_fixable_error，否则 resume 会把重试响应误判为纠正响应）。rebuild：outcomes_g 放宽为 [final_round_correction, X] 两 outcome、correction 分支处理 E1（attempt 2 未启动→PendingReexecute 2）/E2a（orphan→3）/E2b（suspend→3）/E3（纠正响应已提交→PendingResponse 重派发）四种中断点，`_recovered_finished_event` 提取复用，RoundResume 带 feedback 字节重建纠正 request。5 个新测试含 resume 字节等价（attempt 2 与 attempt 3 request.json 逐字节相等）与 happy path 零纠正后缀断言；更新 6 处既有测试脚本（末轮违规补纠正响应）。
- **Commit 3（`dec4058`）**：Layer 2 slot 更替。ledger v1.2.0→v1.3.0（`forfeited_entries`+`forfeited_spend_cny`，`ingest_forfeited_comparison_cost`，`_validate_ledger_arithmetic` 扩展重算，硬上限/重审批/投影全部用含 forfeited 总额）；`quarantine_comparison_run`（write-once `0600` `comparison-run-quarantine-v1.0.0`，六类前置 fail-closed）；`reserve_comparison_slot` 序号化（seq = 1+quarantine 记录数，`comparison-run-reservation-v1.2.0`，`supersedes_reservation_sha256` 链）；`supersede_execution_code_pin`（旧字节逐字入 `superseded/`，write-once 记录，ingest 旧 epoch 仍 `EXECUTION_CODE_PIN_MISMATCH`，quarantine epoch 校验覆盖 superseded pin）。15 个新测试含 CLI 腿与合成 E2E（零 idea → quarantine → 重跑 → 8 run → packet）。
- **双轴 code-review**（Standards/Spec 并行子代理）：无硬违规；修复 4 项判断发现（commit `a02d8cd`）：纠正响应再选 SearchLiterature 不再执行检索（与纠正指令"检索已不可用"一致，记 `FINAL_ROUND_FINALIZE_REQUIRED`）；feedback artifact attempt 坐标改为真实响应 attempt；`resumed_corrective` 谓词去重；virtual iteration 死渲染消除；纠正载体 tuple→NamedTuple。
- **Commit 4（docs，本条提交）**：Proposal 002 追加"收敛保障与 Slot 更替修订"（根因/两层设计/schema bump/slot 1 处置/spec 链接/偏差记录）；Validation Matrix 新增 VM-CONTRACT-COMPARE-06；ticket 02/03 post-close 补记；本记录。回填 `2026-09-05_00-29.md` [01:23] 条目 SHA=`3cc6936`。

**结果：**
- ✅ 全量 `python -m pytest -q` **787 passed**（766 基线 + 21 新增：gate 4 ×1、Layer 1 ×5、Layer 2 ×15）；`compileall`、改动文件 black 全过。
- ✅ 新增测试计数：21；既有测试因末轮合同变化补纠正 stub 共 6 处（sealed_ideation ×2、sealed_non_success ×4）。
- ✅ 零 provider 调用、零费用、不读 `.env`；run-matrix/commands 生成输入零改动；两臂 profile 模板与 30.00/5.00/7.08 数字零改动。
- ⚠️ PLAN 内部矛盾（已按诚实记账原则解决并记录）：ledger bullet 说 run_index 至多一条 forfeited，但 reservation seq 设计要求 seq-3 → 第二条 quarantine → 第二条 forfeited。采用"forfeited 按 run_id 唯一、同 slot 可多条、全部计入总额"，已写入 Proposal 002 修订案偏差说明。
- ⚠️ 规格字面"修改的模块只有两个"与"纠正调用禁用内部传输重试"冲突：adapter `deepseek.py` 新增 `max_attempts`（默认值保持既有行为），已记入修订案偏差记录。
- ⚠️ 行为后果（诚实 fail-closed）：若末轮之前从未成功检索，末轮搜索被拦后 run 以 `RETRIEVAL_BACKSTOP_FAILED` seal failed（既有 run 级 backstop 合同自然生效）；slot 1 形态（前轮已检索）不受影响。

**问题与状态：**
- ✅ 编码实施全部完成（commit 1–4），迁移序列未执行——按边界留待 originating session：重冻结 → pin supersede → quarantine `966d0fdc` → 交付同一条 frozen slot 1 命令。
- ⏳ 未推送 commit：`bf92ea0`、`2d61d6a`、`dec4058`、`a02d8cd` + 本次 docs commit，由 originating session 统一处理。

**决策记录：**
- 纠正 outcome 采纳 `final_round_correction`（spec 语言"outcome code"）+ 保留原始 error_code，而非 PLAN 字面的"新增封闭错误码"单一方案：证据完整性优先，末轮搜索这一新违例类才有真新码 `FINAL_ROUND_FINALIZE_REQUIRED`。
- 纠正响应的 dispatch 复用轮次 dispatch（记第二 action_outcome）而非静默 budget_exhausted：live 与 resume 重放共用同一代码路径，事件链完整记录每次模型响应的处理结果。
- resume 对"纠正 attempt suspend"采取再执行（attempt 3+，ticket-025 既有语义 + resume 独立成本审批），不改 budget_exhausted：模型未答的纠正问句应被诚实补问。

**相关 Commit：**
- `bf92ea0` fix: enforce comparison evaluation coverage gate
- `2d61d6a` feat: force final-round convergence in ideation controller
- `dec4058` feat: add comparison slot replacement with forfeited-spend accounting
- `a02d8cd` fix: tighten final-round correction dispatch and evidence coordinates
- 本条目回填：待提交
