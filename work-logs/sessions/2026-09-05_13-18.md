# Session 记录 - 2026-09-05 13:18

**[13:18] 完成 AI 评审单条模式（wayfinder ticket 01）离线编码验收**

**工作内容：**
- 按交接文档（AI-REVIEW-DESIGN-HANDOFF.md）领取 [01-deliver-single-idea-ai-evaluation](docs/wayfinder/ai-assisted-ideation-evaluation/tickets/01-deliver-single-idea-ai-evaluation.md)，先读规格/map/票面与 `evaluation.py`/`comparison.py`/现有测试，确认 comparison ingestion 只消费 v1 人工 artifact 的顶部版本，AI 记录放 `ai/` 子目录不会破坏 ticket 03 兼容面。
- 用 prompt-engineer 技能编写 pinned 单条评审模板 `ai_scientist/ideation/policies/ai-review-prompt-single-v1.md`（任务角色、材料即数据边界、七维「判断什么/不得推导」表、弃权规则、JSON 输出契约、五个合成边界例），SHA-256 pin 进 `contract.py` 新版本族（authoring contract `evaluation-authoring-contract-v2.0.0` 等 7 个常量）。
- 新增 `ai_scientist/ideation/ai_review.py`：`export_review_package`（匿名两层材料包：model-visible payload 只含包内 `S###` source ID + Workshop/idea 七字段/Target Comparator/审计范围声明/检索操作**全部**论文节选；外层绑定 run/seal/idea 哈希与私有 paper_id registry；无时间戳、重复导出逐字节一致）、`import_review_response`（write-once 保存原始响应，provenance 结构上恒为 `user_supplied`，解析失败 exit 1 保留原文）、`validate_ai_review`（包重推导比对、七维枚举 + 独立 `insufficient_evidence` 弃权、引用 source 存在 + quote 逐字核验、judged 无引用须「推断：」标记、线性 supersedes、write-once 记录 + 中文证据卡）。
- CLI 新增 `evaluation export-review-package` / `import-review-response` / `validate-review` 三个子动作；`test_ideation_import_contract.py` 期望闭包加入 `ai_review` 模块。
- 新增 `tests/test_ai_review_evaluation.py` 19 项测试，覆盖八个边界 fixture 类（正常/缺材料弃权/伪造引用/引文改写/注入文本惰性/合法领域方法与 ML framing 模板规则/换位材料确定性/无效响应）+ CLI seam + v1 人工 artifact 共存。
- 文档：`docs/agents/ai-review-authoring-contract-v2.md`（v2 authoring contract + CLI 使用说明）、CONTEXT.md 三个新术语（AI Review Record / Review Material Package / Evidence Card）、validation-matrix 追加 VM-QUAL-02 行与版本化修订说明、离线验证报告 `docs/research/ai-review-offline-validation-evidence.md`。
- tracker 关票：ticket 01 全部验收项勾选 + Resolution，map `Decisions so far` 追加条目；回填 12-20 记录的 commit SHA（`879a878`）。

**结果：**
- ✅ `pytest tests/test_ai_review_evaluation.py -q` 19 passed；`test_post_seal_evaluation + test_ideation_import_contract` 60 passed（零回归）；全量 `pytest -q` **806 passed in 103.59s**（基线 787 + 19）。
- ✅ black（改动文件）/compileall 通过；全仓 black 未过的 14 个文件均为上游遗留模块（treesearch/、llm.py 等），不属于本票范围，未触碰。
- ✅ 未发生任何真实模型调用或付费生成；未触碰 slot 1。

**问题与状态：**
- ✅ 已解决：测试早期失败 3 处均为测试自身断言过宽/草稿残留（"baseline" 出现在 idea 正文属合法材料；fixture 检索操作含 grounding 之外论文恰是「完整释放记录」预期；清理时误删 `record_bytes` 行），修正后全绿。
- ⏳ 真实 smoke（六次基础评审调用）未执行：需 exact model 配置、费用上界与数据出站授权；离线 fixtures 只证明软件契约，已在报告与关票 Resolution 中如实单列。

**决策记录：**
- 材料包检索节选取「sidecar 绑定操作返回的全部论文」（含非 grounding，逐条标注）而非沿用 v1 brief 的 grounding-only：保证 `retrieval_release_record: complete/recorded` 审计声明为真，并扩大 contamination 检查面；v1 函数与测试保持不动，新增并行收集器。
- 审计事实从 run 链可验证面派生（hygiene 对 finalized idea 通过 / workshop 语义审批 / 释放记录 / provider 侧污染审计 incomplete），`provider_training_contamination_audit` 恒报 incomplete，防止评审越界断言。
- 导入 provenance 不提供「工具已执行」选项：文件导入无法认证调用方，恒标 `user_supplied`；将来 transport 集成路径再以新 provenance kind 版本化。
- 包不做本地语义打分/新增 coverage 状态：单评审记录不进 v1 coverage，两位评审 complete_resolved/complete_unresolved 核算归 ticket 02。

**相关 Commit：**
- `e3c3a84` feat: add single-idea AI review with pinned prompt and evidence card
- `6c26507` docs: record AI review authoring contract v2 and offline validation evidence

---

**[14:05] 两轴 code-review 修复 + slot 1 真实数据管道演示**

**工作内容：**
- 按 code-review skill 对未提交变更跑 Standards/Spec 两个并行审查 agent，13 条发现；修复 9 条：
  - `AI_REVIEW_RESPONSE_IMPORT_SCHEMA_VERSION` 从 ai_review.py 归位 `contract.py`（合同「统一 pin」声明与实现一致）；
  - 抽取 `evaluation._collect_release_record` 共享收集器（v1 `_collect_retrieval_excerpts` 变为 grounding 过滤包装，v1 测试不改一字全过），ai_review 收集器从 ~90 行重复瘦身为一层包装 + grounding 覆盖检查；
  - 抽取 `_derive_package`（导出/重推导共用派生路径）；`_existing_versions` 参数化 pattern/label 后复用于响应列举；
  - `review-request.txt` 增加 `REVIEW_REQUEST_DRIFT` 校验（import/validate 双端，先于响应落盘）；
  - 新规则 `AUDIT_STATEMENT_REF_REQUIRED`：contamination_signal/leakage_review 为 judged 时必须引用 audit_statement 来源（范围性结论锚定审计事实，模板本有此指令，现在机检）；
  - 补「引文存在但不支持」fixture（claim 超范围仍落盘且如实标 not_performed）与跨 run 引用 fixture（A run 响应在 B run 包上引用核验失败）；
  - Promotion Proposal 002 追加版本化 addendum（只登记工具存在与效力边界，不改 pre-registered 判据）；合同文档修正 exit-behavior 与 request 漂移说明。
- 未修复并记录为判断项：CLI handler 结构性重复（与文件既有约定一致）、ai_review 对 evaluation 私有 helper 耦合（ticket 02 v2 coverage 时收敛）。
- slot 1 真实数据演示：run/workshop/数据集/policies 复制到 /tmp throwaway 副本跑通 export→import→validate；同 run 两工作区导出 SHA-256 一致（`3af4a332…`）。⚠️ 过程失误一次：首次忘了 cd 进副本，CLI 以 repo 为 cwd 把 review-package.json/review-request.txt 写进了真实工作区 evaluations 目录——即查即删（仅这两个派生文件，Robert 的 brief/draft 未动），教训是 CLI 用 cwd 做 workspace root，跑命令前必须确认 cwd。
- 证据卡渲染增加规范七维顺序（原按响应键序展示，可读性差）；demo 响应标注 offline-demo/fixture-response-not-a-model，不回填真实矩阵。

**结果：**
- ✅ 聚焦 23+60 passed；全量 **810 passed**（等最终后台确认）；black/compileall 通过。
- ✅ 真实工作区 evaluations 目录恢复原状（仅 brief.md + draft.json）。

**决策记录：**
- 审计引用规则选择机检而非纯 prompt 约束：spec 要求 none_found 限于实际审查范围，把「引用 audit_statement」变成确定性校验是语义支持不可机检前提下唯一可锚定的部分。
- 演示用 /tmp 副本而非真实工作区 in-place：fixture 响应生成的记录不能留在生产 slot 的评审区，避免 ticket 03 接入时误读 provenance。

**相关 Commit：**
- `e3c3a84` feat: add single-idea AI review with pinned prompt and evidence card
- `6c26507` docs: record AI review authoring contract v2 and offline validation evidence

---

**[14:15] 证据卡新增浅色单文件 HTML 渲染**

**工作内容：**
- Robert 反馈 md 卡阅读费劲且不需要暗色模式：`validate_ai_review` 现在同时落盘 `evidence-card.md`（合同文本版）与 `evidence-card.html`（浅色单文件网页版）；HTML 自包含（无脚本、无外部资源、无 prefers-color-scheme 媒体查询），动态文本全部 HTML 转义，维度中文名 + verdict 色标（绿/黄/红/蓝，仅阅读辅助，锚定 pre-registered 质量底线维度），弃权维度缺材料内联高亮，假设/缺失信息用原生 details 折叠；两卡同一记录的确定性渲染，记录为权威。
- 测试补 HTML 断言（自包含/浅色/无脚本/维度标签/弃权高亮），合同 v2 文档与离线验证报告同步更新；playwright 截图自检排版通过后经浏览器交付 demo 卡。

**结果：**
- ✅ 全量 810 passed；slot 1 demo 卡 HTML 版已生成并打开给 Robert。

**决策记录：**
- 色标只按 pre-registered 质量底线维度映射（mismatched/unsound/leak_found/signal_found/name_dropped 红色等），recover/materially_different 用方向中立蓝，避免渲染层偷偷引入规格反对的「重叠方向性排名」。
- 不建 Web 服务/平台：validate 直接写静态 HTML 文件，浏览器直接打开，符合规格「不新增 UI」的精神（是渲染格式，不是交互系统）。

**相关 Commit：**
- `e3c3a84` feat: add single-idea AI review with pinned prompt and evidence card
- `6c26507` docs: record AI review authoring contract v2 and offline validation evidence

---

**[16:30] 完成 AI 评审双评审与成对盲评（wayfinder ticket 02）离线编码验收**

**工作内容：**
- 领取 [02-deliver-independent-and-pairwise-review](docs/wayfinder/ai-assisted-ideation-evaluation/tickets/02-deliver-independent-and-pairwise-review.md)：设计并落地双评审（两个异 model family evaluator 的隔离评审 + 逐维共识汇总）与成对盲评（两匿名 idea 的可换位 pair + 盲映射还原）。
- prompt-engineer 编写 pinned 成对模板 `ai-review-prompt-pair-v1.md`（`pair-review-v1`：A###/B###/C### source 约定、三项判断定义含 tie≠incomparable、引用与「推断：」规则、五个合成边界例），与新增 7 个 schema 版本常量一起 pin 进 `contract.py`。
- `ai_review.py` 扩展：write-once 评审执行配置（`register-review-config`，强制两 slot 异 family/异 exact model id、pinned prompt 绑定、可选 real_call_authorization 声明）；import/validate slot 化（`--evaluator-slot`，记录迁入 `ai/primary/`、`ai/second/`）；`aggregate_review`（逐维 consensus/conflict/abstained/incomplete_evaluator + coverage 四态 + pre-registered 质量底线 + 中文共识卡 md/html）。
- 新增 `ai_pair_review.py`：确定性匿名 pair 包推导（content_1/content_2 按 (run_id, idea_index) 排序、blind mapping ab/ba、C###/A###/B### source 编排、私有 registry；key+文本双向盲泄漏扫描）、`export-pair-package`/`import-pair-response`/`validate-pair-review`/`reduce-pair-review` 四上下文链路、盲映射还原（stable winner/tie 或 incomparable + position_flip/evaluator_conflict/incomparable_judgment/missing_valid_record 原因分类学）、每臂质量底线独立携带、中文还原报告 md/html。
- CLI 六个新子命令接线；import-contract 闭包加入 `ai_pair_review`（stdlib-only 保持）。
- 测试：迁移 `test_ai_review_evaluation.py` 到 slot 布局并新增 config/双评审 14 项（共 37）；新建 `test_ai_pair_review.py` 17 项（导出 golden/匿名性/换位对称/四上下文/跨方向阻断/还原七场景/质量底线携带/CLI 全链路）。
- 文档：authoring contract v2 追加双评审与成对章节（布局/新 schema/CLI）；新建 `docs/agents/ai-review-smoke-runbook.md`（六次调用计划 + config 模板 + 报告字段 + 禁止表述）；`docs/research/ai-pair-review-offline-validation-evidence.md` 离线证据报告；validation-matrix 新增 VM-QUAL-03 + 修订说明二；CONTEXT.md 6 个新术语；关票 ticket 02（10 项验收全勾 + Resolution）+ map.md Decisions。

**结果：**
- ✅ `pytest tests/test_ai_pair_review.py tests/test_ai_review_evaluation.py -q` 54 passed；import contract 通过；全量 `pytest -q` **841 passed**（基线 810 + 31）；black（本票改动文件）/compileall 通过。
- ✅ 真实 smoke 未执行（无付费授权），runbook 作为可执行入口交付；无任何真实模型调用。

**问题与状态：**
- ✅ 已解决：`register_review_config` 漏建 evaluations 根目录（STORAGE_WRITE_FAILED）；`aggregate` 响应完整性检查误用「文件字节 vs 文本哈希」比对——改为深度再验证（文本哈希 + 重解析一致 + 记录 judgments ≡ 响应重推导三方比对，pair 侧同修）；`_check_config_binding` 键名不兼容两类记录块；pair 模块漏导入 `_load_context_and_comparator`；共识卡 HTML evaluator_rows 混入非 tuple 元素；reduce 误用完整响应校验器验记录 judgments；同 workspace 两次 seal 的 fixture 复用（write-once corpus/workshop id 冲突 → 缓存已批准输入 + 空 commit 容忍）。
- ✅ 已解决：black 全仓重排误伤 17 个上游历史文件（HEAD 本就不是该版本 clean），已 `git checkout` 恢复，本票只保留改动文件格式化。
- ⏳ 真实六次 smoke（单条 ×2 + 合成 pair ×4）待授权执行。

**决策记录：**
- per-slot 布局（`ai/<slot>/`）而非共享目录加 slot 字段：隔离是规格硬要求，目录级隔离让「第二位看不到第一位」成为存储结构性质而非约定；repo 无已提交 AI 记录，迁移零成本。
- pair 显示侧枚举（a_better/b_better/tie/incomparable + a_more/b_more/equal）对齐 comparison.py 既有 pair-relative 语义，盲映射还原到内容空间；稳定结果要求「内容收敛」而非「显示位收敛」，position_flip 天然成为还原失败原因。
- 记录再验证策略：聚合/还原时对绑定响应做「文本哈希 ↔ 导入记录 ↔ 记录 judgments」三方逐字比对——单验证记录 judgments 无法检测「响应文件被改但记录完好」的篡改，重跑完整校验 + 规范比对是 fail-closed 语义下最强的机检面。
- 跨 case `PAIR_CASE_MISMATCH` 不写专项 fixture（同 workspace 两 case 需复制异 case run 目录，工程量大且属防御性检查），验收按票面七类 fixture 覆盖。

**相关 Commit：**
- 待提交
