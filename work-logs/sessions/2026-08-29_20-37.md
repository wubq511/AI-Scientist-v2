# Session 记录 - 2026-08-29 20:37

**[20:37] 进展记录**

**工作内容：**
- Robert 明确启动 Wayfinder ticket `Define run identity and evidence layout`；按本地 tracker 规则将 ticket assignee 从 `null` 设置为 `Robert`。
- 读取 Wayfinder、grilling、domain-modeling、record-session-progress、`CONTEXT.md`、相关已关闭 contracts、当前 ideation 入口和相邻 work logs，建立 run identity 与 evidence layout 的 design tree。

**结果：**
- Ticket 已认领，`git diff --check` 通过；当前仅修改 ticket assignee，尚未写入 Resolution、修改 runtime code 或调用模型。
- 当前入口把 idea archive 固定写到 Workshop 旁的 `<workshop>.json`，启动时会直接重读同一文件；没有 `run_id`、call identity、独立 evidence root 或 cross-run guard。
- 第一轮 frontier 聚焦 identity hierarchy、resume/replay lineage、Evidence Chain 权威源与 raw/sanitized trust zones；这些都仍等待 Robert 决策。

**问题与状态：**
- ⏳ 进行中：在 shared understanding 得到明确确认前不会关闭 ticket；controller 的 action/retry/resume 状态机仍留给 `Define control flow, failures, and resume`。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`

---

**[22:39] 进展记录**

**工作内容：**
- Robert 批准第一轮 Q2–Q4，并要求从第一性原理重新审查 Q1 的 ID 模型，因为原推荐存在边界模糊与命名杂乱。
- 重新区分领域实体身份、run 内顺序、进程写入 ownership、逻辑 operation、物理 attempt、事件位置与跨 run 分析关系，检查哪些概念真的需要独立 ID。

**结果：**
- Q2 锁定 immutable event envelopes 与 per-run immutable artifacts 为 Evidence Chain 权威源；派生视图不具有更高权威。
- Q3 锁定 private gitignored raw root 与 allowlist-generated sanitized Git root 两个 trust zones；二者只通过 opaque run identity 与 hashes 连接。
- Q4 锁定真实 Ideation Run 必须从 clean Git worktree 启动并 pin exact commit SHA；dirty worktree preflight fail closed。
- ⚠️ 原 Q1 的 `invocation_id`、`call_id`、`attempt_index`、event identity 与 `parent_run_id` 混合了身份、顺序和授权语义；当前不视为已批准，改为最小身份模型重新 grilling。

**问题与状态：**
- ⏳ Q1 仍开放；下一步需要 Robert 判断精简后的两级 opaque identity、run-scoped sequence 与禁止 prior-run runtime input 的建议。

**决策记录：**
- 批准 Q2–Q4；Q1 明确重开，不沿用上一轮推荐。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`

---

**[22:49] 进展记录**

**工作内容：**
- Robert 批准精简后的 Q1 identity model；将已消除的领域歧义同步到 `CONTEXT.md`，不在 glossary 中写入 UUID、路径或 schema 等实现细节。

**结果：**
- 全局 opaque identity 只保留 `case_id` 与 `run_id`；writer ownership、logical operation、physical attempt、immutable event 和 pipeline position 分别使用 run-scoped `writer_epoch`、`operation_seq`、`attempt_seq`、`event_seq` 与 indexes。
- 删除 `invocation_id`、`call_id`、`event_id`、`artifact_id` 和 `parent_run_id`；artifact 由 run-relative path 与 SHA-256 引用，跨 run comparison 由 run 外 evidence 显式列出 run IDs。
- `Ideation Run` 现在明确为固定 run specification 下的一条隔离 Evidence Chain，可跨合法 resume 延续，terminal seal 后结束；rerun/replay 必须新建 run。
- `Run Isolation` v1 明确禁止 prior-run runtime input；未来若确有需求，必须另立 narrower declared-import contract。

**问题与状态：**
- ⏳ 下一轮仍需决定 lifecycle documents、private/sanitized per-run layout 与 immutable event envelope。
- ⚠️ RTK 无法干净代理一次带多个 pathspec 的 `git diff`，已回退原始 `git diff`；检查通过，未影响文件。

**决策记录：**
- Q1 按精简模型批准；此前宽泛 ID hierarchy 正式废弃。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`

---

**[22:56] 进展记录**

**工作内容：**
- Robert 批准 Q5 run lifecycle/layout 与 Q6 immutable event envelope 推荐；同步在 `CONTEXT.md` 增加 Run Admission、Run Seal 和 Evidence Event 的业务定义。

**结果：**
- Private raw evidence 固定在 gitignored `artifacts/ideation-runs/<run_id>/`，sanitized tracked evidence 固定在 `evidence/ideation-runs/<run_id>/`；v1 不允许任意输出 root。
- 每个 raw run 使用 immutable `request.json`、条件存在的 `admission.json`、one-file-per-event ledger、run-local artifacts、non-authoritative projections 和 terminal `seal.json`；approved external inputs 只 pin exact path/hash，不整包复制。
- Sanitized export 只针对 sealed run，核心为 allowlist-generated `manifest.json` 与 `events.json`。
- Evidence Event 使用严格 versioned envelope；event、operation、attempt 和 pipeline positions 均使用 run-scoped sequences/indexes，大 payload 通过 immutable artifact refs 连接，unknown type/field fail closed。

**问题与状态：**
- ⏳ 剩余 hash graph/canonical bytes、non-circular seal、cross-run/path/resume guards 与 corruption handling 尚待 grilling。

**决策记录：**
- Q5、Q6 全部按推荐批准。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`

---

**[22:59] 进展记录**

**工作内容：**
- Robert 批准 Q7 canonical hash graph/non-circular seal 与 Q8 filesystem/cross-run isolation 推荐。

**结果：**
- 所有 canonical JSON 使用统一 deterministic bytes；artifact 使用 exact consumed/stored bytes SHA-256，events 使用 `prev_event_hash` 串联，首 event 为 `null` predecessor。
- Terminal event 先固定 final event hash，再创建引用完整 artifact inventory 的 `seal.json`；terminal event 不引用 seal，seal 不自带 self-hash，sanitized manifest 外部记录 seal hash，从而消除循环。
- Runtime 仅使用固定 roots 和 runtime-generated ASCII paths；new run exclusive-create，resume 只能 exact `run_id`，禁止 latest/glob/case lookup、symlink、absolute/parent paths、跨 run reads 和 shared mutable state。
- Sanitized exporter 只读取已 sealed 的同 ID raw run；已有 export 仅在 byte-identical 时可视为幂等成功，任何差异禁止覆盖。

**问题与状态：**
- ⏳ 最后一轮需要决定 corrupt/unsealed run 行为、resume/seal/export verification gates、raw retention 与 sanitized privacy release gate。

**决策记录：**
- Q7、Q8 全部按推荐批准；不引入 Merkle tree、shared content-addressed store 或 arbitrary output roots。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`

---

**[23:01] 进展记录**

**工作内容：**
- Robert 批准最后一轮 Q9 resume/seal/corruption gate 与 Q10 raw retention/sanitized release gate 推荐。

**结果：**
- Resume 和 seal 前必须验证 lifecycle documents、完整 event hash chain、artifact references/inventory、exact admission inputs/commit、writer ownership 与 terminal dispositions；projection mismatch 只能丢弃并重建。
- Canonical evidence 一旦缺失、断链、hash mismatch、非法或出现 orphan final artifact，run 标记为 corrupt；禁止 repair-in-place、resume、seal、export、replay source 和 evaluation/promotion，保留原证据并新建 run。
- POC 阶段 success、failure 和 preflight rejection raw evidence 均永久本地保留，无自动删除、迁移、上传或备份；raw 丢失后 sanitized summary 不能再支撑 replay/evaluation/promotion。
- Sanitized exporter 从空对象按 positive allowlist 构造，无任意 free text；通过 strict schema、canonical/hash linkage、forbidden-content scans 与 versioned exporter/validator gate 后才有资格进入 Git。

**问题与状态：**
- ⏳ Design tree 已清空；等待 Robert 确认完整 shared understanding，确认后才写 ticket Resolution、关闭 ticket并更新 Wayfinder map。

**决策记录：**
- Q9、Q10 全部按推荐批准。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`

---

**[23:06] 进展记录**

**工作内容：**
- Robert 确认完整 shared understanding 并要求完成后提交；将批准合同写入 `Define run identity and evidence layout` Resolution，关闭 ticket，并在 Wayfinder map 的 Decisions so far 增加 context pointer。
- 在 `CONTEXT.md` 固化 Ideation Run、Run Specification、Run Isolation、Run Admission、Run Seal 与 Evidence Event 的非实现性领域含义。
- 提交前复核并修正两个文档问题：session log 追加顺序倒置；captured external/model-visible JSON payload 必须 hash exact bytes，不能被项目 canonical JSON rendering 重新序列化。

**结果：**
- ✅ Tracker validation 通过：36 tickets，24 closed，12 open，4 个 frontier；所有必填 frontmatter、blocker references、closed-ticket Resolution、map indexes 与当前 ticket/map 相对链接有效。
- ✅ `git diff --check` 通过；没有 runtime code、模型调用、credential 读取、付费运行或 downstream 工作。
- 本次待提交文件为 `CONTEXT.md`、ticket、Wayfinder map 与本 session work log。

**问题与状态：**
- ✅ Ticket 规划已完成；仅剩创建 commit 并复核 SHA/worktree 状态。

**决策记录：**
- 最终合同采用最小两级 opaque identity、run-scoped coordinates、固定双 trust roots、immutable event hash chain、non-circular seal、strict cross-run guards、no-repair corruption policy 与 positive-allowlist sanitized release gate。

**相关 Commit：**
- `97a0c48 add: define run identity and evidence layout`
