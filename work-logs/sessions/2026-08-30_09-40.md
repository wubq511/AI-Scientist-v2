# Session 记录 - 2026-08-30 09:40

**[09:40] 进展记录**

**工作内容：**
- 解决 wayfinder ticket [Define control flow, failures, and resume]（025）。先向 Robert 说明 ticket 意图，确认开工后认领，grilling 两轮共 10 个决策问题，Robert 全部按推荐采纳。
- 决策内容：baseline 循环结构逐字保留（006/024）；所有模型可修复错误（parse 失败、未知 action、参数 JSON 非法、FinalizeIdea 结构违规、retriever input error）统一作为该轮 tool result 回灌，废除 baseline 的静默 break 与 print-only；检索预算结构性等于 `num_reflections`，FinalizeIdea 需本 generation ≥1 次非空检索（per-generation gate）+ 020 run 级 backstop；generation disposition 封闭枚举 `finalized`/`budget_exhausted`，run terminal outcome 三枚 `success`/`failed`/`preflight_rejected`；022 十六类 typed failure 按「同 spec 下 resume 是否有意义」映射为 suspend（环境类，unsealed 可 resume）/ terminal failed（契约确定类，seal 后新 run_id）封闭二值表；SIGINT 立即 abort + per-operation 原子性；resume CLI `resume --run-id`，023 gates + 重放重建控制态 + 剩余工作费用重估重批；原子写入 staging→fsync→hash→rename→event；controller events 三层结构（lifecycle / generation-round / operation-attempt），精确 schema 照 034 先例留实现 ticket 落地。
- 发现并解决一处合同冲突：023 的 orphan final artifact → corrupt 规则会让 rename→event 微秒级崩溃窗口秒杀整个可 resume 的 run；经 Robert 批准修订为 resume 时 quarantine + 记录 incident，已在 023 追加修订指针（照 024→022 先例）。
- 四个新术语入 CONTEXT.md：Terminal Outcome、Run Suspension、Generation Disposition、Model-Fixable Error。
- 将 Resolution 写入 ticket 025 并关闭，map 的 Decisions so far 追加一行 gist。

**结果：**
- ticket 025 关闭，map 更新，commit `42aeb20`（仅含 CONTEXT.md、map.md、023、025 四个文件）。未修改任何 runtime code（本 map 期间仅限规划）。

**问题与状态：**
- ✅ 已解决。工作区有其他 session 的未提交改动（032 修改、新 ticket 037、`prototypes/local_ranking/environments/`、两条 work log），未纳入本次提交。
- 027（validation/test matrix）仍被 026（idea quality rubric）阻塞，025 关闭后未进 frontier。

**决策记录：**
- 失败分级选「环境失败挂起可 resume、契约失败终止封存」而非一律终止：前者保留已承诺证据且修复后不浪费已完成 generation 的费用，后者由 023「sealed 不可 resume」自然承接。
- resume 费用选「剩余工作重估重批」而非把中断消耗进 operation 的 2-attempt 预算：后者会把外部中断变成模型可见失败，且 resume 重执行可能产生第 3 次物理 attempt、击穿原批准上界。
- 检索预算不设独立计数器：每轮恰一个 action，轮次即预算，最少机制满足 020 的版本化 budget 要求。

**相关 Commit：**
- `42aeb20` add: define control flow, failures, and resume contract
