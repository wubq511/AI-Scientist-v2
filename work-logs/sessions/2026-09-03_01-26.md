# Session 记录 - 2026-09-03 01:26

**[01:26] 进展记录**

**工作内容：**
- 按 Codex handoff（`/private/tmp/ai-scientist-v2-formal-003-r1-live-kimi-handoff-20260903-0039.md`）与 amendment v1.0
  （commit `25beeb2`，SHA `566e10b8…7789`）执行 formal 003 r1 live calibration，边界内零额外调用、未用 subagent。
- Preflight 全绿：HEAD=`17583c8` 干净；runtime `*.py` 与 `8ea6443` diff 为空；amendment/archive/profile/ledger/
  freeze-summary 五个 hash 全部复核一致；六个 live 输出路径初始均不存在。fresh extraction（`/tmp/f3-live-r1-jtkLRsSj`）
  跑 `verify-bundle.py` 五步 PASS（输出 SHA `3c25ccd4…6e96a`）。`snapshot-usage` 写入
  `controller/live-r1-usage-before.json`（SHA `05b0f1f0…3dbd`，rolling 7%/weekly 36%/monthly 42% 全 ok，模型在列）。
- r1/o1 live：frozen `atomic_runner`（extraction 内代码，locked Python 3.13.7 `-B -X utf8`），24 logical 全部 attempt-1
  valid，0 retry，24 个 provider response ID 全唯一；trace SHA `9ae53999…4da7a`。随后无凭证 offline resume gate：
  不传 `--kimi-config` 且 unset env，runner 返回逐字节同一 completed run-result（证明 credential lookup 前已从
  evidence 解析）。
- r1/o2 live：25 physical / 24 logical。call-021 attempt-1 被 deterministic validator 判 invalid
  （`PROVIDER_RESPONSE_FAILED`，`finish_reason=length`，tool stream 未正常完成、无 provider response ID、无 receipt）；
  exact same request 重试 attempt-2 valid。其余 23 个 attempt-1 valid。trace SHA `d151e780…6cebd`。
- `atomic_pair` r1：stable_count=20/24（<22 阈值），stable_directional_count=13，**decision=`stop_profile_failed`**
  （合同明示的合法终止态）。unstable：lr-dev-02/04/05-focused、lr-hol-06-focused。pair SHA `5e26f1e7…7dfd`。
  跨 orientation response ID 唯一性由 pair fail-closed 校验通过（48/48 unique）。
- usage-after 快照 PASS（SHA `adc7d730…7356`，rolling 12%/weekly 42%/monthly 45% 全 ok）。
- 生成 `controller/live-r1-execution-summary.json`（canonical，SHA `c31c3285…a607`）：48 logical/49 physical、
  48 valid/1 invalid/0 exhausted、receipt token 合计 prompt 151,664/completion 187,484/reasoning 163,195/total 339,148
  （provider receipt schema 无 cost 字段，如实记 null）；含 per-attempt validity、hashes、failures、deviations=[]。
  已校验 summary 不含任何 endpoint/secret 字符串。
- 打包 `controller/live-r1-output.tar.gz`：SHA `00ecfc5d…9f44`，3,291,324 bytes，1,183 文件；全 output tree grep
  无 Bearer/Authorization/api_key 字符串，credential-free。
- Windows offline replay（windows-executor skill 流程）：archive 传入新子目录 `tmp\f3-live-r1`，certutil 核 SHA 一致，
  短路径解包 1,183 文件对账；用 prep 阶段已验证的 frozen bundle/runtime（Python 3.13.7 `-B -X utf8`）跑本次新写的
  `verify-live-r1.py`（先在 Mac 对同一 evidence dry-run PASS 后才上传）：o1/o2 无凭证 re-enter runner 均返回与 evidence
  一致的 completed run-result，fresh scratch 重生成 pair 与 evidence 逐字节一致。verifier 输出 SHA `befaa484…929f`，
  已回传 `controller/windows-live-r1-verifier-output.json`；Windows 侧只清 scratch，保留任务 evidence 目录。

**结果：**
- ✅ r1 两路完整，终止于 `stop_profile_failed`（stable 20/24 未达 22 阈值）；r2/r3 未授权也未执行。
- ✅ r1 用量 48 logical / 49 physical，远低于 48/192 上限；全部 attempt 证据（executions、attempt ledgers、round
  checkpoints、resolved traces、run results、raw SSE）完整保留。
- ✅ Mac offline gate + Windows offline replay 双重证明：completed run 可从 evidence 无凭证、无网络解析。
- ⚠️ call-021/o2 attempt-1 因 provider 端 `finish_reason=length` 失败一次（16384 max_tokens 截断 tool stream），
  按 frozen retry policy 用 exact same request 重试成功；属合同内确定性重试，非 deviation。

**问题与状态：**
- ✅ 全部合同步骤完成，无 failure/deviation 遗留。首次 tar 解包对账时误用 `find /c ""`（空串恒 0 且 exit 1）造成一次
  假告警，改用反斜杠计数确认 1,183 文件无误——纯对账命令问题，与 evidence 无关。
- ⏳ 本条记录、ticket 更新与 00-16.md 回填待提交为一个最小 documentation commit。

**决策记录：**
- 未在 Mac 对 o2 跑额外 offline gate：amendment 只要求 o1 的本地 offline resume 作为 o2 前置；o2 的离线重放由 Windows
  步骤覆盖。严格按授权清单执行，不加戏。
- summary 中 cost 记 null 而非 0：receipt schema 本无 cost 字段，写 0 会虚构数据。

**相关 Commit：**
- 待提交
