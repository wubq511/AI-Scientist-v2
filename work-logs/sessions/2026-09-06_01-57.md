# Session 记录 - 2026-09-06 01:57

**[01:57] 进展记录**

**工作内容：**
- 接收 `/private/tmp/zcode-handoff-002-promotion-adjudication.md`，复核当前 HEAD `fecf922`、工作区初始 clean、判据文档、相关 session log、四份 pair reduction 与两份 vault AI verdict。
- 修正判据文档的两类文字错误：pair 2 intrusion 方向应为 challenger 更少；incomparable 不计入任何一方胜场。保留原始 write-once 工件不变。

**结果与可信依据：**
- pair 2 原始 reduction 与 vault verdict 均为 `unjustified_ml_intrusion = content_2`，该侧 run `d65859af-779f-4e68-a327-ec5c0d9e5eeb` 为 baseline；authoring contract 的 `a_more` / `b_more` 与 `comparison.py` 角色投影共同证明该值表示 baseline 侵入更多、challenger decreased。
- pair 3 三维 incomparable；pair 4 overall stable challenger，但 fit/intrusion incomparable。两份 stable vault verdict 的现状与交接一致；本次仅直接读取工件，未重跑 validate/reduce 或模型调用。
- 两份最近 session log 未发现待提交或 SHA 占位表述需要回填。

**问题与状态：**
- ⏳ Robert 尚未选择接受现状、全四对人工评判或新 Design Epoch；本次接收文件不视为已批准任一方案。
- 建议接受现状并以 non-promote/inconclusive 收尾；slot 1 暴露材料的裁决权重仍须由 Robert 明示。建议只作 diagnostic，不提供晋升支持。
- 文档勘误不改变晋升门槛或评审合同。未执行正式最终 reduction，未提交或推送。

**相关 Commit：**
- 5008b07 docs: record post-evidence adoption of cross-domain-v1 and baseline retirement（文档勘误与记录）

---

**[01:59] 进展记录**

**工作内容：**
- Robert 提出不应被硬门槛僵硬限制，目标是效果比旧方案更好；使用 think Skill 重新评估采用价值。
- 重新读取规格的问题定义与 reducer，直接统计四份原始 reduction 的 overall 方向判断：pair 1/2/4 各为 4 次 challenger、0 baseline；pair 3 为 3 次 challenger、1 tie、0 baseline。同一 pair 内评审和换位结果相关，不能当作 16 个独立样本。

**结果与判断：**
- 先前「只能接受不晋升并归档」的建议过度绑定完整 verdict 数，遗漏了 pair 4 稳定 overall 胜这一可用证据。现有证据支持 cross-domain-v1 更有采用价值，但四案例和 AI 评审不足以证明跨领域普遍优越；pair 1 的已暴露材料仍只作辅助依据。
- 位置不稳定主要为胜与平之间切换，不能直接解释为新方案更差。fit 与 intrusion 的未知维度仍保留未知。
- 建议方向调整为采用 cross-domain-v1 作为后续工作的候选默认，保留 baseline 便于回退；采用决定应明确依据本次已观察证据，原协议不通过的事实保持不变。最终修改默认前仍需形成与当前规格相容的明确修订。

**问题与状态：**
- 本轮为判断与建议，未修改默认、评审算法、规格门槛或原始工件，未发起新运行。
- 推荐修订决策方式，未把 Robert 对原则的表达扩张为对具体实现或新增费用的批准。

**相关 Commit：**
- 无。

---

**[02:11] 进展记录**

**工作内容：**
- Robert 批准采用 cross-domain-v1，并明确否决保留 baseline 回退：后续用不上，应直接抛弃。此批准覆盖本轮默认 profile 和 baseline 运行能力的修改。
- `profiles.py` 默认改为 cross-domain-v1，新增唯一执行 profile 解析；`admission.py` 在分配 run 之前拒绝 baseline，错误码 `PROMPT_PROFILE_RETIRED`；CLI `--prompt-profile` 可省略，默认 cross-domain-v1。
- controller 去掉重复 baseline prompt 常量与默认 helper 路径，并在读取 admission 后拒绝 baseline 执行；resume 在补写 crash-window 事件、申请费用与新 writer epoch 之前拒绝 baseline，不把旧 run 改成新 profile。
- 修复 admission 前中断的 request 重建：legacy schema 无 profile 固定解释为 baseline 并拒绝恢复，新 schema 必须保有合法 profile hash 字段；移除对 slots dataclass 类级 descriptor 的默认值回退。
- baseline 的冻结身份、模板和 registry hash 仅用于旧 Evidence Chain 校验/导出。没有 fallback 开关，没有改写原始运行或评审工件，没有重新 supersede 已结束矩阵的 pin，也没有恢复 035。
- 更新 Proposal 002（state promoted、decision_basis post-evidence-adoption）、判据报告、Prompt spec、Promotion Gate v1.1 定向修订、Validation Matrix 与 tracker 当前状态。原协议未过门槛的事实保持；本次采用不是伪造 reducer 成功或追溯改判。
- 普通运行测试转为 cross-domain；历史比较夹具仅在生成旧的合成证据时局部模拟退役前规则，离开 factory 立即恢复当前执行限制。保留原始比较 reducer 契约测试。

**结果与可信依据：**
- 首轮定向测试 56 passed / 1 failed：测试改用 cross-domain 后，篡改 request 的夹具忘了同步另一 profile 的 bundle hash，先触发 `PROMPT_PROFILE_HASH_MISMATCH`。修正夹具后 lifecycle 15 passed，仍验证身份交叉核对，不放宽产品检查。
- Python 3.14.6 首轮全量 897 passed（127.20s）；这是补入 admission 前退役边界之前的兼容性结果。
- Python 3.13.7 reference，pytest 9.1.1、httpx 0.28.1、black 26.5.1，按仓库 pinned requirements 通过 uv 隔离运行。profile lifecycle + suspend/resume 定向 51 passed（14.60s）。
- Python 3.13.7 最终全量 **899 passed（115.40s）**：`uv run --no-project --python 3.13 --with-requirements requirements.txt --with-requirements requirements-dev.txt python -m pytest -q`。包含新建默认、baseline 退役、admission 前后恢复、原比较证据消费与历史导出。
- 在实际工作区直接调用只读 `validate_evidence_chain(..., check_sealed=True)`，四对共 8 个真实 run 全部 valid，包括 4 个 baseline；未调用 export、validate-review、reduce-pair-review 或模型。
- ideation 代码、入口及涉及测试 Black 检查通过（43 files unchanged）；`compileall -q ai_scientist`、CLI help 和 `git diff --check` 通过。
- 全仓库 `black --check ai_scientist` 报告 14 个未改动的 legacy/downstream 文件格式问题（llm/vlm、tools/semantic_scholar、perform_llm_review、treesearch 等），本次不进入下游代码修改。不可宣称全仓库 Black 通过。

**决策记录：**
- 采用方向与实际任务匹配及已观察改善优先；原预注册门槛仍按原意义记录，不为凑够胜场重跑。
- 保留历史解码不等于保留执行回退：新建、admission 前/后恢复、controller 直接执行四处均证明 baseline 不能调用模型。
- slot 1 暴露证据仅作 diagnostic 辅助；采用理由不依赖该对，不能把四个案例及相关换位判断包装成普遍优越的证明。

**相关 Commit：**
- 707ba22 add: adopt cross-domain-v1 as the sole executable profile and retire baseline execution
- fb6e5c3 test: cover baseline retirement, cross-domain default, and legacy-evidence decoding
- 5008b07 docs: record post-evidence adoption of cross-domain-v1 and baseline retirement
