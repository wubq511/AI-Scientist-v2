# Session 记录 - 2026-08-30 18:59

**[18:59] 进展记录**

**工作内容：**
- 从第一性原理核验 `intfloat/e5-small-v2` 的 512-token 边界：对照 pinned revision 的 `config.json`、`tokenizer_config.json`、`sentence_bert_config.json`、model card、Transformers tokenizer/BERT 官方文档与源码，以及 E5 原论文 Appendix B / Table 11。
- 新建 `docs/prototypes/e5-length-policy-research.md`，区分 model position/input boundary、tokenizer metadata、训练长度与 evaluation 长度，并比较 head truncation、shared segmentation、长上下文替代模型三条路线。
- 根据主线程提供的只读 aggregate（未接触 private input）评估当前数据规模：168 papers 中 11 个超限、最长 1101 tokens；记录 head truncation 的 source loss 与 zero-overlap full-coverage segmentation 的 segment 分布和 determinism 证据。
- `research` 后台任务另行启动的辅助子代理在工作窗口内未交付文件，已中断；研究任务本身使用已直接核验的一手资料完成文档，没有依赖其未返回的结论。

**结果：**
- 结论：512 应作为加入 E5 prefix 与 special tokens 后的单次 model-input hard supported boundary，不是 raw abstract/corpus cap；当前 primary policy 应只对超限摘要做所有 arms 共用的 deterministic sentence-aware、zero-overlap、full-coverage segmentation，禁止 dense-only 或 all-arm silent/head truncation。
- 12 个唯一外链逐一 `curl -L` 验证，HTTP status 全部为 200；研究文档 205 行。未运行 ranker，未读取 Target/corpora/references/qrels，未修改协议或 runtime code。

**问题与状态：**
- ✅ 研究与文档完成。⚠️ E5 论文给出的是 model card 所指向的 E5 training recipe（pre-train 128、fine-tune 192、evaluation 512）；公开一手资料没有独立的 `e5-small-v2` checkpoint-specific v2 training-length manifest，文档已明确这一证据边界。

**决策记录：**
- 保留 512 作为 pinned E5 arm 的 fail-closed admissibility invariant；原始 publisher abstract 永久完整保留。共享分块用于隔离 scorer 差异；长上下文模型会同时改变模型族与成本，仅在 shared chunking 出现 decision-changing failure 时另立预注册 challenger。

**相关 Commit：**
- `24fa354 add: calibrate ranking length policy`（19:02 提交，含 `e5-length-policy-research.md`，时间戳与内容吻合）

---

**[19:01] 进展记录**

**工作内容：**
- Robert 质疑 512-token 上限是否必要；按 `research` 与 `prototype` comparison 流程，从模型边界、数据完整性和 scorer fairness 三层重新审查，并在运行前预注册 reject/truncate/shared segmentation/all-window/long-context/drop-dense 六条路线与淘汰规则。
- 新增 throwaway `length_policy_probe`：绑定 `inputs-007` approved corpus hashes、pinned E5 revision、exact `tokenizer.json` SHA-256 和 `tokenizers==0.23.1`；对实际 `passage: ` + source text（含 special tokens）non-truncating 计数，只对超限摘要执行 sentence-boundary-first、whitespace/character fallback、zero-overlap、full-coverage segmentation，并保存可回链 offsets/hashes。
- 新增 3 项 deterministic segmentation tests，更新 input preparation/readiness、ticket 021、prototype README；以 addendum 形式冻结长度政策，不修改已被 `input-approval-001` hash 绑定的 v1.1 protocol bytes。
- 按 `record-session-progress` 强制回填，将 `2026-08-29_23-41.md` [17:24] 的 commit 占位替换为 `4fc50ff add: approve local ranking inputs`。

**结果：**
- Exact probe 覆盖 12 cases / 168 papers：full input tokens min/p50/p95/max 为 67/323/567/1101；11 篇分布在 5 cases 中超过 512，不是早期 whitespace-word audit 猜测的 4 篇。
- Head truncation 会删除 1,931 input tokens / 8,458 source characters。Approved shared segmentation 得到 180 segments：157 papers×1、10×2、1×3；maximum=512，loss/overlap/empty/reconstruction failure 全为 0。
- Hardened `length-003/004` 两个 fresh-process `result.json` byte-identical，SHA-256 均为 `fedd7fc747f587903d1c4f5329ec59c0242e9ac57a7c48f72cee70a97ea79083`，segmentation SHA-256 为 `d97997e46d302595dcc04f3056cd6c754f184e98c83adbc819bc74bd60eafb5f`。
- Mac ambient Python 3.14.6 与 Python 3.13.7 reference suite 均为 39 tests passed；Black 26.5.1、Ruff 0.16.5、compileall 与 `git diff --check` 全部通过。v1.1 protocol SHA-256 仍为获批值 `23d9b6d05d57f015a34f9bc75fa0bf9b0dee0ab2afa01c6b646b5d27a422f91c`。

**问题与状态：**
- ✅ 512 的层级已澄清：它是 pinned E5 完整 encoded sequence 的 architecture/official inference support boundary，不是 raw abstract/corpus cap；公开一手资料没有独立 v2 checkpoint-specific training-length manifest，未夸大训练证据。
- ✅ 初始 `length-001/002` 虽有相同 aggregates，但没有 fail closed 验证 tokenizer hash/dependency version；immutable artifacts 保留并由 hardened `length-003/004` supersede。
- ⚠️ `rtk pytest` 再次错误报告 no tests collected，立即回退 raw `python -m pytest`，39 tests 实际通过；ambient Python 没有 Black，改用 pinned `uvx` tool run。
- ⏳ 正式 queries 仍须由只读 query-author packet 的隔离新任务起草；formal input materialization 与 Robert blind qrels 尚未完成，不能运行 development/holdout。

**决策记录：**
- 批准 shared segmentation：原始 abstract 永久完整保留，只有 exact over-limit paper 分段；所有 arms 使用同一 spans 与预注册 `max` paper aggregation。禁止 dense-only 或 all-arm head truncation。
- 不把所有 168 篇统一切碎：157 篇已合法，额外切分会在无 qrels 支持时扩大 segment-count/max-opportunity bias。结果须单列 11 篇 multi-segment subgroup；若 winner 只靠该 subgroup 翻转，标记 sensitivity risk。
- 不在本轮改成长上下文模型或删除 dense；两者同时改变模型族/资源或 candidate matrix，应只在 shared segmentation 出现 decision-changing failure 时另立预注册 challenger。

**相关 Commit：**
- `24fa354 add: calibrate ranking length policy`（19:02 提交，含 probe/tests/addendum/ticket 更新，时间戳与内容吻合）
