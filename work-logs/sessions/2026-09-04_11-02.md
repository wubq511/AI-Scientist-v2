# Session 记录 - 2026-09-04 11:02

**[11:02] Ticket 12（Seal 后逐 idea 定性评估）实现、审查修复与关票**

**工作内容：**
- 响应 `/implement ticket 12` 请求，交付 post-seal Evaluation Artifact 能力（VM-QUAL-01，合同 032/026/037）：
  - 新建 `ai_scientist/ideation/policies/idea-quality-rubric-v1.json`：七项 criterion + 封闭 verdict enums 的 canonical JSON 策略文件，SHA-256 pin 进 `contract.py`（`EVALUATION_RUBRIC_POLICY_PATH/_SHA256/_SCHEMA_VERSION`），篡改即 `POLICY_DRIFT`；
  - 新建 `ai_scientist/ideation/evaluation.py`（约 1200 行）：
    - `assemble_evaluation_brief`：硬前提 sealed + non-corrupt（先 `validate_evidence_chain(check_sealed=True)`）；idea/grounding/sidecar 按 seal inventory path+hash 逐字节核验；retrieval 摘录严格沿事件链取回模型实际所见 payload；Target Paper comparator 经 Workshop Manifest 绑定推导 + target 数据源 dataset/row 双级 hash 校验；产出确定性 `brief.md`（无时间戳、可覆盖重建）与预填 linkage 的 `draft.json` skeleton（已存在绝不覆盖）；
    - `validate_evaluation_artifact`：全确定性 fail-closed 校验（封闭 schema、全 hash linkage、七项封闭 enum、非空 rationale、authoring audit、`brief_sha256` 防 stale、linear supersedes），通过则 O_EXCL write-once 提交 `v%04d.json`；失败不留定稿、draft 不动；
    - `list_evaluation_coverage`：只读三态核算（covered 要求 supersedes 链 head 过 `mode="final"` 同套校验），corrupt/unevaluable run 单列且 ideas 不进三态统计；
  - CLI 增加 `evaluation assemble|validate|list-coverage` 子命令（结构化输出 + exit 码契约）；`.gitignore` 补 `artifacts/evaluations/`；准入导入闭包清单与 `--help` baseline hash 门禁同步更新；
  - 新建 `tests/test_post_seal_evaluation.py`：44 项测试（StubTransport 全零网络零费用），含 21 项负向 draft 参数化、supersedes 链、coverage 三态/异常 run/read-only 证明、CLI seam、terminal `failed` run 已 committed idea 可评估场景（037.5）。
- 编写证据文档 `docs/research/post-seal-evaluation-evidence.md`（脱敏 schema/coverage 说明 + 运行说明）。
- 双轴 code-review（Standards + Spec 两个并行子代理）后修复最实质 finding：`_collect_retrieval_excerpts` 对畸形 `artifact_refs`/`payload_ref`/`payload` 形状的未防护 dict 访问可能抛裸 `KeyError`/`TypeError`，违反 037.8 全确定性 fail-closed——修复为逐层形状守卫（`RUN_CORRUPT`/`EVALUATION_LINKAGE_INCONSISTENT`），新增 15 项测试钉住（13 项参数化负向 + 2 项边界）；同轮小修：`_load_run_context` 的 `assert isinstance` 改 `fail("RUN_CORRUPT")`、rubric schema 版本常量归入 `contract.py`、coverage summary 补 `unevaluable_runs` 计数防高估覆盖率。
- 关闭 Ticket 12：ticket 文件六项全勾、补 Resolution 段、status 置 closed。

**结果：**
- 专用测试集 `tests/test_post_seal_evaluation.py` 59/59 passed；
- 全量回归测试：628 passed（基线 569 + 本票净增 59），无任何回归；
- `compileall` 与触及文件 `black` 全部通过（全树 14 处既存 upstream black 漂移经 stash 验证为基线既有状态，与本票无关，按最小改动纪律不动）。

**问题与状态：**
- ✅ 已解决：code-review 发现的 fail-closed 形状守卫缺口（裸 traceback 风险）；
- ✅ 已解决：中途误删 `_run_legacy` def 行换行的操作事故，已修复并验证；
- ✅ Ticket 12 验收完成并关闭；实现序列 ticket 13 解锁。

**决策记录：**
- code-review 发现的重复模式（`_write_bytes_*`/`_load_idea_evidence` 与 admission/resume 同构）决定不重构：repo 惯例本身接受此同构（CLI handler 五处同构是先例），且去重会触碰 admission/resume 等本票边界外模块，违反最小改动纪律；
- `(context, run_id)` 参数对不重构进 RunContext：属 pre-existing 设计，超本票范围。

**相关 Commit：**
- 待提交
