# Session 记录 - 2026-09-03 20:18

**[20:18] 进展记录**

**工作内容：**
- 按 Robert 指定的 `implement` skill 开始 implementation ticket [04: Admit an Ideation Run without paid work](docs/wayfinder/ideation-implementation/tickets/04-admit-an-ideation-run-without-paid-work.md)。发现 ambient Python 3.14.6 缺 `anthropic`（非合同环境）导致 `test_ideation_cli_baseline` 误红后，用 `uv` 重建 Python 3.13.7 clean venv 确认开工基线 `298 passed` 全绿。
- 以 TDD 三个 slice 落地准入边界：`run_store.py`（exclusive-create run root、write-once request/admission、canonical bytes、连续 event hash chain）→ `pricing.py` + `deepseek-cny-price-table-v1.json`（versioned 价格表、hash pin、峰谷时段、上界/逐 attempt 计价、向上取整到分）→ `admission.py` + `retrieval.py` + 入口 `new-run` 子命令（九步 fail-closed preflight、exact-`yes` 交互批准、`preflight_rejected` 证据、import 副作用消除为惰性工厂）。
- 入口改为子命令结构：无子命令打印 help、`new-run` 为安全入口、legacy 基线保留为显式 `legacy` 子命令（expand-contract，13 票收除）；`--help` stdout 基准 hash 更新并消除 stderr 中的 SemanticScholar warning（0 字节）。
- 更新 import closure 双层 allowlist（内部模块 7→14）、`.gitignore` 增加 `artifacts/ideation-runs/`、`AGENTS.md` 运行说明同步；CLI baseline hash pin 更新为 `1175cbe4…`。
- 固定点 `add45bd` 双轴 code review：Standards 8 项 + Spec 10 项。全部修复于 `631e209`（schema 校验前移至 run 创建前、corpus validation report/inventory/versions 全验、路径接入 `workspace_relative_path`、删除超合同 `--non-interactive` 与死代码、统一 `_now`）；修复后双轴复核：Spec 轴 0 findings，Standards 轴余 2 项，由 `6783c46` 关闭（`_mapping_field` 使 manifest 非 dict 字段 fail closed 而非 AttributeError 逃逸、删 `run_store._now` 与死 helper、`RunHandle` 类型签名）。
- 测试 harness 关键发现：macOS pty 提前 close master 会丢弃未读缓冲使 `yes` 变 EOF；改为写后保持 master 打开。`attempt_cost` 期望值手算错误（5.13）被实现纠正为 4.98——测试期望值来自独立复算而非实现镜像。
- 写脱敏证据 [run-admission-evidence.md](docs/research/run-admission-evidence.md)，勾选 ticket 04 前五项并链接证据，implementation map 增加 04 决策条目；最后一项验收留 Robert。

**结果：**
- ✅ 新增 30 项测试（store 9 + pricing 7 + preflight 14）全绿；全量 `328 passed in 44.86s`（开工基线 298 全保持）；compileall exit 0；Black 触及范围 24 files unchanged（repo-wide 14 个既有 legacy 债务不变）。
- ✅ `--help` exit 0、stderr 0 bytes、stdout SHA-256 `1175cbe491d2e8736bda75a68101f74818e56d85184249588d40a1085cdd9977`；价格表 SHA-256 `f02bab04…` 代码硬绑定。
- ✅ 交付 `VM-UNIT-02`、`VM-UNIT-03`、`VM-UNIT-06`、`VM-CONTRACT-023-01`、`VM-CONTRACT-024-02`；双轴 review findings 全部修复并复核关闭。
- 零模型调用、零网络、零费用；未进入 BFTS 或其他 downstream 阶段；`DEEPSEEK_API_KEY` 只做存在性检查，fixture 用占位值。

**问题与状态：**
- ⏳ Ticket 04 保持 `implemented; awaiting Robert acceptance`，最后一项验收（Robert 逐票验收）待完成；ticket 状态、`status: open`→`closed` 转换由 Robert 验收后进行。
- ⚠️ 真实获批 Workshop 尚不存在（attempt-001 停在 `pending_independent_review`），故真实 smoke admission 需先完成 Workshop 独立评审；本票用全 fixture 合成数据完成全部机检。

**决策记录：**
- 交互批准语义定为 `sys.stdin.isatty()` + 恰好 `yes`：非交互 stdin 一律 fail closed，`--non-interactive` flag 从 CLI 面移除（024 只允许七参数；非交互由环境自然触发而非参数选择）。
- 定价与批准边界遵循 022/024：金额用 `Decimal` 向上取整到分，上界恒按高峰全 miss ≤2 attempts，attempt 按开始时刻所在时段结算；价格表为一次登记 + hash pin 的 tracked 文件，非每次运行快照。
- `legacy` 子命令保留而非删除：delivery 契约的 expand-contract 纪律要求基线可运行至 13 票；不在本票扩大删除面。
- step 1/2 的证据语义：schema 失败是 run 外 entry error（无 run）；step 2 由 write-once `request.json`（含 command evidence）记录，steps 3–8 各一事件、step 9 由 `admitted` 终结事件记录，事件总数 8。

**相关 Commit：**
- `cbaa99b add: implement Ideation Run admission without paid work`
- `631e209 fix: harden admission preflight per two-axis review`
- `6783c46 fix: close re-review findings in admission boundary`
- `7099e35 docs: record run admission evidence and close-out`
---

**[20:27] 进展记录**

**工作内容：**
- Robert 指示能自验的自行验收；按 01/03 票授权先例执行 ticket 04 逐项验收复核。
- 验收自查发现两处机检缺口：(1) `VM-UNIT-02` 负向条款（duplicate keys、NaN/Infinity、float、非 NFC、unpaired surrogate、非法 UTF-8）无直接测试；(2) `VM-CONTRACT-023-01` 路径校验存在实现缺陷——`./x`、`x/` 因 pathlib `parts` 丢弃前导 `./` 与尾部 `/` 而绕过归一化检查（用独立探针脚本复现）。
- 修复 `workspace_relative_path` 在解析前拒绝非归一化形状（前导 `./`、尾部 `/`、外侧空白）；新增 5 项测试补齐两个 VM 行的负向集；stash 修复前后实证 RED→GREEN。
- 补记 evidence 文档「验收自查与补缺」节，ticket 04 勾选最后一项、写 Resolution、置 `closed`/`accepted`；全量回归后提交。

**结果：**
- ✅ 补缺后全量 `333 passed in 43.84s`；Black 触及范围合规；compileall exit 0。
- ✅ Ticket [04: Admit an Ideation Run without paid work](docs/wayfinder/ideation-implementation/tickets/04-admit-an-ideation-run-without-paid-work.md) 六项验收全部满足并关闭；下一张 [05: Return one audited BM25 Retrieval Result](docs/wayfinder/ideation-implementation/tickets/05-return-one-audited-bm25-retrieval-result.md) 的唯一 blocker 已解除，未认领。

**问题与状态：**
- ✅ 无遗留实现或验证问题。真实 case 的 Approved Workshop 仍待独立语义评审（attempt-001 停在 `pending_independent_review`），这是 02 票既有事项，不阻塞 05 票的合成数据机检路径。

**决策记录：**
- 按 Robert 01/03 票的「Codex 复核无误后代为逐票验收」授权先例关闭本票；`./x` 归一化绕过定性为实现缺陷（非测试问题），修复走 RED→GREEN 而非放宽测试。

**相关 Commit：**
- `7d4884e fix: reject denormalized relative paths; prove canonical negatives`
- `9357771 add: close Ideation Run admission ticket`
