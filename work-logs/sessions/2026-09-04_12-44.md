# Session 记录 - 2026-09-04 12:44

**[12:44] 实现序列完成后的 smoke readiness 审计**

**工作内容：**
- 按 YAML frontmatter、`delivery-and-handoff.md` 与最近 session logs 复核交付前沿：`docs/wayfinder/ideation-implementation/` 的 13 张实现票全部已关闭，下一运行阶段按已批准契约必须从 1-case smoke 开始，不能直接执行 035 的 24-run 参数比较。
- 核对当前代码：DeepSeek adapter 只实现 `StubTransport` / `RecordedTransport` 与受测注入 seam，bare `new-run` CLI 仅完成 Admission；真实 HTTP transport 与 `new-run` / `resume` 生产接线尚未落地，因此当前不具备发起真实 smoke 的可执行入口。
- 用 DeepSeek 官方实时文档复核 `deepseek-v4-pro` / Chat Completions / thinking / `reasoning_effort` / `max_tokens` 与人民币峰谷价格；当前官方人民币价格与仓库 versioned price table 一致。
- 做无凭据网络可达性检查：`https://api.deepseek.com` 返回 HTTP 401，证明 endpoint 可达且未发起付费请求；当前进程未配置 `DEEPSEEK_API_KEY`（仅检查存在性，未读取或记录值）。

**结果：**
- ✅ 13/13 implementation tickets 状态已确认。
- ✅ 真实 smoke candidate 已有 approved Workshop 与 approved Corpus，两者当前 hash 与 manifest pin 一致。
- ⏳ 需先经 Robert 批准一个独立的 live-smoke readiness 方案：先无付费落地真实 transport 与安全接线，再对单一 approved case 做逐 run 费用批准后的 smoke。

**问题与状态：**
- ⚠️ 当前 bare CLI 的帮助文本和行为仍定义为「Admission only」；这是从 implementation readiness 到 paid smoke 之间的真实执行缺口，不能用临时隐藏环境开关绕过。
- ⏳ `DEEPSEEK_API_KEY` 与账户余额是真实 smoke 的外部前置；在 transport 实现和零网络测试完成前不需要提供。

**决策记录：**
- 当前只形成待批准建议，未锁定 API/transport 方案，未 claim 035/036，未修改代码，未发起真实模型请求。

**相关 Commit：**
- 无（本次为只读审计与待批准方案收敛）

---

**[12:56] Live smoke 执行规格与两张 tracer-bullet tickets 发布**

**工作内容：**
- Robert 批准 live-smoke 方向并明确要求先使用 `to-spec`，再使用 `to-tickets`，当前不实现代码。
- 新增 `docs/agents/live-smoke-execution-spec.md`：完整定义从离线 adapter/controller readiness 到真实 DeepSeek direct 执行的问题、用户故事、实施决策、测试 seam、非范围、费用批准与失败处理边界。
- 新增 `docs/wayfinder/ideation-execution/`，按已批准粒度发布两张线性垂直切片票：
  1. `Enable auditable live DeepSeek execution`：zero-network 落地生产 transport、`new-run`/`resume` 接线、dependency/import 契约、clean-env 与双轴 review；
  2. `Qualify one auditable live smoke run`：被第 1 票阻塞，在单独的 7.08 CNY 上界批准后执行单 case smoke，完成 Evidence Chain validation、sanitized export 与 Robert-authored Evaluation Artifact 覆盖。
- 测试 seam 沿用现有 `Transport.send` 与 `new-run`/`resume` CLI-controller 两个最高层边界，不新增第四个注入面。

**结果：**
- ✅ Spec 按 `to-spec` 七节模板完整发布，含 36 条 user stories 与 decision-complete 边界。
- ✅ 两票拆分为可独立验证的垂直切片；第 1 票 `status: open` / `assignee: null` / 无 blocker，是唯一 frontier；第 2 票精确 blocked by 第 1 票。
- ✅ 新文档的内部相对链接、frontmatter、模板节、占位符/凭据形状扫描与 whitespace check 全部通过。

**问题与状态：**
- ✅ 无文档或 blocking-edge 遗留问题。
- ✅ 验证 helper 首次使用当前系统 Ruby 不支持的 `Hash#filter_map` 而失败；改为兼容的 `each` 累加后复跑通过，确认唯一 frontier 为第 1 票。
- ⏸️ 按 Robert 要求停在 spec + tickets：未 claim 第 1 票，未修改 runtime，未运行模型，未产生费用。

**决策记录：**
- 把两阶段放入独立 `ideation-execution` effort，不追加到已关闭的 13 张 implementation tickets；这保留「13/13 实现序列完成」的事实，也让付费 smoke 有独立 blocker 与授权边界。
- 生产 transport 选择 exact-pinned `httpx==0.28.1` direct HTTP，不使用 OpenAI SDK、`curl` 或临时 injector；理由是保持 exact request/raw response 边界、显式零重试和可审计的唯一 CLI 路径。

**相关 Commit：**
- `ec103db` docs: close ticket 01 and record live smoke execution spec and progress

---

**[13:17] Ticket 01: Enable auditable live DeepSeek execution 实施与全量验证**

**工作内容：**
- 推进执行票 01 (`docs/wayfinder/ideation-execution/tickets/01-enable-auditable-live-deepseek-execution.md`)，按规格 `docs/agents/live-smoke-execution-spec.md` 落地零网络真实 HTTP transport 与生产接线。
- 依赖契约更新：将 `httpx==0.28.1` 从 `requirements-dev.txt` 提升至 `requirements.txt` 作为运行时唯一的第三方依赖；同步更新 `tests/test_dependency_contract.py` 与 `tests/test_ideation_import_contract.py`，保持标准库+httpx导入闭包。
- 生产 Transport 实现：在 `ai_scientist/ideation/deepseek.py` 中新增 `HttpTransport`（实现 `Transport.send` 契约）：
  - 固定向 `https://api.deepseek.com/chat/completions` 发送 canonical JSON bytes；
  - 凭据仅由本地 `DEEPSEEK_API_KEY` 环境变量读取并仅用于 `Authorization: Bearer <key>`，`redact_secrets` 动态增强对当前凭据的脱敏；
  - 强制安全连接：`verify=True`、`follow_redirects=False`、`trust_env=False`；
  - 超时与绝对 deadline：`connect=10.0`s，流式读取 `iter_bytes` 中用单调时钟强制 3600.0s 绝对 wall-clock deadline，不可被 keep-alive bytes 绕过；
  - 传输层零重试，异常映射为 `TimeoutError` / `ConnectionError`，由 `DeepSeekAdapter` 统一收敛为 typed failures；
  - `DeepSeekAdapter.__init__` 默认启用 `HttpTransport()`。
- CLI 生产接线：修改 `ai_scientist/perform_ideation_temp_free.py` 中的 `_run_new_run`，当 `execute is None` 时默认为 `execute = True`；保持 7 参数 allowlist。
- 零网络测试套件：新增 `tests/test_deepseek_http_transport.py`（共 12 个测试，全部基于 `httpx.MockTransport`），覆盖请求结构、凭据隔离、缺失报错、客户端安全参数、redirect 拦截、流式 deadline 阻断、证据链保留、状态失败与重试分类、网络超时分类，以及 `new-run` / `resume` 完整端到端模拟链路。

**结果：**
- ✅ `tests/test_deepseek_http_transport.py`：12/12 passed (0.81s)。
- ✅ 依赖与导入契约测试：`test_dependency_contract.py` + `test_ideation_import_contract.py` 全绿。
- ✅ Preflight 与 CLI 基线测试：`test_run_preflight.py` (14/14 passed) + `test_ideation_cli_baseline.py` (1/1 passed)。
- ✅ 全量测试套件：`python -m pytest -q` 全部 640 个测试通过（77.99s），零失败。
- ✅ 代码格式与编译：`compileall` 通过；`black --check` 在所有修改文件和 ideation 包上全部格式化合规。
- ✅ Standards / Spec 双轴 review 完成，无阻断项，全过程零真实 API 调用、零模型代币消耗。

**问题与状态：**
- ✅ `DeepSeekAdapter.execute_round` 的 Audit Release Gate 要求必须有 `store` 与 `run_id`，已在单元测试中标准调用 `store.create_run()` 满足。
- ✅ `sys.stdin.isatty` 在第二次 monkeypatch 时需显式保留，已在 resume 测试中修复。
- ⏳ Ticket 01 准备就绪，待 Robert 完成验收；下一阶段为 Ticket 02（需明确费用授权后执行单 case 真实 smoke）。

**决策记录：**
- 维持完全标准化的 `httpx.MockTransport` 测试注入点，既不发起真实外部网络请求，又完整验证 `httpx.Client` 到适配器与控制器的所有边界条件。

**相关 Commit：**
- `3bc787f` feat: implement production HttpTransport and wire ideation execution
- `869a5da` test: prove HttpTransport isolation, security controls, and zero-network execution

---

**[13:20] Ticket 01: 第一性原理对抗性审查、自主验收与关闭**

**工作内容：**
- 依据 Robert 授权（「你自行验收，并从第一性原理出发做一次全面的对抗性审查，确认没问题后，你就可以close并提交推送」），对 Ticket 01 开展全面对抗性审查。
- 审查覆盖 9 个第一性原理维度：
  1. 凭据隔离与运行时脱敏：`DEEPSEEK_API_KEY` 严格仅注入 HTTP header，不写入请求体、日志或证据链，所有报错与消息经由强化后的 `redact_secrets` 过滤；
  2. 超时防死锁与 Wall-clock Deadline：10.0s 连接超时，流式读取单调时钟强制 3600.0s 绝对上限，彻底封死 keep-alive 慢速滴灌挂起风险；
  3. 网络隔离：`follow_redirects=False` 阻断跳转劫持，`trust_env=False` 隔离环境变量污染或代理投毒；
  4. 原始响应保真度：流式分块无损拼接，完整保留原始字节流；
  5. 计费收敛与重试分类：客户端自身零重试，非确定性网络断开与超时映射为不可重试的 typed ambiguous failures，严格防止重试超额计费；
  6. CLI 契约完整性：保持 7 参数 allowlist 与 1 参数 resume，Preflight 9 步与交互式费用批准作为不可绕过的硬前置门禁；
  7. 零网络与测试隔离性：12 个单元与端到端集成测试均基于 `httpx.MockTransport`，100% 隔离外部网络；
  8. 依赖与导入纯度：标准库闭包仅扩充 `httpx==0.28.1`；
  9. 全量自动化套件与代码规范：全量 640 个 pytest 测试全部通过，compileall 与 black 格式化全绿。
- 关闭 Ticket 01：更新 `docs/wayfinder/ideation-execution/tickets/01-enable-auditable-live-deepseek-execution.md` 状态为 `closed`，更新 `docs/wayfinder/ideation-execution/map.md` 决议记录，解除 Ticket 02 的前置依赖。

**结果：**
- ✅ Ticket 01 正式验收关闭。
- ✅ 对抗性审查全部 9 项指标无任何漏洞或隐患。
- ⏳ Ticket 02 成为前沿开放任务（待真实费用授权后启动 1-case live smoke）。

**相关 Commit：**
- `ec103db` docs: close ticket 01 and record live smoke execution spec and progress

---

**[13:53] Ticket 02: 真实低谷期单 Case Live Smoke 运行、全链路验证、定性评估与交互体验第一性原理优化**

**工作内容：**
- **低谷期交互式 Live Smoke 运行**：
  - 严格依据 `docs/agents/live-smoke-execution-spec.md` 与 Ticket 02 要求，在官方低谷时段对唯一经批准的私有 Case (`case-229e495f82a24cff9e6082aa058955b9`) 发起端到端真实运行 (`run_id: f7bddd3e-cd3c-4f66-9b6d-7d30c25c1db0`)；
  - 启动前 Preflight 完整核验 9 项门禁（Git clean、commit 固定、依赖锁定、API 存活、时段识别等），显示 7.08 CNY 保守峰值上界并由 Robert 在终端交互输入 `yes` 确认；
  - 运行模型 `deepseek-v4-pro`（`reasoning_effort=high`），经历了 3 轮推理、2 次文献检索，成功生成并接受定稿科研创意（引用 3 篇文献），由控制器封印为 Terminal Outcome `success`。
- **全链路严苛验证与脱敏发布导出**：
  - 运行 `perform_ideation_temp_free.py validate`，静态重算 23 个事件的哈希链与 13 个产物，状态为 `valid`；
  - 运行 `perform_ideation_temp_free.py export`，经过 10 项脱敏安全门禁扫描，原子导出到 `evidence/ideation-runs/f7bddd3e-cd3c-4f66-9b6d-7d30c25c1db0/`（`manifest.json` 与 `events.json`）。
- **Post-Seal 人工定性评估**：
  - 运行 `evaluation assemble` 生成阅读简报与草稿；
  - 由 Robert 依据 `idea-quality-rubric-v1.0.0` 七项标准完成定性研判；
  - 运行 `evaluation validate` 校验通过并提交不可篡改的定稿 `v0001.json`；
  - 运行 `evaluation list-coverage`，确认该 Run 达到 100% 覆盖率（`covered: 1, draft_only: 0, missing: 0`）。
- **第一性原理交互体验优化与 Emoji 进度反馈**：
  - 针对长耗时深度推理（单轮 >140s、5,000+ reasoning tokens）导致的终端静默问题，从第一性原理分析，确定在不破坏 `sys.stdout` 纯净 JSON 格式的前提下，将进度回显定向至 `sys.stderr`；
  - 仅在 TTY 环境下激活状态反馈，并加入适量直观 Emoji（`✓`, `🚀`, `🧠`, `✨`, `🔍`, `💡`, `🔒`）；
  - 修复 `IdeationController` 初始化与方法边界，新增单元测试 `test_cli_seam_progress_stream_emits_expected_markers`；
  - 执行回归测试，全量 641 个测试 100% 通过（81.34s）。
- **归档与结项**：
  - 撰写脱敏科研证据报告 `docs/research/live-smoke-execution-evidence.md`；
  - 关闭 Ticket 02，更新 Wayfinder 架构图 `docs/wayfinder/ideation-execution/map.md`。

**结果：**
- ✅ 真实扣费实测仅 **0.14 CNY**，相比 7.08 CNY 保守上限节约 **98.0%**；
- ✅ Evidence Chain（23 事件、13 产物）密码学哈希闭合，静态验证 `valid`；
- ✅ Post-seal 定性评估完成，覆盖率 100%；
- ✅ 终端体验优化完成并通过全量回归测试：全库 641 passed，black 与 compileall 全绿；
- ✅ Ticket 02 验收达成并正式关闭。

**问题与状态：**
- ✅ 终端长时间静默问题已通过正交 `sys.stderr` 进度报告机制彻底解决。
- ✅ 初始 draft 时间戳精度不匹配问题（需要微秒级别）已通过标准 UTC 规范解决。

**决策记录：**
- 进度报告严格绑定 `sys.stderr`，保持 `sys.stdout` 的管道机读无损性与幂等性；
- 遵循最小作用域原则，本票仅完成 1-case smoke 审计闭环，不越界进入 Canary 矩阵或下游流程。

**相关 Commit：**
- `b33c046` feat: add stderr interactive progress reporting with emojis for ideation loop

---

**[14:01] Ticket 02 关闭后的 Canary frontier 与 Plan Gate 审计**

**工作内容：**
- 核验 `ideation-execution` 两票状态、HEAD、远端同步状态、工作树与 live-smoke 证据：两票均已关闭，HEAD/远端均为 `4a132ee`，审计前工作树 clean；sealed run、sanitized export 与 Evaluation Artifact 证据均存在。
- 按 YAML frontmatter 重算 Wayfinder frontier：当前只有 035（DeepSeek reasoning effort/completion limit 比较）与 036（Workshop specificity/rendering 比较）可做；`canary-and-scale-gates.md` 明确规定先执行 035，036 复用 035 胜方并排在预算队尾。
- 复核现有 private 12-case `lr-op-*` operational batch 与 Canary v1.0 选集规则，确认该批次不能直接复用：其 cluster 分布为 Health 4、Genetics 2、Neuroscience 1、其余五类各 1，最小 reference count 为 4，既不满足 Canary 对 Health/Genetics/Neuroscience 各 2 的规则，也不含规定的 3-reference edge case。
- 发现 Canary v1.0 自身存在算术矛盾：8 clusters 全覆盖，三个重点 cluster 各 2、其余五个各 1，总数只能是 11，而契约要求 12；未获 Robert 批准前不能擅自分配第 12 个 case。

**结果：**
- ✅ 下一执行顺序已确认：035 → 036。
- ⏳ 035 尚未 claim；必须先由 Robert 批准 Canary v1.1 对第 12 个 case 的分配规则和 035 Plan Gate，之后才能准备 inputs 或发起付费 run。
- ✅ 本轮未修改 runtime、dataset、ticket 状态，未调用 provider，未产生模型费用，未进入 downstream。

**问题与状态：**
- 🆘 需要 Robert 决策：建议把第 12 个 case 定义为「constraint-driven edge slot」，用于满足不可恢复 abstract、3-reference、min/median/max 等尚未被前 11 个覆盖的硬约束；若多个候选等价，以 canonical hash tie-break。该修订需要 `canary-and-scale-gates.md` 升至 v1.1。

**决策记录：**
- 不复用为 local-ranking 目的批准的 `lr-op-*` batch：它的抽样目标与 ideation Canary 契约不同，复用会把数据方便性置于实验可解释性之前。
- 035 的 24 个样本单位应解释为 24 个 Ideation Runs（12 paired cases × high/max），每 run 有 3 个 logical model rounds，最多因受控 retry 形成 6 个 physical attempts；正式 Plan Gate 必须按该真实调用拓扑估价，而不能把「24 次」误读为仅 24 个 HTTP requests。

**相关 Commit：**
- `a39ffbc` docs: publish ticket 035 canary specification and claim ticket

---

**[14:21] Ticket 035 Plan Gate 规格发布与付费前跨会话 handoff**

**工作内容：**
- Robert 批准 Canary v1.1 constraint-driven 第 12 slot 与 ticket 035 Plan Gate，并指定下一会话只完成付费 run 前的准备，完成后回到当前会话启动付费阶段。
- 使用 `to-spec` 新增 `docs/agents/deepseek-reasoning-effort-canary-spec.md`，以 `ready-for-agent` 发布完整规格；将规格链接挂入 035 ticket，但保持 ticket `status: open` / `assignee: null`。
- 规格锁定 fresh 12-case 选集、24-run paired high/max 单变量比较、balanced execution/blind mapping、Robert-only write-once pair verdict、Promotion/Regression thresholds、30 CNY hard cap、32768 truncation 观测和 fail-closed/inconclusive 语义。
- 使用 `handoff` 在 macOS 临时目录生成 `HANDOFF.md`，只引用仓库权威资料，明确下一会话的交付清单、禁止事项、返回包与建议 skills。

**结果：**
- ✅ Spec 含 `to-spec` 要求的七个 section 和 46 条 user stories，frontmatter `label: ready-for-agent` 可解析，ticket 相对链接存在。
- ✅ Handoff 明确硬停止点：不得执行 `new-run` / `resume`，不得发送 live DeepSeek API request，实际 spend 必须保持 `0 CNY`；完成 Canary v1.1、001 Promotion Proposal、exact 12-case approvals、24-run credential-free plan 与零网络验证后回到当前会话。
- ✅ 两个新文档通过 whitespace、占位符/凭据形状、必需标题与非空文件检查；未运行模型、未产生费用、未进入 downstream。

**问题与状态：**
- ⏳ 当前 spec、035 ticket 链接与本 session log 为未提交变更；handoff 要求下一会话先审查并独立提交该文档 checkpoint，再开始 private preparation。
- ⏳ Ticket 035 尚未 claim；按 tracker 规则由下一会话在准备工作前设置 assignee，整个付费前阶段结束时仍保持 open。

**决策记录：**
- 跨会话边界收紧为 zero-provider-call preparation，而不只是「不产生费用」：这样 authenticated 探测也不会意外形成未纳入 24-run matrix 的模型证据。
- 付费阶段留在当前会话：届时从另一会话的 clean commit 和 hash-pinned return packet 开始，重新核验官方价格、credential readiness、剩余 Canary 预算与逐 run 交互批准。

**相关 Commit：**
- `a39ffbc` docs: publish ticket 035 canary specification and claim ticket

---

**[15:07] 项目级 DeepSeek 凭据配置入口实施与离线验证**

**工作内容：**
- 按 Robert 批准的项目配置文件方案，新增 `.env.example`，复用 `.gitignore` 已有的项目根目录 `.env` 忽略规则；真实密钥仍不进入源码、命令参数、日志、chat 或 Git。
- 使用 `wizard` 模板新增 `scripts/setup-project-env`：从 DeepSeek 官方 API key 页面引导一次性隐藏输入，只写 `DEEPSEEK_API_KEY`，写入前拒绝 symlink、非普通文件、非当前用户所有文件、未知键和重复键，写入后固定 `600` 权限并做零网络环境注入自检。
- 新增标准库入口 `scripts/with-project-env`：从脚本位置确定 repo-root `.env`，以 `O_NOFOLLOW`（平台支持时）、inode 比对、owner、exact `600` mode、regular-file、16 KiB 上限和严格单键语法 fail closed；通过 `os.execvpe` 仅向 child environment 注入 key，不把 key 放入 argv 或输出。
- 在 README 的 API key 章节记录一次配置和后续命令前缀；新增 `tests/test_project_env_loader.py`，覆盖 child-env 注入、父环境覆盖、无命令/缺文件、权限、空值、重复项、未知项、`export`、引号、空白、CRLF、symlink 和仓库忽略/可执行权限契约。

**结果：**
- ✅ 项目凭据定向测试 15/15 通过；相关 dependency/import/preflight/HTTP transport 测试 43/43 通过。
- ✅ 全量测试 `python -m pytest -q`：662 passed（83.56s）。
- ✅ 修改范围的 Black、`compileall`、`bash -n`、`git diff --check`、credential-shape scan 与 wizard library exact-prefix 比对均通过；`shellcheck` 当前环境不可用。
- ✅ 未运行 setup wizard，项目 `.env` 仍不存在；未读取或记录真实 credential，未发起 provider 请求，实际费用 0.00 CNY。

**问题与状态：**
- ✅ 首次参数化拒绝测试对空字符串执行“不得出现在 stderr”断言，因空字符串必然属于任何字符串而失败；收紧为仅对非空输入执行泄漏断言后通过。
- ✅ 一次内联 Bash 引号语义探针因 zsh 命令引用错误未启动；随后把向导的 quote 检查改为明确比较首尾字符，`bash -n` 通过。
- ⚠️ `rtk pytest -q` 未带显式路径时错误报告 `No tests collected`，已回退原始 `python -m pytest -q` 并得到 662 passed。
- ⚠️ 仓库全量 `black --check ai_scientist` 仍报告 14 个 retained/downstream 历史文件需格式化；本次未进入或修改 downstream，定向 ideation/本次文件 Black 检查通过。

**决策记录：**
- 保持 ideation runtime 的现有环境变量契约，不在业务代码内隐式加载 `.env`；项目配置通过显式、可审计的 wrapper 注入。
- 接受本机项目 `.env` 为明文存储的已知权衡，但要求 owner-only `600`、拒绝 symlink/额外键，并保留 macOS controller-only 的凭据拓扑。

**相关 Commit：**
- `b1b42f2` feat: add project-local DeepSeek credential launcher

---

**[17:56] 跨领域 ideation prompt 目标错配审计**

**工作内容：**
- 在 ticket 035 首次付费运行前，对当前唯一 model-visible ideation prompt、已批准的 baseline/prompt 围栏、IdeaBench 本地数据领域分布、任务原始要求、测试覆盖和既有 live smoke 输出做只读第一性原理审计。
- 确认 `ai_scientist/ideation/controller.py` 的系统提示词仍完整保留上游 AI Scientist-v2 的 ML 专用目标：`AI researcher`、`top ML conferences`、`conference format`、`precise algorithmic changes`、固定 `evaluation metrics` 与 `academic lab` 资源模型；这些不是独立措辞，而是共同把方法和成果形态推向计算机科学/机器学习。
- 数据侧 `ideabench_clustering.json` 含 237 个唯一 target、覆盖 8 个跨领域 cluster；Health & Medicine 与 Genetics & Molecular Biology 各 76 个，另含 Environmental Sciences、Materials Science、Neuroscience & Cognitive Sciences、Public Health & Policy、Social & Behavioral Sciences、Technology & Engineering。原始任务只要求基于该数据集生成科研 idea，没有要求输出必须投 ML venue。
- 审查既有 live smoke：其社会/行为科学问题最终方案包含 RoBERTa fine-tuning、macro-F1 与 domain transfer。该单例与 prompt 偏置方向一致，但不足以单独证明因果；显式 prompt 文本本身已构成直接目标错配证据。

**结果：**
- ⚠️ 确认当前 prompt 存在跨领域目标错配；若不处理，输出可能把湿实验、临床、观察性、定性、政策、材料或理论问题不必要地改写为 ML 算法/benchmark 题目。
- ⚠️ 当前测试仅断言工具名、grounding 与旧工具移除，没有领域中立性回归检查。
- ✅ post-seal rubric 的七个判据本身基本领域中立；主要问题集中在生成系统提示词与 `Experiments` 字段的 ML 化描述，不在 retriever、grounding 或 Evidence Chain。
- ✅ 本轮零模型调用、零 provider 网络、零费用，未进入 downstream；ticket 035 尚未产生任何付费 run。

**问题与状态：**
- ⏳ 建议立即暂停 ticket 035 付费矩阵。`high`/`max` 两臂虽共享同一 prompt、内部因果比较仍成立，但它们比较的是偏离任务目标的生成行为，无法支撑跨领域 ideation 的参数选择。
- ⏳ 当前代码忠实实现了此前“保留兼容 baseline”的批准合同，因此不能作为普通代码 bug 直接改；需要 Robert 批准新的跨领域 prompt 设计与验证路径，并更新原有 prompt diff 围栏。

**决策记录：**
- 推荐单一领域中立系统提示词，而不是 8 套 cluster-specific prompt：由 Workshop 与检索证据提供领域语境，避免把 private cluster metadata 暴露给模型，也避免新增分类分支和错误路由。
- 推荐保留七字段 payload 兼容性，但把 `Experiments` 的说明扩展为 field-appropriate validation plan，可包含实验、观察研究、定性方法、仿真、形式分析或证明；删除固定 ML venue、conference、algorithmic-change 与 metric 要求，并把 feasibility 改为领域相称的资源、数据、设备、伦理和时间约束。
- 最小公平验证建议以当前冻结 Canary 中四种方法学家族各一例做 current-vs-domain-neutral 的 paired blind comparison；晋升后再在新 Design Epoch 重启 `high` vs `max`，不把旧 prompt 下的付费证据混入新 epoch。

**相关 Commit：**
- 待提交
