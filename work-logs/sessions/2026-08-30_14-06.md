# Session 记录 - 2026-08-30 14:06

**[14:06] 进展记录**

**工作内容：**
- 解决 wayfinder ticket [Define the optimization promotion gate]（029）。先向 Robert 说明 ticket 意图（问题、blocked_by 023/026/027 全闭、frontier 状态、与 028 的兄弟边界），确认开工后认领（assignee → Robert），加载 grilling + domain-modeling。
- 事实自备：读 tracker 文档、012 Resolution、028 Question、CONTEXT.md；grep validation-matrix.md 确认 027 给 029 留的接口（「优化类修订必须携带证据并过 promotion gate(029)」、行 ID 消费约定）；ls `docs/agents/` 确认契约文档先例。
- 第一轮 grilling 五题（适用范围路由规则、载体 = 契约文档 + promotions/ 目录、双 gate 审批形态、对比方法 = pinned baseline 单主变量对照、强制 pre-register 判据）全部按推荐批准；第二轮四题（分维度 Regression Budget + 裁量前置 Plan Gate、Design Epoch 证据可比性、生命周期状态机与提案人、回滚与紧急 halt）全部按推荐批准。命名随第二轮确认：Optimization Promotion / Plan Gate / Promotion Gate / Promotion Proposal。
- 落盘：新建 `docs/agents/promotion-gate.md`（v1.0，八节契约 + Promotion Proposal 模板）；029 写 Resolution 并关闭；map 的 Decisions so far 追加索引行（gist 点名 Design Epoch 规则供 028 引用）；CONTEXT.md 新增六术语（Optimization Promotion、Promotion Proposal、Plan Gate、Promotion Gate、Regression Budget、Design Epoch，插在 Evidence Feedback Loop 之后）。

**结果：**
- ticket 029 关闭。`python -m pytest -q` 26 passed；`git diff --check` 通过。未修改 runtime code、未调用模型、未产生费用。
- 并发注意：CONTEXT.md 编辑时撞上 028 session 的磁盘写入（Canary 术语，未提交），重读后合并，无冲突；030 的 blocked_by 中 029 一项解除，030 仍待 028。

**决策记录：**
- 路由规则「更好→promotion / 错了→bugfix / 还没定→wayfinder」：bugfix 豁免防止 gate 沦为所有 commit 的橡皮图章，wayfinder 豁免保持规划与运行期操作分离。
- 双 gate 且裁量权全部前置 Plan Gate：允许 Promotion Gate 临时放宽等于允许看到结果后移动球门，pre-register 纪律（012 可证伪性）会被事后合理化掏空。
- Design Epoch 不新增 runtime 字段：由既有 spec pins（023/027/034）天然实现，仅是证据可比性规则——避免为概念加存储。
- 预算外维度意外回退也自动 reject：防止只盯 pre-registered 维度而漏掉真实回退。

**相关 Commit：**
- 无（待提交，Robert 已批准完成后提交）。
