# Session 记录 - 2026-08-29 10:15

**[10:15] 进展记录**

**工作内容：**
- 按 Robert 的明确选择启动 Wayfinder grilling ticket `Define the Workshop File contract`，将 ticket assignee 从 `null` 设为 `Robert`。
- 复核 Target Paper、Workshop File、Target Reference Corpus 的领域边界，以及 target-to-workshop、metadata、routing 三项前置研究；只读核对本地数据 schema、现有 Workshop Markdown、ideation 入口的文件读取与输出行为。
- 将第一轮 design tree 收敛为 Workshop 的 topic specificity、derivation source boundary、derivation authority 和 model-visible/private artifact boundary 四个根决策。

**结果：**
- 确认 237 个 targets 均有 title 与 raw abstract；`abstract_summary` 和 target-authored reference `contexts` 是答案泄漏面，不可作为 Workshop seed。
- 当前入口会把 Workshop Markdown 全文原样读入并发送给模型，不解析 schema；同名 `.json` 用作 idea payload，因此 provenance 必须与 model-visible Markdown 分离。
- 确认纯确定性规则可以验证 schema、标识符、hash、路径和精确/近似文本重叠，但不能证明语义改写没有泄漏，也不能消除模型对 2024 Target Paper 的参数化记忆。
- ticket 已认领；未修改任务代码、未生成 Workshop、未调用模型、未进入 downstream；`git diff --check` 通过。

**问题与状态：**
- ⏳ 进行中：等待 Robert 回答第一轮根决策，之后才能展开 schema、命名、provenance 和 leakage gate 的后续分支。

**相关 Commit：**
- 无（当前工作区基线最新提交：`9703d66 docs: log DeepSeek provider decision`）

---

**[10:25] 进展记录**

**工作内容：**
- Robert 要求在锁定 Workshop topic specificity 前深入研究原始 AI Scientist-v2 的真实 Workshop input，并从第一性原理重新判断输入合同；已启动 primary-source research，核对官方仓库当前 commit、文件历史、Nature 论文和公开的 ICLR 2025 workshop experiment。
- Robert 同意先选少量跨领域 Target Papers 做 canary，不在缺少效果证据时直接生成全部 237 个 Workshops。

**结果：**
- 官方 AI Scientist-v2 当前 `main`（`96bd51617cfdbb494a9fc283af00fe090edfae48`）只有一个 Workshop Markdown 样例；四段 Markdown 被全文直接放入 ideation prompt，没有 schema parser。
- 官方论文将真实 ICBINB 运行描述为使用 workshop 的 broad theme；公开 experiment 仓库只包含三篇结果和 review artifacts，没有发现第二份 Workshop input。
- 深入研究报告仍在编写；Q1 暂未决，不提前更新 Workshop File 的领域定义。

**问题与状态：**
- ⏳ 进行中：需要完成官方历史、Supplementary prompts 和第一性原则分析，再向 Robert 给出 Q1 证据判断。
- ⚠️ 第一次临时检出命令因包含自动删除临时目录而在执行前被安全策略拒绝；改为保留 `/tmp` 临时目录后成功完成只读检出，仓库未受影响。

**决策记录：**
- Derivation source boundary：默认只使用 Target Paper 的 `title + raw abstract` 作为私有转换输入；reference abstracts 留给 ideation retrieval。
- Rollout：先对少量跨领域 targets 做 canary，根据实际 Workshop 效果决定是否扩到全量。
- Derivation authority：先锁机制无关的语义合同，再通过 prototype 公平比较候选机制。
- Artifact boundary：model-visible Workshop `.md` 与 private provenance manifest 分离。

**相关 Commit：**
- 无

---

**[10:32] 进展记录**

**工作内容：**
- 完成原始 AI Scientist-v2 Workshop input 的 primary-source 深入研究，产出中文报告 `docs/research/original-workshop-file-contract.md`，并将其链接到开放的 `Define the Workshop File contract` ticket。
- 根据 Robert 已批准的 model-visible/private boundary，立即收紧 `CONTEXT.md` 中 `Workshop File` 的定义，并新增 `Workshop Manifest`；尚未批准的 `problem envelope` 未写入领域词汇。

**结果：**
- 官方公开历史只有一个 331-word、四段自由 Markdown 样例；runtime 不解析 section，全文直接进入每个 proposal 的首轮 prompt，并通过 message history 影响后续 reflection。
- Nature version of record 与官方页面把 Workshop 的作用定义为 broad theme / research subfield；公开 Workshop Experiment 仓库未提供真实 ideation input，不能证明当前样例与 2025 内部 prompt 字节级相同。
- 第一性原理判断：Workshop 必须作为搜索空间边界，兼顾 target-level 区分性与 held-out answer 隔离；推荐把 Q1 收紧为 `B-guarded`，并用 `A-control` 做少量跨领域 canary 对照，拒绝 target-specific method/result 路线 C。
- 报告内 20 个唯一引用 URL 全部返回 HTTP 200；Markdown whitespace 与 `git diff --check` 通过。未调用模型，未进入 downstream。

**问题与状态：**
- ⏳ 进行中：等待 Robert 是否批准 `B-guarded` 作为待 canary 验证的 Workshop specificity hypothesis；批准后再展开 schema、命名与 leakage gate 分支。

**决策记录：**
- `Workshop File` 是 target-derived、model-visible 的 topic description；不得暴露 Target Paper 的 held-out contribution 或 identity。
- `Workshop Manifest` 是 private、non-model-visible 的 provenance record，负责连接 Workshop 与 Target Paper 并保存可审计 derivation evidence。

**相关 Commit：**
- 无

---

**[10:41] 进展记录**

**工作内容：**
- Robert 决定初始 Workshop 采用官方 README 的 `Title`、`Keywords`、`TL;DR`、`Abstract` 四段结构，在少量 targets 上先建立兼容基线；第一性原理提出的 problem-envelope、字段重组或删减作为后续证据驱动的研究点，不在当前 ticket 中提前优化。
- 新增 blocked prototype ticket `Compare Workshop specificity and rendering`，用于在 optimization gate 建立后公平比较 README-compatible baseline、`B-guarded` 和其他 rendering 变体。

**结果：**
- 本地 tracker 现有 36 个 tickets；新 prototype 标题唯一，正确被开放的 `Define the optimization promotion gate` 阻塞，不进入当前 frontier。
- 当前 ticket 继续负责 compatible baseline 的内容安全、schema strictness、filename、Workshop Manifest 和 validation contract；未生成 Workshop、未调用模型、未进入 downstream。
- `git diff --check` 通过。

**问题与状态：**
- ⏳ 进行中：需要 Robert 明确 README-compatible baseline 是只兼容四段 rendering，还是允许 Target Paper 原文进入 model-visible 内容；后者会违反已批准的 held-out-answer 隔离。

**决策记录：**
- 先建立 README-compatible 四段 Workshop baseline，并用少量 canaries 观察效果；只有通过既定 promotion gate 的比较证据，后续 schema 或 specificity 改进才能替换 baseline。

**相关 Commit：**
- 无

---

**[10:45] 进展记录**

**工作内容：**
- Robert 批准 README-compatible baseline 的内容与 schema 边界：Target Paper 的 `title + raw abstract` 只作为私有 derivation source，model-visible Workshop 必须重新表述；Markdown 固定使用 `Title`、`Keywords`、`TL;DR`、`Abstract` 四个 heading、固定顺序、全部非空且不允许额外 section。

**结果：**
- Baseline rendering 与安全语义不再混淆；Workshop `Abstract` 明确不是 Target Paper raw `abstract` 的复制位置。
- 初期不设置从唯一官方样例外推的硬 word-count 配额，只记录各 section 和全文长度供 canary 分析。
- 未修改任务代码、未生成 Workshop、未调用模型、未进入 downstream；`git diff --check` 通过。

**问题与状态：**
- ⏳ 进行中：下一轮需要批准四段各自的允许语义，以及 deterministic 与 human semantic validation 的组合。

**决策记录：**
- 拒绝把 exact Target Paper title、raw abstract、identity、method、experiment、result 或 conclusion 直接写入 model-visible Workshop。
- Baseline schema 使用 strict README-compatible four-section rendering；长度阈值留给 canary evidence，而不是凭单一样例锁定。

**相关 Commit：**
- 无

---

**[10:48] 进展记录**

**工作内容：**
- Robert 批准 README-compatible baseline 的全部字段语义与 layered validation 推荐：`Title`、`Keywords`、`TL;DR`、`Abstract` 分别承载 identity-free 问题标题、既有领域词、开放问题和问题空间叙述；不得包含 target-specific answer。
- 在 `CONTEXT.md` 新增 `Approved Workshop`，区分“文件已生成”和“已获准进入 Ideation Run”。

**结果：**
- Deterministic validation 覆盖 schema、encoding、标识符、exact title、URL、forbidden-source overlap 和可重放文本重叠检查；独立 semantic review 覆盖近义答案泄漏、multiple-answer breadth 与 target-problem relevance。
- 任一 deterministic 或 semantic gate 失败时 Workshop 不得进入 ideation；未调用模型、未生成 Workshop、未进入 downstream。

**问题与状态：**
- ⏳ 进行中：剩余合同需收敛 model-visible language/canonicalization、filename identity、失败/重试行为和 Workshop Manifest 最小字段。

**决策记录：**
- 只有同时通过 deterministic 与独立 semantic validation 的 `Approved Workshop` 才能作为 Ideation Run 输入。

**相关 Commit：**
- 无

---

**[11:10] 进展记录**

**工作内容：**
- Robert 批准 Workshop File 的剩余合同；已将完整 Resolution 写入 `Define the Workshop File contract`，把 ticket 设为 closed，并在 Wayfinder map 的 `Decisions so far` 登记决策。
- 将已具体化的 Workshop specificity/rendering 优化问题从 fog 移除，保留为受 optimization promotion gate 阻塞的 `Compare Workshop specificity and rendering` prototype ticket。

**结果：**
- 初始合同固定为 target `title + raw abstract` 私下派生、英文 canonical 四段 Markdown、opaque `case_id` 文件名、private Workshop Manifest，以及 deterministic validation + independent semantic review 的 fail-closed 准入。
- Retry 必须保留全部 attempts 和失败证据，禁止回退到 raw target 内容、旧文件或其他 target；首轮只做少量跨领域 canaries，不直接扩到 237 个 targets。
- Tracker 校验得到 36 个 tickets、20 个 closed tickets、20 个 map decisions 和 6 个 frontier tickets；title、frontmatter、blocker、closed-decision 对应关系全部通过。
- 未生成 Workshop、未调用模型、未产生费用、未进入 downstream。

**问题与状态：**
- ✅ `Define the Workshop File contract` 已解决；具体 derivation mechanism、validation thresholds、run layout、canary selection 与 rendering optimization 分别留给现有后续 tickets。

**决策记录：**
- 只有规范化、无答案泄漏并同时通过两层验证的 `Approved Workshop` 才能进入 Ideation Run；model-visible topic 与 private identity/provenance 严格分离。

**相关 Commit：**
- 本记录随本轮文档提交；SHA 见完成报告。
