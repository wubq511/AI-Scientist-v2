# Session 记录 - 2026-08-31 07:09

**[07:09] 进展记录**

**工作内容：**
- 将已批准的 v1.2 AI-qrels 设计落地为 fail-closed `ai_judge` harness：冻结 Kimi/DeepSeek actual-model bundles、校正原错误 Codex provenance、验证 exact quote 与完整覆盖，并生成不暴露 A/B labels/rationales 的 judge-C disagreement packets。
- 用户授权的独立 Codex judge-C（`gpt-5.6-sol` / `xhigh`）完成 development 与 sealed holdout 分歧裁决，并生成 consensus。Controller 只核验 development 明文；holdout 仅接收 sealed paths、PASS 与 hashes。
- Judge-C 首次 finalize 因通用完整 qrels parser 错误拒绝 73-item partial adjudication。保留失败 attempt 后，新增独立 `local-ranking-adjudication-qrels-v1.0` schema、frozen-input 逐字段 subset 校验和 canonical trace 重验证；禁止 partial artifact 输入 ranker。
- 新增确定性 `diagnose-agreement`，记录 linear-weighted Cohen's kappa、paper-grade agreement、`1↔2` boundary、两级以上 gap、grade distribution 与 exact support agreement；同步 prototype README、evidence 文档和 ticket current state。

**结果：**
- Development judge-C partial validator PASS（73/73）；consensus validator PASS（176/176）。Consensus qrels SHA-256 为 `beb301eaaa51260fcfc26e34de1de593a453419abeebf4aa7b7f901ebb874869`。
- Development A/B：exact paper-grade 126/176（71.59%）、linear-weighted kappa 0.6561、`1↔2` 7/176（3.98%）、grade gap≥2 为 2/176（1.14%）、both-relevant exact support 0/34。Diagnostics SHA-256 为 `1db6deee2416f084108277f9d822dfc39879a0d4b0821881981f82396c805272`。
- Holdout judge-C partial 与 consensus validator 均 PASS；明文未进 repository，报告只记录 receipts，consensus qrels receipt 为 `f263876d5b3dbebf58b1ebd969d75acae51a35e804cffbb31e87978901a4b890`。
- 修复后的 targeted AI-judge tests 为 10 passed；提交前将重跑 Python 3.13.7/3.14.6 full suite、Black、Ruff、compileall 与 diff check。

**问题与状态：**
- ✅ 336 条人工 qrels blocker 已由已批准的独立 AI judging + consensus + first-party utility design 替换；development qrels 已可用于三套 sensitivity comparison。
- ✅ Partial adjudication 被误当完整 qrels 的 validator bug 已修复，并保留两 split 的原失败 evidence。
- ⚠️ A/B exact support payload agreement 为 0/34，说明 evidence-span 选择不稳定；不能把 synthetic qrels 宣称为 gold。下一步必须满足 A/B/consensus 三套 qrels 的相同 ranker direction，否则结果为 `inconclusive`。
- ⏳ 尚未运行正式 development ranking；必须先提交 clean code/protocol/evidence，再在 Windows Python 3.13.7 CPU FP32 reference environment 运行。

**决策记录：**
- Partial adjudication 继续保留 `qrels.json` 作为 hash-bound evidence，但使用独立 schema；完整 `local-ranking-qrels-v1` 的全覆盖语义不放松。
- 不设置新的 kappa threshold。最接近实际决策风险的 gate 是 ranker promotion/winner direction 是否在 judge-A、judge-B、consensus 下稳定。
- Holdout 在 finalists 与参数冻结前继续 sealed；不因 judge-C 已完成而提前读取。

**相关 Commit：**
- `16fde5b add: establish synthetic qrels consensus`

---

**[07:16] 进展记录**

**工作内容：**
- 在生成正式 Stage A protocols 前复核 hash-bound v1.1 与 schema，发现 output budget 实现仍把 `total_segment_cap` 固定为 6，而获批协议要求 `segments_per_paper=1` 且 `total_segment_cap=paper_cap`。
- 将 schema 改为只接受 paper cap 3/5、每篇 1 segment、总 segment cap 等于 paper cap；同步 tracked fixture 和 dense missing-model fixture，并新增两项 protocol-deviation 回归测试。

**结果：**
- Python 3.13.7 full suite 55 passed；Black、Ruff、`git diff --check` 通过。
- cap=3/5 现在与 protocol 的 payload policy 一致；旧的 cap=3 + total=6 和 `segments_per_paper=2` 会 fail closed。

**问题与状态：**
- ✅ 已在任何 formal development run 前修复 schema/protocol drift。
- ⏳ 修复提交后，才可从 clean commit 生成 Windows Stage A 三套 qrels protocols。

**决策记录：**
- 以已批准、hash-bound v1.1 文档为权威，不保留历史 fixture 的错误常量；fixture evidence 只用于行为诊断，不得覆盖正式 payload contract。

**相关 Commit：**
- `91b144c fix: align ranking output budgets`

---

**[08:19] 进展记录**

**工作内容：**
- 从 clean `91b144c` 创建 Windows immutable checkout，冻结 3 份仅 qrels 不同的 development Stage A protocols，每份包含 13 个 scorer arms；逐一验证 commit、input、qrels、model manifest、environment lock 与 protocol hashes。
- `stage-a-001` 在 environment collection 前 fail closed：Windows Git checkout 将 lock 文件转成 CRLF，导致协议 hash mismatch。保留失败后恢复 exact commit bytes，并用新 attempt `stage-a-002-lock-bytes` 重试。
- 第二次 attempt 的 12 个 lexical workers 与 dense worker 均在返回 JSON 时失败。真实 Unicode input 含 `U+2009`，Windows worker stdout 默认 GBK，触发 `UnicodeEncodeError`。新增 worker `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8`，parent `subprocess.run(encoding="utf-8")`，并添加 transport 回归测试。

**结果：**
- 两个失败 attempts 均保留：第一个为 `HASH_MISMATCH`，第二个为 candidate-level `WORKER_PROTOCOL_ERROR`，stderr 精确指向 GBK 无法编码 `U+2009`；judge-B/consensus 未启动。
- UTF-8 修复后，Python 3.13.7 与 3.14.6 full suite 各 56 passed；Black、Ruff、`git diff --check` 通过。

**问题与状态：**
- ✅ Environment lock 的 LF/CRLF identity 问题已定位；后续 Windows checkout 必须关闭 `core.autocrlf` 并传输 exact lock bytes。
- ✅ Worker transport 的 Windows locale bug 已修复，不能依赖当前 console code page。
- ⏳ 需从包含 UTF-8 修复的新 clean commit 创建第三个 Windows checkout 与全新 attempt，使用真实 formal input 验证。

**决策记录：**
- 不在旧 Windows checkout 热补丁，也不复用任何失败 attempt；代码修复、测试、commit、new checkout、new attempt 的证据链保持分离。

**相关 Commit：**
- `a406a79 fix: force utf8 worker transport`

---

**[08:47] 进展记录**

**工作内容：**
- 从 clean `a406a79` 创建关闭 `core.autocrlf` 的 Windows immutable checkout，使用新 attempt `stage-a-003-utf8` 完成 judge-A、judge-B、consensus 三套 development Stage A comparisons，共 39 个 scorer arms。
- 复核 resource evidence 时发现 measurement-boundary 实现偏离 protocol：parent 将整个 preflight 墙钟时间记为 cold start，而 worker preflight 在返回前已经构建全部 case corpus，导致 E5 corpus embedding 同时进入 cold-start 与 corpus-build 两项门禁。
- 将 cold start 修正为 parent worker elapsed 减去 worker 报告的各 case corpus-build 总时长；保留进程启动、依赖导入、input 读取、模型加载与 scorer 初始化。新增缺字段、bool、负数、非有限值和大于总 elapsed 的 fail-closed tests。

**结果：**
- `stage-a-003-utf8` 39/39 candidates 成功，三套 comparison 的 payload hashes 完全相同；所有 lexical candidates resource gate 均通过。
- Relevance 方向稳定：三套 qrels 上 BM25 都是最佳 lexical mechanism，E5 nDCG@5 都高于最佳 lexical。E5 只有旧定义下的 `cold_start_p95_seconds` 失败；warm latency、最大 case corpus build、RSS、model size、environment size 均通过。
- 修复后 macOS Python 3.13.7 与 3.14.6 full suite 各 63 passed；Black 26.5.1、Ruff 0.16.5、compileall 与 `git diff --check` 通过。

**问题与状态：**
- ✅ UTF-8 transport 修复已被真实 formal input 证明有效。
- ⚠️ `stage-a-003-utf8` 的 relevance evidence 可审计，但其 cold-start gate 数字不能用于淘汰 E5；必须从包含计时修复的新 clean commit 创建新 attempt 重跑三套 comparisons。
- ⏳ BM25 exact parameter 仍未冻结：judge-A/consensus 名义最优为 `k1=1.6,b=0.5`，judge-B 名义最优为 `k1=1.6,b=0.75`，两者 judge-B nDCG@5 仅差约 0.0005；修正资源测量后再做稳定性判定。

**决策记录：**
- Cold start 回到“process spawn 到 scorer ready”的被测边界；corpus preparation 由独立 `max_corpus_build_seconds` gate 负责，禁止同一工作量重复计罚。
- 不热补丁、不覆盖 `stage-a-003-utf8`；错误测量作为失败证据保留，新代码、新 commit、新 checkout、新 attempt 构成独立证据链。

**相关 Commit：**
- `2c1b332 fix: separate cold start from corpus build`

---

**[09:37] 进展记录**

**工作内容：**
- 从 clean `2c1b332`、关闭 `core.autocrlf` 的新 Windows checkout 运行 `stage-a-004-cold-boundary`，串行完成 judge-A、judge-B、consensus 三套 13-arm comparisons；回传并校验 raw archive。
- 按用户授权充分利用 Windows 算力，冻结只改变 E5 `thread_count∈{1,4,8,16}` 的 full-corpus probe；每档保持 5 次 cold、每 query 30 次 warm、同一 model/input/qrels/output budget。
- 新增 Stage A tracked result 与 v1.3 thread-count overlay，更新 ticket current state；holdout 未读取。

**结果：**
- Stage A 39/39 candidates success；三套 comparison environment SHA-256 均为 `a88ba1e5875e948d6d2fd2f2b40c9a114bea48486fb53cb22c49037668771277`，13 个 candidates 的 12-query payload hash maps 跨 qrels 完全相同。
- E5 corrected cold p95 `5.155–5.546 s`、max case build `26.644–26.846 s`、warm p95 `58.4–59.4 ms`，所有 resource gates pass；三套 qrels 的 E5 nDCG@5 为 `0.855525/0.878195/0.865697`，均显著高于 best lexical。
- BM25 在三套 qrels 都是最佳 lexical mechanism。`k1=1.6,b=0.5` 的 maximum regret 仅 `0.000524`，小于 `b=0.75` 的 `0.002658`，冻结进入 Stage B。BM25/E5 在三套 qrels 都有对方漏掉的 grade≥2 papers，RRF eligibility pass。
- 16-thread 与 1/4/8-thread 的 12/12 payload hashes 完全相同；corpus build total 从 60.948 s 降至 7.863 s（7.75x），max case 从 26.746 s 降至 3.162 s（8.46x），全部 gates pass。后续 Windows formal development evidence 使用 16 threads，1-thread portable CPU FP32 path 保留。
- Stage A archive SHA-256 `a676bce751af7d94f24d8b4c529954d8e538affaf3aea449bb57d4605da8bfba`；thread probe archive SHA-256 `c9ec9d14835e8ebe2ffd502e8e00e73fe41ce2f15beb5cdaf13533d0863bbd96`。

**问题与状态：**
- ✅ Corrected Stage A sensitivity/resource gates 全部通过，E5 与冻结 BM25 进入 Stage B，RRF ablation 合格。
- ✅ Windows 16 logical CPUs 可安全用于本 workload，未出现 observable payload drift 或依赖变化。
- ⏳ 尚未运行 Stage B title/phrase/RRF 与 output-budget calibration；finalists/参数冻结前 holdout 继续 sealed。

**决策记录：**
- 后续 evidence 选择 16 threads 是因为本轮端到端瓶颈为 corpus build；8-thread 虽有最低 warm p95（17.5 ms），16-thread 的 23.8 ms 仍远低于 1 s gate，且 build 更快。
- 不为一次性 prototype 新增 multi-qrels score-replay schema。16-thread 已把剩余重复成本降至可接受范围，三套 qrels 独立运行提供更直接的 sensitivity evidence。
- Stage A 的 1-thread relevance evidence 不因线程 probe 失效：四档 canonical payload 完全相同，且 1-thread 本身通过全部 gates。

**相关 Commit：**
- `3f66601 add: record ranking scorer screen`

---

**[09:55] 进展记录**

**工作内容：**
- 从 clean `3f66601` 新建 Windows checkout，冻结三套只含 BM25/E5、title weight `0/1/2` 的 Stage B1 protocols；全部使用 Python 3.13.7、CPU FP32、16 threads、同一 input/model/lock 和三套 development qrels。
- 串行运行 18 个 arms，回传 raw archive，核验 clean commit、environment identity、candidate status、cross-qrels payload identity 与 resource gates。

**结果：**
- 18/18 arms success；三套 environment SHA-256 均为 `00c7887d1fe0b860e4d2b33a473b43f556782891c113a55a37d3a324d946d91a`，相同 candidate 的 payload hashes 跨 qrels 完全一致，全部 resource gates pass。
- BM25 `title_weight=1` 在 A/B/consensus 的 nDCG@5 均最高：`0.752826/0.720736/0.711809`，冻结。
- E5 `title_weight=1` 相比 0 在 A 的 nDCG@5 下降 `0.007617`，在 B/C 提升 `0.019344/0.017582`；三套 Recall@5 均提高，A/C grade-3 misses 从 5 降至 2，B 保持 0。与 weight 2 的 nDCG 差异均 ≤`0.003710` 且 miss 相同，选择 field coupling 更低、B/C nDCG/Recall 更高的 weight 1。
- Raw archive SHA-256 为 `0fcc3a22c60fc906d15a3f025dc256c79a2cf40d96decfc3bf63219455cb75a4`。

**问题与状态：**
- ✅ BM25 title direction 对三套 qrels 完全稳定。
- ⚠️ E5 exact title optimum 在 judge-A 为 0、judge-B/consensus 为 1；该差异未改变 E5 finalist direction，并按 primary/Recall/catastrophic-miss/complexity 联合证据冻结 weight 1，风险保留到三套 holdout sensitivity。
- ⏳ 下一步只对 BM25 weight 1 做 phrase off/on；之后才做 RRF。

**决策记录：**
- 不用多数票掩盖 E5 title sensitivity；明确记录 A 的 `0.007617` primary trade-off。Weight 1 的 Recall 与 catastrophic-miss 改善跨三套一致，且相对 weight 2 更少依赖 title field。
- Stage B 保持预注册顺序：title 冻结后才允许 phrase/fusion，未入选的 title weights 不在后续复活。

**相关 Commit：**
- `456f363 add: calibrate ranking title weights`

---

**[09:58] 进展记录**

**工作内容：**
- 从 clean `456f363` 创建新 Windows checkout，只比较冻结 BM25 `k1=1.6,b=0.5,title_weight=1` 的 phrase off/on；三套 qrels 各自独立运行。
- 回传并校验 6-arm raw evidence、protocol/environment/commit identity、cross-qrels payload 与资源门禁。

**结果：**
- 6/6 arms success、resource gates pass；三套 environment SHA-256 均为 `b8dd94c82caf97bb0db89385d60c0dbc05559081489f22bcfe9dd521fcd92ace`。
- Phrase off/on 的 12/12 canonical payload hashes 完全相同；三套的 nDCG@5、Recall@5、MRR、EvidenceHit 和 grade-3 catastrophic miss 也全部相同。
- Raw archive SHA-256 为 `bf1917a3eb5d7be53b4620c1cd51d2c9569eca4f8bab923c76af3b624ca98bbf`。

**问题与状态：**
- ✅ Phrase bonus 在冻结 development input 上没有可观察作用。
- ⏳ 下一步用 phrase-off BM25 与 E5 weight 1 比较 RRF k=10/60；holdout 继续 sealed。

**决策记录：**
- 冻结 `phrase_bonus=false`。当 canonical outputs 与全部 metrics byte/数值等价时，额外机制没有进入 finalist 的合理性。

**相关 Commit：**
- `2dab65f add: reject ranking phrase bonus`

---

**[10:05] 进展记录**

**工作内容：**
- 从 clean `2dab65f` 创建新 Windows checkout，在每套 qrels 中按预注册顺序运行冻结 BM25、冻结 E5、RRF-k10、RRF-k60；RRF resource evidence 使用 conservative source composition。
- 回传并校验 12-arm raw evidence、protocol/environment/commit identity、cross-qrels payload 与 resource gates。

**结果：**
- 12/12 arms success、resource gates pass；三套 environment SHA-256 均为 `00c7887d1fe0b860e4d2b33a473b43f556782891c113a55a37d3a324d946d91a`。
- E5 相对 best RRF-k10 的 nDCG@5 高 `0.018333/0.070198/0.071793`，Recall@5 高 `0.009127/0.032738/0.006944`；A/C 各少 2 个 grade-3 misses，B 相同。RRF-k60 更差。
- Raw archive SHA-256 为 `0cc73e376c07dc6b9282a2dcfdcd1cc9390bdeb9666b1d2205cd4b6ab337f3dd`。

**问题与状态：**
- ✅ RRF 虽满足 complementarity entry gate，但没有转化为整体收益，两档均拒绝。
- ✅ 两个 development finalists 冻结为 BM25 baseline 与 E5 best single scorer。
- ⏳ 下一步校准两 finalists 的 paper cap 3/5；budget 冻结前 holdout 继续 sealed。

**决策记录：**
- 不因为 RRF 能找回个别 BM25-only paper 就保留它；它在三套 primary/Recall 上均被 E5 支配，增加 source composition 成本但没有净收益。

**相关 Commit：**
- `25e98e7 add: reject ranking fusion`

---

**[12:39] 进展记录**

**工作内容：**
- 从 clean `25e98e7` 创建新 Windows checkout，对冻结 BM25/E5 各比较 paper cap 3/5；所有 arms 保持 `segments_per_paper=1,total_segment_cap=paper_cap`。
- 三套 qrels 独立运行，回传并核验 12-arm raw evidence、protocol/environment/commit identity、cross-qrels payload 与 resource gates。

**结果：**
- 12/12 arms success、resource gates pass；三套 environment SHA-256 均为 `00c7887d1fe0b860e4d2b33a473b43f556782891c113a55a37d3a324d946d91a`。
- BM25 cap3/cap5 的 EvidenceHit 均为 `0.833333/0.833333/0.833333`，E5 均为 `0.916667/0.916667/0.916667`；两组的 nDCG、Recall、miss 也相同。
- Cap 3 将 mean payload 从 BM25 8,092.3 降到 5,215.5 bytes，将 E5 7,777.3 降到 5,084.1 bytes，按预注册 tie-break 两者均冻结 cap 3。
- Raw archive SHA-256 为 `503eb59176a10b9bb0f8a261ad6aa9c82658ec28363b6556ba5f71e16b942094`。

**问题与状态：**
- ✅ 两个 development finalists 的 scorer、参数、title、phrase 与 output budget 全部冻结。
- ⏳ 下一步做 scoring-only Windows 10-run 与 Mac 3-run determinism replay；通过前 holdout 继续 sealed。

**决策记录：**
- 当 EvidenceHit 与 ranking metrics 完全相同时，按 payload bytes 选择 cap 3；不向模型传入没有观察收益的额外两篇 papers。
- Finalists：BM25 `k1=1.6,b=0.5,title_weight=1,phrase=false,cap=3`；E5 pinned model `title_weight=1,cap=3`。

**相关 Commit：**
- `5ffda00 add: freeze ranking finalists`

---

**[12:57] 进展记录**

**工作内容：**
- 从 clean `5ffda00` 创建 Windows replay checkout，冻结 10 份只改变 attempt ID 的 scoring-only protocols；每份只含两个 frozen finalists，不读取 qrels。
- 使用 16 threads 串行完成 10 个真正的 fresh-process attempts；回传整个 comparison root 并核验 status、commit、environment、resource gates、query hash maps 与 raw JSONL hashes。

**结果：**
- 10/10 attempts success，20/20 candidate runs resource gate pass；10 份 environment SHA-256 均为 `00c7887d1fe0b860e4d2b33a473b43f556782891c113a55a37d3a324d946d91a`。
- BM25 与 E5 各自 12-query payload hash maps 10/10 identical；各自 `payloads.jsonl` 和 `scores.jsonl` 也只有一个唯一 SHA-256。
- E5 10-run cold p95 `5.166–6.620 s`、max case build `3.184–3.439 s`、warm p95 `22.5–37.2 ms`、peak RSS `509,931,520–512,966,656 B`，全部通过。
- Raw archive SHA-256 为 `e1dfed55edfb85e9989fbe9d45c0bc11130b296dd79c524f30104e76e73524a2`。

**问题与状态：**
- ✅ Windows 10-run determinism gate 通过，且不是复制单次结果。
- ⏳ Dense 是 finalist，按协议仍需 Mac 3-run cross-platform replay；完成前 holdout 继续 sealed。

**决策记录：**
- Replay 使用 scoring-only protocol，将 qrels 从 determinism boundary 移除；验证对象严格限定为 frozen scorer/input 产生的 observable paper/segment payload。

**相关 Commit：**
- `91379fa add: prove finalist determinism on windows`

---

**[13:06] 进展记录**

**工作内容：**
- 从 clean `91379fa` 建立隔离 macOS checkout，按 frozen Python 3.13.7 lock 安装 finalist-only 环境，并运行 63 项测试。
- 只运行两个 frozen finalists 的 scoring-only replay；保留首次 import 的环境变化，稳定后再取得 002/003/004 三次正式结果。
- 将稳定集合与 Windows 10-run 逐项比较 query payload hashes、raw payload/score files 与 resource gates。

**结果：**
- 63/63 tests passed；正式 stable set 3/3 attempts success、6/6 candidate resource gates pass。
- Attempts 002/003/004 的 environment bytes 均为 `669,956,676`，SHA-256 均为 `7ecf39c553a9343dc70548a5648b4d34cda5a5aec05a2d1a365927e172e5244d`。
- 两 candidates 的 macOS payload 与 Windows 10-run byte-identical；BM25 score bytes 也跨平台 identical。E5 raw float scores 跨平台有差异，但平台内 3/3 identical，且没有改变排序或 observable payload。
- E5 stable cold p95 `1.636–2.286 s`、max case build `1.136–1.418 s`、warm p95 `7.089–7.767 ms`、peak RSS `896,991,232–1,178,353,664 B`，全部通过。
- macOS raw archive SHA-256 为 `dbc69798438745b47fcdb81a1e13ad8515ecb48678f58d395a77e6eba6a7695d`；private model/input transfer archive SHA-256 为 `a4a335e0c55a010a543de22589200a3efe538ba557c03c384a255289102672fa`。

**问题与状态：**
- ⚠️ Attempt 001 首次 import 生成约 `51.7 MB` `__pycache__`，environment hash 因此变化；该 run 作为 warm-up deviation 保留，不计入正式稳定集合。
- ✅ Windows 10-run + macOS stable 3-run 共同完成跨平台 determinism gate；holdout 现在可按 receipts 解封。

**决策记录：**
- 跨平台 gate 约束 observable payload，不要求不同 CPU/BLAS 下 dense raw floats 逐位相同；排序和最终 payload 必须相同且本次满足。
- Holdout 仅评估已冻结 BM25/E5，不允许利用结果调参、增加 candidate 或重跑择优。

**相关 Commit：**
- `4cad4ae add: prove cross-platform ranking determinism`

---

**[13:14] 决策记录**

**工作内容：**
- 从第一性原理重新审查 Mac/Windows 的职责：区分 bulk evidence 成本、日常 3–36 篇单次排序、跨平台 correctness 与远端依赖风险。
- 将结论提升到仓库根 `AGENTS.md`，并同步 ranking protocol/v1.3，确保后续 Codex 会话和其他 agent 自动读取。

**决策：**
- Windows 固定承担 candidate matrix、calibration、重复 replay 与正式 holdout；从本地 immutable 副本运行并充分使用 16 CPU threads。
- Mac 固定承担 controller、protocol/input preparation、SSH 编排、结果审阅、ordinary single-run 与 Windows-unavailable fallback。
- Mac 必须能运行 frozen scorer 且 canonical payload 与 Windows 相同；不要求 raw float 逐位相同或性能对等。只有 scorer/model/lock/output semantics 改变时才重做 Mac compatibility check，不为每次 Windows evidence 重跑。
- Windows 暂时不可用时不得静默把 bulk evidence 转到 Mac，正式 run 应 fail closed/标记 blocked。

**验证与状态：**
- 现有 Windows 10-run + macOS stable 3-run 已满足当前 scorer/model/lock/output semantics 的 compatibility evidence；后续 holdout 只在 Windows 运行。

**相关 Commit：**
- `d44e8b2 add: codify ranking execution topology`

---

**[13:23] 进展记录**

**工作内容：**
- 按 sealed receipts 解封并验签 holdout judge-A、judge-B、consensus qrels；三套均覆盖 160/160 paper judgments 与 12/12 queries。
- 从 clean `d44e8b2` 建立 Windows isolated checkout，只运行两个 frozen finalists、三套只改变 qrels 的 protocols；顺序固定 A → B → consensus。
- 回传完整 raw evidence，核验 commit/environment/input/qrels/model/protocol/archive hashes、resource gates、逐 query metrics 与跨 qrels scoring identity。

**结果：**
- 三套 comparisons 全部 success，6/6 candidate resource gates pass；environment SHA-256 均为 `00c7887d1fe0b860e4d2b33a473b43f556782891c113a55a37d3a324d946d91a`。
- BM25/E5 各自的 payload 与 score files 在三套 qrels 间 byte-identical；qrels 未进入 scoring。
- Judge-A：BM25/E5 nDCG@5 `0.764606/0.727041`；judge-B `0.840605/0.875249`；consensus `0.799749/0.808047`。
- E5−BM25 的逐 query wins/ties/losses 为 A `5/2/5`、B `5/4/3`、consensus `5/3/4`；grade-3 miss delta 为 `+1/0/-1`。
- E5 resource 范围：cold p95 `5.311–5.593 s`、max case build `2.289–2.408 s`、warm p95 `23.3–24.6 ms`、peak RSS `728,543,232–783,822,848 B`，全部通过。
- Windows raw archive SHA-256 为 `575aac05ad4fbe68d57f5febdeb9373f0826361895ff9645eacec16f1713fcaa`。

**问题与状态：**
- ⚠️ 首次 Windows checkout preparation 使用了未核对的错误完整 SHA；Git 拒绝 checkout，未运行 scorer。随后用实际 `rev-parse HEAD` 恢复同一空 clone，验证 clean 后才开始正式 attempt。
- ❌ Holdout qrels-stability gate 失败：A 的 direction 为 BM25，B 为 E5，consensus 的 E5 优势低于 margin；formal result=`inconclusive`。
- ⛔ v1.2 utility gate 以三套 same winner direction 为前提，本次不生成 12-query card；不得用多数票、重跑、调参或修改 judge/margin 修补当前 attempt。

**决策记录：**
- 当前没有 evidence-backed production winner。BM25 与 E5 继续作为 frozen candidates 保留，但不写 ticket Resolution、不更新 map、不进入 production implementation。
- Future revision 必须使用新冻结 topics 与真正不同 provider/model family 或领域专家小样本，重新预注册；当前 holdout 不再复用。

**相关 Commit：**
- `026cb78 add: record inconclusive ranking holdout`

---

**[13:41] 进展记录**

**工作内容：**
- Robert 质疑“停止 v1.4、以 BM25 为保守默认”的建议是否过早。复核 formal result、ticket、输入 universe 与一手检索评测研究后，区分了“已批准实验完整执行”和“科学问题已经解决”：前者为真，后者为假。
- 调研 near-tied rankers 的 AI qrels、first-party pairwise、跨 provider panel、expert calibration、下游 consumer utility、citation/known-item 与外部 benchmark；新增补救设计研究报告。
- 对研究初稿做反向审计：初稿仍把 12 次 Robert 盲选当 primary，未遵守用户此前关于经验与人工量的反馈。最终改为 fresh topics 上的 AI-consumer-first paired evaluation；Robert 只审阅最终 decision record 和真实 API 批次成本。

**结果：**
- 现有 selection manifest 显示 234 个 eligible cases、12 个已使用，仍有 222 个未进入旧 development/holdout，可建立真正独立的新 batch。
- 推荐 future revision 使用 12 个新 cases/24 queries，冻结 BM25/E5 与 ideation consumer；以两个不同 provider/model-family evaluators 的双位置一致 pairwise 判断为 operational signal，以 exact quote/corpus-boundary validator 为 hard gate。
- 增加 union context 与可行时 full-corpus ceiling diagnostics：若它们明显优于两个 top-3 finalists，说明 evidence budget/cascade 是瓶颈，不能强选 BM25/E5。

**问题与状态：**
- ✅ 已撤回“无需继续 v1.4”的过早结论；没有遗漏或少跑已批准 matrix，但此前停止建议在 evaluation construct 上走了捷径。
- ✅ 旧 holdout 保持 spent/diagnostic-only，不追加 judge、不改多数票、不重跑择优。
- ⏳ AI-consumer future revision 仍是未批准提案；执行前需冻结 exact consumer config、估算调用/token/最坏成本并按 provider/canary 合同取得批次授权。

**决策记录：**
- 不再把 pointwise synthetic qrels 或 Robert 的跨领域论文判断作为默认最终真值；直接评估冻结 ideation consumer 的 source-faithful evidence artifact，并严格限制结论到该 consumer/prompt/query/corpus 分布。
- 若 fresh paired evaluation 仍无材料性差异，才允许按预注册 parsimony policy 采用 BM25 工程默认；不得宣称 BM25 relevance superiority。

**相关 Commit：**
- `4a7d04f add: research ranking evaluation recovery`

---

**[15:56] 进展记录**

**工作内容：**
- Robert 批准 AI-consumer-first future revision，并提出在需要双模型评审时，由其通过本地隔离 agent 分别调用 `deepseek-v4-flash` 与 `kimi-k3`。
- 复核两者的独立性边界与 position-bias 控制，确定后续不复用旧 qrels judge 会话，而是为每个模型建立 `A/B` 与 `B/A` 两个互不共享上下文的 fresh evaluator sessions。

**结果：**
- 两个模型可构成最低 evaluator panel。它们属于不同 model families，但若都由 Kimi Code 编排，则不能声明为两个独立 providers；结论只限于跨模型一致的 synthetic operational signal。
- Controller 将在 judge bundle 准备完成后提供 4 份可直接复制的 exact prompts：Kimi `A/B`、Kimi `B/A`、DeepSeek `A/B`、DeepSeek `B/A`。每份绑定 bundle hash、独立输出目录和 closed JSON schema。

**问题与状态：**
- ✅ Robert 不承担论文打分；仅负责在收到明确提示时启动四个本地隔离 agent runs。
- ⏳ 当前没有 judge bundle，不能提前运行模型。Prompt 不提前冻结，以免路径、hash、schema 与最终 v1.4 protocol 漂移。

**决策记录：**
- 同一模型的两个位置顺序必须在 fresh sessions 中运行，不能把镜像 pair 放在同一上下文中；任一模型 position flip 或两模型 content choice 不一致时，该 query 记为 `unresolved`，不得用多数票或第三票强行决胜。
- 两个 agent 只读取指定 bundle，不看 ranker identity、scores、旧 qrels、旧 holdout winner、另一模型输出、Target Paper 或网络内容；输出目录按 model/orientation 分离，避免再次发生 raw-draft collision。

**相关 Commit：**
- `cf4f887 add: record ranking evaluator plan`

---

**[18:45] 进展记录**

**工作内容：**
- 对 v1.4 调整方案做第一性原理与一手文献对抗性审查，重新界定评测构念、holdout 复用、LLM judge 偏差、统计单位、promotion burden、隔离边界与 provenance。
- 将原“ranker → consumer memo → evaluator”两层方案收缩为直接匿名比较 query + 两组 exact top-3 payload，避免未冻结 consumer、二次摘要与 claim/quote repair 混杂。
- 实现 fresh operational 12-case sampler、operational formal-input path、四路 tool-less 镜像 judge bundles、closed-schema/byte-exact quote validator、case-level exact randomization 与 bootstrap reducer。
- 补强正式输入 provenance：formal manifest 绑定 input approval、selection、protocol、query packet 与 exact input；judge preparation 再绑定 formal manifest、selection、protocol、candidate IDs/payload hashes 和 resource gates。
- 用真实旧 holdout BM25/E5 payload 完成 bundle preparation 及 synthetic-draft validator/reducer 全链 dry-run；旧 evidence 被机械标为 `spent_diagnostic_only`，不能触发 winner。

**结果：**
- 新增 [v1.4 对抗性审查](../../docs/research/local-ranking-v1.4-adversarial-protocol-review.md) 与 [approved v1.4 protocol](../../docs/prototypes/local-literature-ranking-comparison-protocol-v1.4.md)，并更新 ticket 与 harness README。
- `python -m pytest -q tests`：70/70 passed；`python -m compileall -q prototypes/local_ranking` passed；Black 25.9.0 已格式化本次 Python 变更；`git diff --check` passed。
- 旧 holdout dry-run 构建 4 个匿名 mirrored bundles，最新 mapping SHA-256 为 `e2f1a9d24a736e900e8e2224b5d5eda07c76987dc4745d2df928c219f516129a`；完整 reducer dry-run 输出 `decision=diagnostic_only`、`challenger_promoted=false`。

**问题与状态：**
- ✅ 修复 JSONL canonical 校验遗漏行尾换行的问题，否则真实有效 payload 会被误拒。
- ✅ 将 position stability 限定为 winner direction；量表差异保留诊断，避免轻微 score 波动错误清空同方向判断。Catastrophic omission 仍要求两模型 × 两位置四次一致。
- ⚠️ 从仓库根直接运行 pytest 会误收集旧 Mac replay artifact 的同名 tests/__pycache__ 并产生 import mismatch；未删除历史证据，正式验证显式限定 `tests/`。
- ⏳ 付费 Kimi/DeepSeek formal calls 未授权、未执行；须等 fresh prompt 生成后报告 exact bytes、context feasibility、调用与 retry 上界。

**决策记录：**
- 旧 development/holdout 只作 dry-run；正式 winner evidence 必须使用 commit 后机械抽取的 fresh cases，并重新走 Workshop/corpus/query approval chain。
- Primary 只比较相同 top-3 budget 的 frozen BM25 与 E5；union/full-corpus 不进入 winner gate。
- 统计单位为 12 cases，不把 24 queries 伪装成独立样本。E5 只有同时通过 `Delta>=1/3`、one-sided exact `p<=0.05`、resolution/strata/group/catastrophic gates 才晋级；否则只保留 BM25 parsimony default，不宣称 scientific superiority 或 equivalence。

**相关 Commit：**
- `eb69bc8 add: pre-register fresh ranking evaluation`

---

**[18:53] 进展记录**

**工作内容：**
- 在 clean `eb69bc8` 后首次运行 fresh operational sampler；程序在任何 selection/artifact 写出前因某个 stratum 没有 cluster representative、maximin 距离集合为空而 fail-fast。
- 将 operational selection version 从 `v1.0` 提升为 `v1.0.1`，要求 cluster assignment 在每个 stratum 至少放置一个 representative，再执行 maximin fill；同时对空距离集合加入防御性默认值。
- 新增专门回归用例：四个 clusters 在 small/medium 的 strategy 优先级高于 large，证明算法仍会给三个 strata 建立锚点并完成 4/4/4 选择。

**结果：**
- 失败 attempt 未创建 output directory，没有选择或暴露任何 fresh case，故未消耗 formal batch。
- `python -m pytest -q tests`：71/71 passed；compileall、Black 与 `git diff --check` passed。

**问题与状态：**
- ✅ 根因已修复并由对抗性回归覆盖。
- ⏳ 需先提交 `v1.0.1` 修复，再按协议重新运行 fresh selection。

**决策记录：**
- 不复用原 selection version：虽然失败发生在输出前，但 selection objective 增加了“每 stratum 至少一个 cluster representative”，必须通过 version bump 与新 commit 明示，不能静默改变 seed/选择语义。

**相关 Commit：**
- `d557ee6 fix: seed every ranking selection stratum`

---

**[22:44] 进展记录**

**工作内容：**
- 从 clean `d557ee6` 机械选择 fresh operational batch，并以独立 replay 验证 selection/preparation/corpus bytes。
- 只读 12 个 target title/abstract 生成 Workshop；初稿 deterministic gate 12/12 pass 后，语义对抗复核主动拒绝 7 个泄漏具体材料、机制或干预家族的条目，保留 immutable draft-001，再以 draft-002/draft-003 逐步抽象并重验。
- 完整检查 166 篇 target-scoped reference titles 与 abstract-only corpus validation；确认语料覆盖相应研究问题，同时保留真实的弱相关/跨领域负例，未按预期 ranker winner 手工筛 paper。
- 修复 input approval validator 将 protocol version 硬编码为 v1.1 的兼容缺口：operational selection 现在要求 exact v1.4，legacy selection 仍要求 v1.1。

**结果：**
- Fresh selection：12 cases、222 fresh candidates、8/8 clusters、small/medium/large=`4/4/4`、spent target overlap=`0`、strategy-2 count=`2`。
- 两个 preparation attempts 共 52/52 files byte-identical；selection/preparation/workshop-authoring SHA-256 分别为 `546cf089f6ff3d6374e095f426afd90ab82c3ecd9f438a6cdce72456318a8f44`、`7036163b836c80687055ffdf65a25bae174baf432cce9b554d0d8e26fdea236a`、`bfcc642ccf20d399ead6c8bb57068eeea9bad7a3a6d1c247325179e529169656`。
- Workshop draft-003 deterministic gate 12/12 pass；`python -m pytest -q tests` 72/72 passed，compileall/Black/diff-check passed。

**问题与状态：**
- ✅ draft-001 的具体答案泄漏未进入 approval；其 bytes 与原 validation hash 保持一致，后续 revisions 使用不同 draft-set，不覆盖失败证据。
- ✅ v1.4 approval-version blocker 已修复并新增回归测试。
- ⏳ 下一步生成 hash-bound semantic/corpus decision record，并以 draft-003 建立 immutable input approval/query-author packet。

**决策记录：**
- Reference corpus 的作用是保留 target-scoped candidate universe，而不是预先保证每篇都高度相关；不能因为少数标题偏离 Workshop 就人工删除，从而在排名前改造难度或泄漏目标答案。
- Workshop 必须比 Target 再抽象一层：保留问题边界，但删除论文特有材料、named intervention、observed direction 与具体机制，即使这些词已出现在 Target title 中也不作为后续 query 的提示。

**相关 Commit：**
- `505cd08 fix: bind operational input approval protocol`

---

**[23:05] 进展记录**

**工作内容：**
- 研究本机 Kimi Code alias `opencode-go/deepseek-v4-flash` 是否足以作为 local-ranking v1.4 的第二 AI 裁判；只读检查非秘密模型配置，并核对 DeepSeek、OpenCode、Kimi Code 的一手文档与固定 registry commit。
- 将“公开身份/规格可验证性”与“本项目正式裁判准入”分开，提出只使用 spent payload 的最小 qualification gate，覆盖 rubric、closed JSON、byte-exact evidence、双方向镜像稳定性、Kimi K3 角色独立性和失败替换条件。

**结果：**
- 新增 [DeepSeek V4 Flash 裁判适配研究](../../docs/research/deepseek-v4-flash-judge-fit.md)。确认当前 OpenCode Go registry commit `4a3a072b45d6d79611b6d1ccddf23f22a7b4cfc2` 将该 alias 路由到 `deepseek/deepseek-v4-flash-0731`；本机 Kimi Code CLI 为 `0.39.1`。
- 未调用任何付费模型，未修改 v1.4 protocol 或 harness；`git diff --check` 通过，报告未记录 credential。

**问题与状态：**
- ✅ 公开规格足以支持进入 qualification：1M context、thinking、`low/high/max` effort 与 JSON Output 均有官方证据。
- ⚠️ alias 仍是浮动名称；正式 evidence 必须保存 registry/响应元数据和时间戳，不能无条件宣称固定 checkpoint。
- ⏳ 正式裁判资格仍待 spent-data qualification；公开 agent/coding benchmark 不等于学术检索 setwise judge 准确率。

**决策记录：**
- 保留 DeepSeek V4 Flash 作为第二裁判候选，最终冻结以 `high` effort 做与正式 24-item bundle 同形的双镜像 qualification；只有 rubric、schema、evidence、position 与 cross-model gates 全部通过后才准入 fresh formal evaluator calls。先证明计划正式使用的较轻配置足够，不能用 `max` 认证 `high`。
- 同属 Kimi Code harness 不否定不同 model-family 的价值，但不得声称 two-provider independence；若重复 qualification 失败，优先改用可固定版本/structured-output 的 DeepSeek transport，其次才升级 Pro 或替换为另一模型族。

**相关 Commit：**
- `c69e42c add: qualify isolated ranking judges`

---

**[23:13] 进展记录**

**工作内容：**
- 直接调用本机 Kimi Code `0.39.1`，以独立 `K2.7 Coding` 模型、fresh session、空工作目录、空 skills directory、`tools: []`、`subagents: []` 只读取 hash-bound 12-Workshop prompt，生成 operational query draft。
- 新增 deterministic query-author finalizer：只接受 bare JSON 或 Kimi text renderer 的单一 `• ` 前缀，校验 12 cases / 24 broad+focused queries、closed schema、顺序、ASCII、长度、唯一性、禁用 evaluation identity，并绑定 raw stdout/stderr、prompt、agent、packet、input approval、protocol、CLI/model/config hashes。
- Controller 逐项对照 Workshop 完成语义复核，按 Robert 已授予的决策权原样批准 24 条 query，未人工改写模型文本。
- 将 DeepSeek/Kimi 裁判资格测试冻结为独立 companion protocol：旧 spent 24-item、双模型双镜像、`high` effort、机械/镜像/side-collapse/cross-model/controller audit/identity gates；qualification 不贡献 winner。

**结果：**
- Kimi query author 首轮 exit code `0`；raw stdout `8150` bytes，query manifest 12/12 cases、24/24 queries PASS，SHA-256 `f073c1434d76630405ac7f6ec9aae1a3cc76710abc22b80142da78c53fd9befa`。
- Query approval 为 `approved_as_is`、rewrite count `0`，SHA-256 `4af27dd0a1d7f058325ca6272c7aef28d4559ba13e56266190a0dfc0a2a3794e`。
- 新增 `query_authoring.py` 与 5 个拒绝/绑定测试；`python -m pytest -q tests` 为 77/77 passed，changed-file Black 25.9.0、compileall 与 `git diff --check` 通过。

**问题与状态：**
- ✅ CLI text mode 在 stdout 首部增加 `• `；原始 bytes 完整保留，finalizer 只白名单剥离这一固定 renderer prefix，未重跑付费调用或宽松清洗。
- ✅ Query author 使用 K2.7，避免与后续 Kimi K3 / DeepSeek V4 Flash 裁判角色重合。
- ⏳ Formal operational input 尚待从 clean commit 物化；DeepSeek 只有 specification PASS，仍须通过 spent qualification 才能进入 fresh evaluator panel。

**决策记录：**
- `transcatheter` 作为 Workshop 中 `catheter-based` 的直接同义改写，不加入外部疗效事实或目标答案，允许保留；所有 query 同时保持 Workshop-only 和 neutral retrieval intent。
- 资格与正式 evaluator 计划统一使用 `high` effort；若只有 `max` 能通过，必须版本化 protocol、重报成本并全量重验，不能用 max 的结果替 high 背书。

**相关 Commit：**
- `c69e42c add: qualify isolated ranking judges`

---

**[23:20] 进展记录**

**工作内容：**
- 从 clean `c69e42c` 使用 pinned E5 tokenizer 与已批准 operational input/query chain 物化 formal operational input，并以新 immutable output root 完整 replay。
- 新增通用 Kimi response extractor，为后续 qualification/formal judge 调用保留 raw prompt/agent/config/stdout/stderr/timestamps/exit code，只允许 bare JSON 或单一 Kimi renderer `• ` 前缀，再输出 canonical response 交给独立 judge validator。

**结果：**
- Formal input：12 cases、24 queries、166 papers、187 shared segments；exact E5 inputs 最大 query/title/segment tokens=`24/69/512`，全部未越界。
- Formal manifest SHA-256 `590a344776b17f0ee416abe269e2419632525b7b3bef0ace9702c810462a94a3`；operational input SHA-256 `5c440b6e57f3c1764961557795378b788220e6b582db309a913f727045376385`；两个 attempts 3/3 files byte-identical。
- `python -m pytest -q tests`：81/81 passed；Black 25.9.0、compileall 与 `git diff --check` 通过。

**问题与状态：**
- ✅ Mac 仅执行 controller/input preparation，未重复 bulk ranking；下一步仍由 Windows clean checkout 执行 BM25/E5。
- ✅ Renderer extraction 与 semantic validation 分离；extractor 不 repair、补字段、续跑 session 或修改模型判断。
- ⏳ Extractor 需先提交，再按 frozen spent qualification protocol 启动四个本地 Kimi Code fresh sessions。

**决策记录：**
- Formal input replay 已证明物化确定性；不需要在 Mac 重跑 candidate matrix，避免无收益的重复负载。
- 四个 qualification calls 使用已冻结的 `high` effort 和 spent bundles；其结果只能决定 panel 资格，不能改变正式 winner。

**相关 Commit：**
- `f986dc3 add: extract immutable kimi responses`

---

**[23:29] 进展记录**

**工作内容：**
- 从 frozen spent qualification bundle 启动首个 Kimi K3 / orientation-1 / high / fresh tool-less session；保存 raw invocation evidence 后经新 extractor PASS，再进入原 closed-schema quote validator。
- Validator 在 `lr-hol-01-focused` 拒绝 quote `noiseless and noisy`；逐字对照 bundle 确认该 19-scalar phrase 原文存在，但 validator 要求 20–500，而 prompt/draft contract 未披露长度。
- 保留整个 qualification-001 失败 attempt，不启动其余三个 calls；将 bundle/preparation schema bump 至 `v1.0.1`，在公开 draft contract 与 prompt 中显式加入 `20-500 Unicode scalars`，新增回归断言。

**结果：**
- K3 首轮 invocation exit code `0`，raw stdout/stderr=`11,595/29,523` bytes；extraction receipt PASS，但 judge result 为 `QUOTE_NOT_EXACT`，没有生成 trace/result，也未进入 mapping/reducer。
- 根因是 hidden validator constraint，不是引用内容不存在；旧 output 未 repair、未重跑、未用于 judge qualification。
- 修复后 `python -m pytest -q tests`：81/81 passed；Black 25.9.0、compileall 与 `git diff --check` 通过。

**问题与状态：**
- ✅ 在只消耗 1/4 qualification calls 时暂停，避免让其他模型对不可见规则重复失败。
- ⚠️ 已输出的 qualification protocol v1.0 与 spent-holdout-002 bytes 保持不变，作为失败证据；修复必须由新 bundle 和 v1.0.1 companion protocol 重新绑定，不能原地改旧协议。
- ⏳ 提交 schema/prompt correction 后，需用 current v1.4 hash 重建 spent bundles，计算新 prompt hashes，再冻结 v1.0.1 qualification protocol 并四路全量重跑。

**决策记录：**
- 保留 20–500 quote 长度约束：它避免极短词组冒充可审计证据；问题在于规则未公开，而不是 validator 严格本身。
- 这属于 deterministic contract bug，且 mapping 未解封、formal fresh data 未接触；允许版本化修复后全量重跑，不允许仅重跑失败 item/orientation。

**相关 Commit：**
- `147a241 fix: disclose judge quote length gate`

---

**[23:31] 进展记录**

**工作内容：**
- 从 clean `147a241` 以 current v1.4 protocol hash、旧 spent formal holdout、冻结 BM25/E5 payloads 重建 corrected setwise qualification bundles。
- 新增 qualification protocol v1.0.1，只覆盖 exact bundle binding 与公开 quote length contract；v1.0 的 evaluator、high effort、isolation 和全部准入阈值保持不变。

**结果：**
- Corrected preparation schema `local-ranking-setwise-preparation-v1.0.1`，manifest SHA-256 `bd38345e82ec9f5d75f2ed3aefcda5084f3cc7c7b5a19420396db747af6971cd`，mapping commitment `c6ebae6f7b0809308da24b66e04a86511e17c327ae13d7e860dc3c714310b868`。
- Kimi 两份 prompts 各 132,480 bytes；DeepSeek 两份各 132,494 bytes；4/4 prompts 均明确包含 `20-500 Unicode scalars`，hashes 已写入 v1.0.1 protocol。

**问题与状态：**
- ✅ v1.0 失败协议与旧 output bytes 未改；v1.0.1 使用新路径、新 hashes 和四路全量 fresh sessions。
- ⏳ 需先提交 v1.0.1 protocol，再开始 corrected qualification attempt，确保门槛早于输出冻结。

**决策记录：**
- 只修正模型可见 contract，不修改 validator、rubric、mapping、数据或资格阈值，因此属于允许的 deterministic implementation correction。
- Corrected bundles 使用 final v1.4 protocol SHA-256，消除了旧 dry-run bundle 与 current parent protocol hash 不一致的问题。

**相关 Commit：**
- `6377444 add: freeze corrected judge qualification`

---

**[23:43] 进展记录**

**工作内容：**
- 启动 corrected qualification-002 的 Kimi K3 orientation-1；extractor 通过后，validator 再次在 24 条中的一条拒绝 evidence quote。
- 只读核验确认模型把 visible source `CVA-Net) for` 写成 `CVA-Net for`，属于一个括号的逐字复制误差；没有继续另外三个 calls，也没有 retry/repair。
- 从目标构念重新审查 exact-quote gate：完整 segment 已由 bundle hash 固定，quote substring 只能测复制精度、不能证明语义支持；将 v1.5 evaluator overlay 冻结为 visible `side/paper/segment` reference + 20–500 scalar support statement，并保留 6-item controller grounding audit。
- 实现 setwise schemas v1.1、完整 model alias/provider/high-effort profile、v1.4 base + v1.5 evaluator 双 protocol hash binding、evidence-ref closed validator 与统计报告版本升级。

**结果：**
- Corrected K3 invocation exit code `0`，raw stdout `14,895` bytes；response extraction PASS，但 old-schema judge validation 为 `INVALID/QUOTE_NOT_EXACT`，未生成 trace/result，qualification-002 不成立。
- v1.1 synthetic four-orientation reducer 仍验证 case-level统计与 promotion semantics 不变；`python -m pytest -q tests` 81/81 passed，Black/compileall/diff-check 通过。

**问题与状态：**
- ✅ 停止“重试直到抄对”的选择性成功路径；两次失败 attempts 与旧协议保持原样，均不贡献资格或 winner。
- ✅ Evidence reference 只能指向当前 item 中唯一可见 tuple；support 不是 quote，语义真实性由预注册 blind audit 与跨模型/跨位置一致性共同约束。
- ⏳ 需提交 v1.5/schema v1.1 后重建 spent bundles，再用 exact hashes 冻结 qualification v1.1，之后才允许新四路调用。

**决策记录：**
- 一个括号的复制误差不应淘汰模型的 setwise 判断能力；exact quote gate 与目标构念错位，删除它提高的是 construct validity，不是放宽未知 ID、缺项或外部证据规则。
- Formal input/query/retrieval 继续绑定 v1.4；v1.5 只作为 evaluator overlay 单独绑定，避免无语义必要地重做 fresh query authoring。

**相关 Commit：**
- `8505e1c fix: reference judge evidence by segment`

---

**[23:45] 进展记录**

**工作内容：**
- 从 clean `8505e1c` 使用 base v1.4 + evaluator v1.5 双 protocol binding 重建 spent qualification bundles，并检查四份 prompts 的 segment-reference contract。
- 冻结 qualification protocol v1.1：exact aliases/providers/high effort、四个 prompt bytes/hashes、mapping commitment、fresh/tool-less execution、mechanical/mirror/side/cross-model/6-item grounding gates。

**结果：**
- Preparation schema `local-ranking-setwise-preparation-v1.1`，manifest SHA-256 `bca64fc52b9d80453624f3b38dda41ea5fbb9c7db58d49dde160e96ec0fa73e9`，mapping commitment `877e6ee16baf37ac22462e9a1e926d647abab3815beed9301ef7b6e630654335`。
- Kimi prompts 各 132,747 bytes，DeepSeek prompts 各 132,762 bytes；4/4 包含 support length 与 visible segment reference 规则。

**问题与状态：**
- ✅ 旧 quote attempts 完全排除；新 attempt ID 固定 `qualification-003`，没有读取任何 v1.1 model output。
- ⏳ 提交 qualification v1.1 后才能启动四个新 sessions。

**决策记录：**
- 资格 receipt 名称固定 `qualified_for_fresh_v1.4_v1.5`，明确区分 base retrieval protocol 和 evaluator schema overlay。
- Qualification 通过也只解锁 fresh prompt preparation；formal 四次调用仍需单独报告 exact size/cost/retry 上界并获授权。

**相关 Commit：**
- `89edba5 add: freeze segment-reference qualification`

---

**[03:54] 进展记录**

**工作内容：**
- 执行 qualification-003 的 Kimi K3 orientation-1；response extraction 与 v1.1 segment-reference validator 均通过，但 result 只有 12 judgments，暴露 v1.1 协议写的 24-item gate 与实际 spent-holdout-only bundle 不一致。
- 立即暂停其余三个模型调用，保留该 12-item PASS 作为规模不足的诊断证据，不允许它贡献最终资格；根因是旧 development 与 holdout 各有 6 cases/12 queries，而准备命令只读取了 holdout。
- 新增 `qualification_input.py`，把已耗尽的 development + holdout 输入、selection、两份 comparison summaries 与四份 finalist payload files 装配为显式 `spent_qualification` 派生集；manifest 对五个组合输出和九个源文件 hash 做 fail-closed 绑定。
- 扩展 ranking input 与 operational judge，只允许该派生 split 使用专用 manifest/selection schema，强制 12 cases/24 queries、每个 corpus-size stratum 4 cases、`spent_diagnostic_only`，并增加端到端回归测试与 replay 命令文档。

**结果：**
- qualification-003 K3 orientation-1：12/12 mechanical PASS，trace SHA-256 `c62e5b7e180094797d0c258dda64bd5f3b1bc9479a2d0506d60d7cbfc40cb337`；因不满足预登记 24-item scale 而排除。
- 新测试证明两个 12-query spent splits 被确定性合并为 12 cases/24 queries，且四个 judge bundles 各包含 24 items；`python -m pytest -q tests` 为 82/82 passed，Black 25.9.0、compileall 与 `git diff --check` 通过。

**问题与状态：**
- ✅ 在首个半规模调用完成后暂停，没有继续消耗另外三个错误规模 calls，也没有把 12/12 误报为最终资格。
- ✅ 规模现在由 input parser、derived manifest、selection strata 与 end-to-end judge preparation 四层共同约束，不能再靠协议文字假设。
- ⏳ 需先提交装配修复，再生成全 24-item immutable bundle，冻结 v1.2 exact hashes 后才能重新启动四个 fresh sessions。

**决策记录：**
- Development 与 holdout 都已在既往实验中暴露，合并只用于裁判准入诊断，不产生 ranking winner，因此不会污染 fresh operational evidence。
- qualification-003 永久保留且排除；正确规模调用使用新 attempt ID，禁止补跑其余三路后与半规模 K3 混合。

**相关 Commit：**
- `dc18caf fix: assemble full judge qualification input`

---

**[04:01] 进展记录**

**工作内容：**
- 从 clean `dc18caf` 物化 combined spent qualification input，并用独立 attempt 重放；随后用 base v1.4 + evaluator v1.5 生成正确规模的双模型双镜像 bundles。
- 冻结 qualification protocol v1.2，绑定 corrected input/preparation manifests、四份 24-item prompts、mapping commitment、模型配置、重试上限和原 v1.1 全部准入门槛。

**结果：**
- Combined input manifest SHA-256 `491ce2f205251098da98b2b4bc03286565864a85e2cf9d63fb2ea5bbea1b64c1`；12 cases / 24 queries，small/medium/large 各 4 cases；replay 6/6 files byte-identical。
- Setwise preparation manifest SHA-256 `59ea7fd753d9ee6de0ad0f11da35b9c331aef37c8fa64f526c4734478d062958`，mapping commitment `339ad6034a7c0e9bd17b55ad57c05fff1a5fc787f3a8d0066752c106442271ec`。
- Kimi prompts 各 264,880 bytes；DeepSeek prompts 各 264,895 bytes；四份 bundle 均机械确认 24 items。

**问题与状态：**
- ✅ Corrected input/preparation 全程未读取或复用 qualification-003 judgment；旧 12-item PASS 明确排除。
- ⏳ 需先提交 v1.2 协议，之后才允许启动 qualification-004 四个 fresh sessions。

**决策记录：**
- 只修正 spent qualification 的覆盖规模，不修改模型、rubric、阈值或正式 fresh data，因此属于输出前可审计的 deterministic protocol correction。
- 每次模型运行按历史 3–4 分钟耗时设置长等待，不做 30 秒高频轮询。

**相关 Commit：**
- `43027ff add: freeze full judge qualification`

---

**[04:23] 进展记录**

**工作内容：**
- 启动 qualification-004：Kimi K3 两个 orientations 分别完成 extraction 与 v1.1 closed validation，均为 24/24 PASS；DeepSeek V4 Flash orientation-1 的 transport/extraction 成功，但 validator 以 `UNSUPPORTED_SCHEMA` fail closed，因此未启动 orientation-2。
- 只读诊断确认 DeepSeek 把 bundle root 的 `local-ranking-setwise-judge-bundle-v1.1` 复制为 response schema；将该字段单独替换为 draft contract 已给出的 `local-ranking-setwise-judge-draft-v1.1` 后，其余 24 judgments 可全部通过 validator。该诊断结果不写回、不贡献资格。
- 对 prompt 与 validator 做逐项对抗性核对，发现 prompt 虽嵌入 `draft_contract`，却没有明确要求复制其 schema/attestation，也未直接列出 rationale 长度、winner/catastrophic enums、unique refs 等机器约束；开始将所有 construct-irrelevant closed-schema 约束显式化并增加回归断言。

**结果：**
- Kimi orientation-1/2 trace SHA-256 分别为 `bbb0cf497cf04d43973f0b42a6ad41fc5ded432d402b3f46590db620dec6217e`、`e3c74cec28a56ba13f7d41c8e8f034733146d2ef4aa5944935ef3b8bf3ff8877`。
- DeepSeek response 24 items、root keys 正确，response SHA-256 `3d28cff58d7e8997de1b18c8764dd1774cc34298d264318d3d3034640b0e670f`；唯一机械失败是 ambiguous schema label，但 qualification-004 仍按原协议判 FAIL，禁止 repair/retry。

**问题与状态：**
- ✅ qualification-004 artifacts 保持 immutable；没有打开 private mapping，没有启动已无意义的 DeepSeek orientation-2。
- ⚠️ v1.2 prompt 同时暴露 bundle schema 与 nested draft schema，却要求一个未写明精确值的 response schema，格式失败混入了不属于裁判语义能力的歧义。
- ⏳ 修复必须作为新 preparation/protocol/attempt 全量重跑四路，不能只给 DeepSeek 特殊补考；提交前先用测试保证 validator 的非语义约束都在 prompt 中显式可见。

**决策记录：**
- 不据此直接淘汰 DeepSeek Flash：schema-only diagnostic 证明其 24 judgments 机械结构其余部分合格；目标构念是 setwise evidence judgment，不是猜测两个 schema 常量哪个应复制。
- 为保持双模型公平，新 prompt 不能只给 DeepSeek；qualification-005 将让 Kimi 与 DeepSeek 全部从 fresh sessions 重跑。此次修复不改变 rubric、数据、mapping、阈值或任何 judgment 内容。

**相关 Commit：**
- `64cdfab fix: expose judge output contract`

---

**[04:29] 进展记录**

**工作内容：**
- 从 clean `64cdfab` 重建 explicit-output-contract setwise preparation；四个 bundles 与原 v1.2 完全相同，仅 prompt 增加明确的 closed-schema 常量与边界。
- 冻结 qualification protocol v1.3，要求 qualification-005 对 Kimi/DeepSeek 四路全部 fresh 重跑，禁止复用 qualification-004 的两个 Kimi PASS。

**结果：**
- Preparation manifest SHA-256 `b71fc9b932193d4481520fc0920c635e0cc088165c85b5408e4afd2ce074f7ab`；mapping commitment 仍为 `339ad6034a7c0e9bd17b55ad57c05fff1a5fc787f3a8d0066752c106442271ec`。
- Kimi prompts 各 265,418 bytes，DeepSeek prompts 各 265,433 bytes；四份均为 24 items，并包含 exact draft schema/attestation 指令。

**问题与状态：**
- ✅ Prompt change 没有改变 blind payload 或 side assignment；qualification-005 开始前未读取任何新输出。
- ⏳ 提交 v1.3 后启动四个 fresh sessions；按已测耗时 Kimi 使用约 5 分钟长等待，DeepSeek 使用约 2 分钟长等待。

**相关 Commit：**
- `d77cd6e add: freeze explicit judge qualification`

---

**[04:46] 进展记录**

**工作内容：**
- 执行 qualification-005 的 DeepSeek V4 Flash 两个 orientations：orientation-1 在 explicit contract 下 24/24 PASS；orientation-2 虽返回 24 judgments，却完全漏掉 prompt 已明确指定的 `schema_version`，closed validator fail closed。停止后续 Kimi calls，避免在 panel 已失败时继续消耗。
- 对 orientation-2 做不写回的 schema-only diagnostic：仅补入 exact schema 后其余 24 judgments 可通过，证明 Flash 的语义/主体结构可用，但 Kimi Code text transport 的 closed-output reliability 不足。
- 使用官方 primary sources 调研 DeepSeek Responses API；确认官方 `/responses` 支持 `deepseek-v4-flash`、`reasoning.effort=high` 和 `text.format=json_schema`，并记录 response terminal/usage/model/failure 边界与当前价格。
- 实现最小 `deepseek_responses.py` adapter：固定 official endpoint/model/high/JSON Schema/131,072 max output，credential 只从 env 或本机 Kimi config 内部读取，request 不含 Authorization evidence；raw response、safe headers、timestamps、usage 与 canonical draft 分层保存，仍交给原 local semantic validator。
- 将 `judge-deepseek` transport profile 从 OpenCode Go + Kimi CLI 改为 DeepSeek official Responses；新增 request/schema binding 与 provider-response fail-closed tests。

**结果：**
- qualification-005 DeepSeek orientation-1 trace SHA-256 `b93742b7d6f2e5000433858e0193a92443d6dc758be0773e0a41b7698ddc49b5`；orientation-2 response SHA-256 `3e1f0c70a84f0a3a72f8c8e0bd0b17a111b8c6b39bec2b51403666ebe5b6337c`，因 missing `schema_version` 排除。
- 结构化 transport 调研报告写入 `docs/research/deepseek-v4-flash-structured-transport.md`；没有读取 credential、没有调用官方 API、没有产生费用。
- `python -m pytest -q tests` 为 84/84 passed；Black 25.9.0、compileall 与 `git diff --check` 通过。

**问题与状态：**
- ✅ Flash 在两次 text-transport qualification 中分别出现 wrong schema 与 missing schema，已满足预注册的“重复失败后更换 structured-output transport”条件；不需要先升级模型能力。
- ✅ qualification-005 保持 immutable；没有打开 private mapping，Kimi 也没有产生可选择性复用的新输出。
- ⏳ 需先提交 adapter，重建 official-provider DeepSeek bundles 与 exact Responses requests，再冻结新 protocol；付费 smoke/qualification 调用前单独报告 request size、token/cost 上界并取得授权。

**决策记录：**
- 选择同一 V4 Flash + official `json_schema`，而非直接升级 Pro：观察到的唯一失败是非语义字段可靠性，structured transport 是更直接、更轻、更可归因的修复。
- `json_schema` 不能替代本地 validator；官方未文档化完整 JSON Schema subset/strict，实机不支持某关键字时必须以 transport qualification 失败记录，不能静默降级到 JSON object。
- `deepseek-v4-flash` 是浮动别名，正式 evidence 只能记录 official endpoint/model/created_at 与调用时规格页版本，不能声称密码学固定 0731 checkpoint。

**相关 Commit：**
- `1aafd39 add: constrain deepseek judge responses`

---

**[04:53] 进展记录**

**工作内容：**
- 从 clean `1aafd39` 重建 judge-deepseek profile 为 official Responses 的 setwise preparation，并为两个 orientations 物化 credential-free canonical request + conservative JSON Schema。
- 冻结 qualification protocol v1.4：exact prompt/request/schema hashes、131,072 output-token ceiling、provider/local double validation、调用顺序、terminal/retry semantics 与 peak-price cost cap。

**结果：**
- Setwise manifest SHA-256 `de1198a4b949f68059393e33b4fe6545232771464b13bc2b30240620369fd7de`；mapping commitment 未变。
- DeepSeek orientation-1/2 requests 各 273,275 bytes，SHA-256 分别为 `68cb731f725d746110ad31318645757e85bd7a30c70dc28c9a3083650464a45b`、`f33552e30b605c10285722d3cb7630cf6ec89a1c47b85d4d31d9c38146257a64`；全程未读取 credential、未联网、未计费。
- 两次 direct qualification calls 的保守 peak-price 上界 `< USD 0.59`；off-peak 约减半，实际值以后端 usage 为准。

**问题与状态：**
- ✅ Request 规模 + 131,072 output ceiling 仍显著低于 1M context；provider JSON Schema 不取消本地 validator。
- ⏳ DeepSeek official API 会产生独立按 token 费用，超出此前仅授权 Kimi Code managed quota 的调用路径；需 Robert 明确批准最高 `< USD 0.59` 后才执行。

**相关 Commit：**
- `7c2e32f add: freeze structured judge qualification`

---

**[09:06] 进展记录**

**工作内容：**
- 按 Robert 的 exact provider constraint 终止尚未调用的 DeepSeek official Responses 路线；恢复同一个 `opencode_go_structured_transport` subagent 完成 384 行一手证据报告，没有因其额度中断另开替代 agent。
- 核对 OpenCode Go 官方 endpoint matrix、固定 gateway/registry 源码与本机非秘密 provider 配置；确认 Flash 唯一可冻结入口是 `POST /zen/go/v1/chat/completions`，`/responses`、raw `json_schema` 与 reasoning 实际执行均未获 Go contract 证明。
- 删除 `deepseek_responses.py`，新增 `opencode_go_chat.py`：固定 OpenCode Go direct non-streaming Chat、裸 model ID、`json_object`/`reasoning_effort=high` qualification candidate、32,768 output ceiling、credential 隔离、raw response/usage/identity/hash receipt 和本地 fail-closed validation。
- 将 model draft 根字段缩减为 exact `bundle_sha256/evaluator/judgments`；移除模型自报的 `schema_version/attestation`，由 controller/validator/execution receipt 承担版本与隔离证据；所有 judgment 语义、coverage 和 visible segment reference gates 不变。
- 新增 v1.6 evaluator overlay，冻结 provider/transport/retry/evidence/data-retention 边界；更新 harness README 与 statistics schema v1.2。

**结果：**
- `python -m pytest -q tests` 为 84/84 passed；Black 25.9.0、`python -m compileall -q prototypes/local_ranking` 与 `git diff --check` 全部通过。
- OpenCode Go credential 仅确认配置项存在，未打印/保存值；没有读取 credential value、没有模型调用、没有 DeepSeek official 调用或额外按 token 费用。
- 当前 OpenCode Go 页面仍写明 DeepSeek current ZDR agreement 只到 2026-08-31；2026-09-01 不能再声称 0-day retention。

**问题与状态：**
- ✅ provider 已恢复为 `opencode-go`，并移除 Kimi CLI renderer 这个已观察到的 closed-output failure surface。
- ✅ qualification-004/005 保持 immutable，private mapping 仍未打开；旧 official v1.4 protocol 仅作被取代的设计证据。
- ⏳ 先允许不含 interview/Workshop/query/真实 corpus 的 synthetic transport probe；24-item spent qualification 与 fresh/private calls 在新 ZDR 证明或 Robert 明确接受 exact payload 的 retention 风险前停止。

**决策记录：**
- 不把两次漏/错冗余 schema 字段误判为 Flash 裁判语义能力不足；改为删掉不服务实验构念的模型自报元数据，同时保留最小 bundle/evaluator binding 与完整本地 semantic validator。
- 不继续 Kimi renderer，也不切 DeepSeek official；direct OpenCode Go Chat 是同时满足用户 provider 约束、减少 renderer 混杂并保留可审计 raw response 的最短路径。
- `json_object` 和 `reasoning_effort=high` 只在 synthetic/spent qualification 通过后才能记录为目标账号实测能力，不能从 OpenAI-compatible 名称直接推断。

**相关 Commit：**
- `dd0ea3e fix: keep deepseek judge on opencode go`

---

**[09:09] 进展记录**

**工作内容：**
- 为 v1.6 live transport smoke 新增 deterministic `prepare-synthetic`：只生成一个 checksum/file-corruption 虚构 case、两侧各 3 段 synthetic evidence，并绑定 v1.4/v1.6 protocol hashes、bundle self-hash 与 OpenCode Go request manifest。
- 新增回归测试，证明生成的 bundle 只有 1 个 synthetic item、不含 interview/Workshop/publisher abstract 文本，并且 direct request message byte-for-byte 使用生成 prompt。
- 生成 `synthetic-001` 作 pre-commit rehearsal，核对 request 为 5,645 bytes、`json_object`、snake-case high effort、non-streaming；该 rehearsal 因生成时 source 尚未提交而不用于 live call。

**结果：**
- `python -m pytest -q tests` 为 85/85 passed；compileall、Black 25.9.0 check 与 `git diff --check` 通过。
- Synthetic bundle/prompt/request SHA-256 分别为 `696b5b8ca0b67106c56f0c8bb324490295bf677fc07a9af7f2eaf0ad8dd15180`、`7de9d42c340738bce0953727314422af5dc32b572f12d89434177345a2b037f7`、`04d470f736e74281ec45cb169a7095359212d7585d07e7812c9e747903f6232d`；只用于生成器 rehearsal。

**问题与状态：**
- ✅ Synthetic packet 明确隔离于真实 spent/private payload，可在 retention 未确认时安全用于 transport shape qualification。
- ⏳ 需提交生成器后从 clean commit 生成新 attempt，再把 exact hashes 预注册到 transport qualification protocol；`synthetic-001` 不发送。

**决策记录：**
- 不从现有 spent bundle 人工删减内容，避免漏出真实 query/corpus 和不可重放的临时编辑；用代码内置 deterministic fixture 形成可审计 privacy boundary。

**相关 Commit：**
- `28ef8ae test: add synthetic opencode transport probe`

---

**[09:11] 进展记录**

**工作内容：**
- 从 clean `28ef8ae` 重建 `synthetic-002`，并与 pre-commit rehearsal 做 bundle/request byte comparison；两者 byte-identical，但只批准 clean attempt。
- 用 authenticated read-only `GET /zen/go/v1/usage` 核对调用前 quota，不发送模型 prompt、不打印 key；冻结 OpenCode Go synthetic transport qualification v1.0 的 exact hashes、单次调用、no-retry、cost 和 PASS gates。

**结果：**
- `synthetic-002` request 5,645 bytes，SHA-256 `04d470f736e74281ec45cb169a7095359212d7585d07e7812c9e747903f6232d`；bundle/prompt hashes 与 rehearsal byte-identical。
- 2026-09-01T01:10Z usage snapshot：rolling `1% / ok`、weekly `0% / ok`、monthly `24% / ok`，已有 Go quota 足够，预期 response `cost="0"`。

**问题与状态：**
- ✅ exact synthetic request 已从 clean commit 生成并完成隐私内容检查。
- ⏳ 需先提交 qualification protocol，再允许唯一一次 live request；失败不自动 retry。

**决策记录：**
- Synthetic PASS 只证明当前账号接受 `json_object` request shape 和本地 validator 可消费一次响应；不外推 reasoning 实际执行、24-item judge 能力、ZDR 或正式 winner。

**相关 Commit：**
- `f4142b3 add: freeze opencode transport qualification`

---

**[09:14] 进展记录**

**工作内容：**
- 按 frozen synthetic transport v1.0 发送唯一一次 OpenCode Go direct request；随后将 canonical response 独立交给 `operational_judge finalize`，没有 retry、repair 或字段补写。
- 在不打开 private mapping、也不外发真实数据的前提下，从 clean `f4142b3` 用 v1.4 + v1.6 重建 12-case/24-query 四路 bundles，并为 DeepSeek 两个 orientations 物化 exact credential-free Chat requests。
- 冻结 qualification protocol v1.5：attempt `qualification-007`、四路调用顺序、no-retry、24/24/mirror/side/cross-model gates、6-item deterministic blind grounding audit 与 retention authorization gate。

**结果：**
- Synthetic live：HTTP 200，model `deepseek-v4-flash`，`cost="0"`，usage prompt/completion/total `1548/4566/6114`，raw response SHA-256 `992d546f5711a5558ec004c144809382dc187da16e872321b82fbd06fa2c9786`。
- Synthetic local validator 1/1 PASS，winner `left`，左右 scores 分别全 2/全 0，trace SHA-256 `1ec1c01b7a0c21d498211aa61d58fe4a24a7a08ed49f5f9fdb674cf4eb0c0ec0`；只证明当前账号的 exact `json_object` request shape 可用，receipt 明确 `reasoning_execution_proven=false`。
- Full setwise manifest SHA-256 `676600c8243bbf2b498cbc93c2125626d22b55076649df8e88392e90a67401ac`；mapping commitment `61c52447cdf57348ca5d91f654c38f51b2a9942b51dbf974483936e366838168`；四份 bundle 均 24 items、mirror check PASS。
- Kimi prompts 各 265,151 bytes；DeepSeek prompts 各 265,181 bytes。DeepSeek requests 各 269,578 bytes，SHA-256 `9c085636616fd1d90a0769ea54d2ef37d4b50be1219bd3798f3f0e61bd908765`、`2c47348451dc09d78807c026171ad4ea000fba69618e7dee9a86f856e968f243`。
- `python -m pytest -q tests` 仍为 85/85 passed；`git diff --check` 通过。

**问题与状态：**
- ✅ OpenCode Go direct transport 已通过最小 live qualification，Kimi renderer 与 DeepSeek official 均不再需要。
- ✅ 24-item qualification 的所有本地输入、requests、hashes 和 gates 已准备完成，private mapping 未打开。
- ⏳ 真实 spent prompts 仍含私有 query/corpus；OpenCode Go DeepSeek ZDR 证明已于 2026-08-31 到期。需新官方证明或 Robert 明确接受当前 retention 未确认风险后，才能启动 qualification-007。

**决策记录：**
- Qualification-007 先 DeepSeek 两路、后 Kimi 两路，并且 no retry；这样 transport/schema failure 会真实暴露，不会在 panel 已失败时浪费 Kimi quota或挑选性重跑。
- 6-item blind audit 在开 mapping 前按每个 `stratum × query kind` 的 deterministic hash 最小项选择，避免人工挑看顺眼或结果有利的 judgment。

**相关 Commit：**
- `dba2df9 add: freeze opencode judge qualification`

---

**[09:18] 进展记录**

**工作内容：**
- 向 Robert 明确说明 qualification-007 exact prompts 的内容边界与 OpenCode Go retention 未确认风险；Robert 选择 protocol v1.5 条件 B，授权立即发送两份 exact DeepSeek prompts，并要求不再让与任务结果无关的外部顾虑阻塞执行。
- 创建 tracked authorization sidecar，绑定 orientation 1/2 的 exact prompt/request hashes；evidence 继续如实记录 `retention unconfirmed / user-authorized exact payload`，不冒充 ZDR。

**结果：**
- qualification-007 的唯一外部数据治理 blocker 已解除；调用顺序、no-retry 与全部 qualification gates 不变。

**问题与状态：**
- ✅ 获得发送 exact OpenCode Go payload 的明确授权。
- ⏳ 需先提交 authorization sidecar，随后按 DeepSeek O1/O2、Kimi O1/O2 顺序执行。

**相关 Commit：**
- `037884f add: authorize opencode qualification payloads`

---

**[09:26] 进展记录**

**工作内容：**
- 按 qualification v1.5 执行 OpenCode Go DeepSeek orientation 1/2；两路 response extraction 与 local v1.2 validator 均 24/24 PASS，之后在不打开 mapping 的情况下先计算 mirror stability。
- Mirror gate 实测 22/24，低于冻结的 23/24；立即停止 Kimi、retry、mapping 与 reducer。
- 对两个 unstable items 做公开 bundle/trace 盲态诊断，确认一个是完整候选方向翻转，另一个是 tie 到单侧胜；差异来自对 top-3 complementary coverage 的位置敏感评价，不是 schema、引用或 transport failure。
- 写入 qualification-007 failure result，并比较后续路线：不事后降阈值、不复用输出；推荐同 provider 的 DeepSeek V4 Pro 作为新候选，Flash 六篇 scaffold 仅作次选。

**结果：**
- Orientation 1：HTTP 200、`cost="0"`、usage `58043/36385/94428`、trace `c0f8ca4fa14a0dc09d270dccd5cb83e9db756e5e282d4181f8fca9c7b217e766`。
- Orientation 2：HTTP 200、`cost="0"`、usage `58039/38748/96787`、trace `21009fdbfd8bc7447cfb94b8b2bb44e481ec2739f7ace039b90abd1c51a9ad6f`。
- Winner distributions：O1 left/right/tie `5/9/10`；O2 raw left/right/tie `11/4/9`。Stable `22/24`，qualification-007 FAIL。

**问题与状态：**
- ✅ OpenCode Go direct transport 与 simplified v1.2 schema 均已实证解决此前漏/错字段问题。
- ❌ Flash 未达到预注册 setwise position-stability gate；qualification-007 永久排除。
- ✅ Kimi 未启动、private mapping 未打开、没有 retry 或事后改 gate。
- ⏳ 切换 `deepseek-v4-pro` 属于 material model decision，需要 Robert 明确批准后才能实现/调用。

**决策记录：**
- 不把 23/24 改成 22/24：在看到结果后降低阈值会把 qualification 变成过关工程，破坏 fail-closed 可信度。
- 优先验证 Pro 而非继续加 Flash prompt/schema：当前剩余问题是完整集合语义稳定性，模型能力升级是变量更少、可归因更直接的修复；Pro 仍必须从 synthetic 到 full attempt 独立通过，不能预设成功。

**相关 Commit：**
- `de44dec add: record failed flash qualification`

---

**[10:17] 进展记录**

**工作内容：**
- 按 Robert 批准的 `Flash/high → Flash/max → Pro/high → Pro/max` 阶梯，冻结三组镜像重复校准：每组至少 22/24、至少两组达到 23/24、pooled 至少 69/72，qualification-007 只作先导证据且不进入分母。
- 新增 fail-closed `evaluator_profile_calibration` 聚合器，校验三组 exact bundle/request identity、24/24 traces、result-to-trace SHA binding、六个唯一 provider response IDs 和全部 aggregate gates；Python 3.13 targeted 11/11、full suite 89/89 PASS。
- 先后执行 qualification-008（并发 2）、qualification-009（错峰 45 秒并发 2）和 qualification-010（严格串行 1），每次都使用全新 attempt、保留失败 evidence、禁止跨 attempt 拼接输出。

**结果：**
- qualification-008 replicate 1 两路 24/24 PASS；replicate 2 一路 HTTP 500、一路 HTTP 200，按预登记规则整次 `INCOMPLETE`，不计算 mirror score。
- qualification-009 首组一路 HTTP 200、一路运行约 421 秒后 `RemoteProtocolError`，整次 `INCOMPLETE`。
- qualification-010 严格串行后，第一路 HTTP 200/24-of-24 PASS（96,135 total tokens；57,856 cached input tokens），第二路仍在约 421 秒后 `RemoteProtocolError`，证明并发不是唯一 failure surface。
- 三次 incomplete attempts 均未打开 private mapping、未运行 candidate reducer、未贡献 BM25/E5 winner，也未改变 Flash/high 的 semantic qualification 状态。

**问题与状态：**
- ✅ 并发 3–4 已被实测否决：当前 route 在并发 2 连续两次出现一成一败，提高并发只会增加不可归因的基础设施失败。
- ⚠️ 严格串行仍出现 remote disconnect；当前证据更支持“长 reasoning 的 non-streaming connection 脆弱”，而不是 Mac 算力不足或请求互相竞争。
- ⏳ 下一最小修复候选是 OpenCode Go Chat SSE streaming；必须先获得 material transport change 批准，再实现 fail-closed SSE accumulator，并用 synthetic + 单路 full-size transport qualification 验证，不能直接重开六路。

**决策记录：**
- 不把 HTTP 500/remote disconnect 计为模型 semantic FAIL，也不靠追加调用直到凑够三组；基础设施失败使整个 attempt incomplete，下一 attempt 必须从 fresh calls 重来。
- 不因 Robert 允许并发 3–4 就忽略 provider evidence；实际安全并发由 full-size workload 的可观测稳定性决定。
- Streaming 只作为待验证 transport hypothesis：OpenCode repository evidence 同时显示 DeepSeek Flash Chat 可 streaming，也存在 long-reasoning mid-stream cutoff 报告，因此必须 fail closed，不能预设成功。

**相关 Commit：**
- `f44dc00 add: freeze evaluator profile calibration`
- `3cefb33 add: record incomplete flash calibration`
- `a3632a9 add: serialize evaluator profile calibration`
- `9857237 add: diagnose serialized flash disconnect`
