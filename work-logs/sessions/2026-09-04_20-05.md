# Session 记录 - 2026-09-04 20:05

**[20:05] Ticket 02（blinded comparison boundary）实施、全判据测试与关闭文档**

**工作内容：**
- 实施 `docs/wayfinder/ideation-prompt-neutralization/tickets/02` 全部 10 项 checklist，新增离线 comparison boundary 模块 `ai_scientist/ideation/comparison.py`（~2000 行，stdlib-only）：
  - Selection seam：`select_comparison_cases` 只接受 pinned 已批准 12-case Canary manifest SHA-256（`aae9d766…bb2f` 常量 pin）；`COMPARISON_CLUSTERS` 四 cluster 恒定；canonical case hash 最小者胜出（完整 digest 无 tie）；`CanaryCase` 输入类型 identity-only，测试证明 selection manifest 序列化与 poisoned CSV 的 `contribution`/`route_result` 字段零交集；缺 cluster/重复 case/重复 target/identity 不符/snapshot drift 全部 typed fail closed。
  - Frozen matrix：seed=selection manifest SHA-256，`sha256(seed|arm_order|case_id)` 奇偶定 baseline/challenger-first 再按 canonical hash 再平衡，硬断言 2/2；`assert_single_variable_matrix` 两臂逐字段 diff（除 profile id 外任一差异 → `ARM_SPECIFICATION_DRIFT`）；matrix 文档自带 self-pin（`matrix_sha256` = 不含 pin 的 canonical digest）。
  - Blind mapping + pair packet：`sha256(seed|blind_mapping|case_id)` 平衡 2/2 A 侧；`ComparisonVault` write-once vault——`record_verdict` exclusive-create（覆盖 → `ARTIFACT_EXISTS`）、`reveal` 要求 4/4 verdict 冻结否则 `REVEAL_BLOCKED`、reveal 后写 verdict → `VERDICT_AFTER_REVEAL`；`build_pair_packet` 扫描 `PAIR_PACKET_FORBIDDEN_SUBSTRINGS`（profile id、cost、latency、usage、attempt、provider、reasoning_effort、max_tokens 等），命中即 `PAIR_PACKET_BLINDING_VIOLATION`。
  - Commands：`python scripts/with-project-env -- python … new-run …` 形状，每条命令经生产 `_build_parser().parse_args(shlex.split)` 证明可执行；禁断 unsupported `--reasoning-effort/--max-tokens`、key/env-assignment/`yes` 管道。
  - Spend ledger：0.00 CNY 起、append-only（run_id/slot 重复即拒）；success/failed/suspended/resume/物理 attempts 皆可 ingest；subcap ≤ 30 CNY；`spent+next_bound ≤ subcap 且 ≤30.00`，exact-bound 接受、+0.01 拒；Plan Gate 批准 `scope: matrix_level_only_not_per_run`。
  - Result ingestion：matrix self-pin 复核 → `validate_evidence_chain(check_sealed=True)` → request/admission profile/case/model/budgets pin → frozen matrix workshop/corpus hash pin → sanitized export identity → Evaluation coverage（复用 `list_evaluation_coverage`）→ sealed chain 派生 RunMetrics（finish_reasons、物理 attempts、Σduration latency、Σcost）。
  - Verdict/reducer：封闭四枚举 + domain-method fit / ML intrusion / rubric floor 三映射逐臂校验；`reduce_prompt_comparison` 9 门槛（completeness→incomplete、quality 3–0、domain-method ≥2 改善 & 0 回退、ML intrusion 不增、rubric floor 双臂、deterministic 零容忍、challenger-only truncation/failure/retry、cost/median-latency ≤2.0×（exactly 2.0× 接受）、预算复核）逐项留证，同输入 byte-identical。
- 两个测试文件：`tests/test_prompt_comparison.py`（48 tests）+ `tests/comparison_synthetic.py`（fixture 工厂，4 prepared cases/2 fixture targets，真实九步 preflight 与批准链）；E2E 以真实生产 lifecycle 生成 8 个 synthetic sealed runs（admission→controller→seal→validate→export→evaluation artifact），`ingest_comparison_result` 全量消费后 reduce 到 `promote` 且重放 byte-identical。
- 文档：ticket 02 关闭（checklist 全勾 + implementation notes）、validation-matrix 升 v1.2（VM-CONTRACT-COMPARE-01..04）、spec Testing Decisions 回填 seam 交付、map.md 决议记录。

**结果：**
- ✅ 全量 `python -m pytest -q`：742 passed（694 基线 + 48 新增），compileall、目标范围 Black、`git diff --check` 全绿。
- ✅ import/dependency 契约保持不变（comparison 是离线 boundary，不进 CLI entry import closure，与 corpus/preparation 链的先例一致）。
- ✅ 全程零网络、零真实 provider request、零费用；不读活动 `.env`；Proposal 002 未修改；ticket 035 未执行；最终私有 selection manifest 留给 ticket 03。

**问题与状态：**
- ✅ import closure 最初把 comparison 加入 EXPECTED 清单导致失败：BFS 复查发现该清单只 pin「entry 可达模块」——comparison 无 CLI 入口，正确做法是保持清单不变。
- ✅ self-pin 自引用：`matrix_sha256(doc)` 若把 pin 算进 digest 会恒不等；改为 digest = 不含 pin 字段的 canonical bytes（`frozen_matrix_digest`，ingest/reduction 复核同一函数），单测验证。
- ✅ E2E DIRTY_WORKTREE：同一 workspace 连续 8 个 run 的 sanitized export 写入 `evidence/ideation-runs/`（未 gitignored），下一个 new-run 的 clean-worktree 步骤 fail；fixture `.gitignore` 补 `artifacts/evaluations/` + `evidence/ideation-runs/` 后全绿（与主 repo 的 gitignored 约定一致）。
- ✅ E2E 重复 case id：4 pair 需要 4 个 distinct case id；fixture 从 2 个 fixture target 各派生 2 个 prepared case（`sha256(target+":second")[:32]` 后缀），每 case 独立 workshop/corpus 批准目录。
- ✅ 盲法测试初版用固定 `a_better` 字母写 sweep，撞上 pair 1 的 baseline=A 侧映射；改为按 frozen blind mapping 求 challenger 侧字母，与真实盲审语义一致。
- ✅ 双轴 code-review（Standards + Spec，fixed point `08aa5ec`）发现并全部处置：①`shlex_quote` 自制安全集把 `|` 视为安全字符（管道注入风险）→ 收敛为 stdlib `shlex.quote`；②`matrix_sha256` 名下三种 digest（self-pin/ledger/reduction 的 fabricated stub）→ 统一为 `frozen_matrix_digest`，reduction 直接 pin 冻结矩阵文档自带 digest，删除 `matrix_document_of` stub；③`_rebalance` 双处重复 → 抽出共享 `_balanced_partition`；④matrix builder 内嵌 arm check 与 `assert_single_variable_matrix` 重复 → builder 直接复用该断言；⑤`plan_gate_one_cent_over` 与 next_run_allowed 互补重复 → 取反实现；⑥`record_verdict`/`reveal` O_EXCL 块重复 → 抽出 `_write_bytes_once`（并补齐 reveal 缺失的 OSError→STORAGE_WRITE_FAILED 分支）；⑦`Decimal(str(cost))` 无 guard → `ArithmeticError` typed fail（对齐 duration 解析先例）；⑧Spec 轴：reducer 原先直接吃 `mappings` 参数（reveal gate 无机器绑定）→ 签名改为 `reveal_document`（vault 落盘的 write-once reveal.json，内部 `assert_blind_mapping_document_shape` 校验），E2E 从磁盘读 reveal.json 传入；⑨`deterministic_validation_passed=True` 硬编码 → 改为本次 ingestion 实际运行 gate（chain/seal/export/coverage/identity）的合取；⑩single-variable 断言缺 retriever/rubric/runtime controls → matrix 文档新增 `retriever_policy_version`/`rubric_version`/`runtime_controls` 字段并纳入 comparable_fields；⑪「非 Canary case」未机器证明 → 输入必须是完整 12-case 且每 case cluster ∈ `CANARY_CLUSTERS` 八 cluster 闭集；⑫`DUPLICATE_CANARY_TARGET` 零覆盖 → 独立测试；⑬pair packet 禁词子串扫描过宽（idea 正文合法词误伤，如 "cost-effective"）→ 改为结构级键扫描 + idea payload 外的 profile 身份值检查；⑭`target_dataset_sha256` 纯装饰 → winners 循环复核 dataset snapshot hash。
- ✅ review 后文档同步：ticket 02 notes、validation-matrix v1.2 行、work-log 本条全部与最终实现一致。
- ⏳ Ticket 03（freeze paid-run-ready package）成为 frontier；由本票交付的 boundary 函数支撑。

**决策记录：**
- comparison 不进 CLI import closure：ticket 02 明确「不生成最终私有 selection、不冻结 package」，boundary 由测试消费；ticket 03 需要落盘工具时再经其自己的 import 审批扩面。
- 盲字母（arm_a/arm_b）在 verdict schema 中使用而非 profile id：verdict 必须在 reveal 前可写；reducer 经 write-once reveal 文档揭盲后才把字母映射回 profile。
- matrix self-pin 采用「digest of doc-without-pin」而非链式 hash：保持 `matrix_sha256(doc)` 单函数语义（内部即 `frozen_matrix_digest`），ingest 校验一次 O(doc) canonicalize。
- 确定性排除了时间戳：selection/matrix/blind/commands/reducer 全部 hash-derived；只有 ledger ingest 记录事实时间戳，reducer 不消费它。
- packet 扫描选键级而非全文子串：模型 idea 文本不可控且合法英文词与元数据词重叠；键级 + 身份值检查足以机检「packet 不携带操作元数据」这一盲法契约。

**相关 Commit：**
- `9c2738d`（回填上条记录）feat: add closed versioned Prompt Profile seam for ideation runs
- `255b905` feat: add offline blinded prompt-comparison boundary module
- `fa6ec5e` test: prove deterministic blinded comparison boundary
- `07f8394` docs: close ticket 02 and add comparison validation rows
- `7f16404` docs: record prompt comparison ticket 02 session progress（含本条记录与其回填 diff）