# Session 记录 - 2026-08-29 17:47

**[17:47] 进展记录**

**工作内容：**
- Robert 启动 `Define the Scoped Literature Retriever contract`；ticket 已认领，开始按 Wayfinder 的 HITL grilling 与 domain-modeling 流程解决合同。
- 第一轮锁定 runtime scope ownership 与 Reference Content eligibility，并同步更新 `CONTEXT.md`。

**结果：**
- Scoped Literature Retriever 必须由 Ideation Run preflight 绑定到唯一 Approved Target Reference Corpus；模型只能提交 query，不能选择或切换 corpus。
- `publisher_abstract` 可直接进入检索；`official_full_text` 只能以确定性、版本化、原文可追溯的有限 `Retrieval Segment` 使用；`derived_text`、citation context 与模型摘要默认禁止。
- Title 只能辅助匹配；缺少合格 Reference Content 的 record 不得返回给模型，但在 corpus 仍有合格候选时只记录为 ineligible，不击穿整个 query。

**问题与状态：**
- ⏳ 合同仍在 grilling；输出接口、审计证据、空结果、排名控制与 fail-closed 错误尚待后续轮次决定。

**决策记录：**
- 选择 runtime-owned scope 而非 model-selected scope，结构性阻止跨 case 检索和 corpus substitution。
- 允许 source-faithful official full text evidence，但不允许整篇或伪装成 abstract 暴露给模型。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:09] 进展记录**

**工作内容：**
- Robert 批准 Q5 的 paper-level result 方案，并要求在决定 Q4 query/control 与 Q6 model-visible payload 前，先调查主流文献检索 AI 工具、API 和 MCP 的实现模式，再从本项目目标反推合同。
- 已启动 primary-source research，报告目标为 `docs/research/scoped-literature-retriever-interface-patterns.md`；比较 query、filters、pagination/ranking、result fields、provenance、empty/error 与 replay implications。

**结果：**
- Retriever 的结果单位锁定为 paper；同一 `paper_id` 最多返回一次，允许在 paper 内携带有界 `Retrieval Segment`，paper rank 与 segment rank 分离。
- Q4 与 Q6 暂未锁定，等待外部接口证据和第一性原理综合；未按行业惯例直接选择方案。

**问题与状态：**
- ⏳ 外部 primary-source research 进行中；ranking 算法与数值阈值仍明确留给后续 prototype ticket。

**决策记录：**
- 采用 paper-level aggregation，防止长篇 full text 的多个片段占满结果，并保持与 Related Work 的论文级语义一致。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:16] 进展记录**

**工作内容：**
- 完成主流文献检索接口的 primary-source 调研，覆盖 Semantic Scholar、OpenAlex、Crossref、PubMed E-utilities、Elicit API/MCP 与 Consensus API/MCP，并将报告链接加入当前 ticket 的 Evidence。
- 按“模型判断不可缺信息 / runtime policy / private audit”三层重新推导 Q4 与 Q6；Scite 因无可核验公开 tool schema，没有根据营销页推断接口。

**结果：**
- 全球检索产品暴露 filters、sort、pagination、score 等控制面，是为持续更新的大规模开放索引服务，不能直接迁移到本项目 3–36 篇 frozen corpus。
- 证据支持 Q4 候选：模型只提交严格的非空 `query`；scope、`top_k`、filters、sort、pagination、search mode 与 content type 均由版本化 runtime policy 控制。
- 证据支持 Q6 候选：模型 payload 仅保留 `paper_id`、`title`、`segments[{content_type,text}]`；array order 表达 rank，其他 metadata、score、hash 和 provenance 默认留在 private audit。`year` 仅作为后续可实证扩展，不默认进入合同。

**问题与状态：**
- ⏳ Q4/Q6 候选尚待 Robert 批准；empty/error taxonomy、audit completeness 与 ranking budget 仍待后续 grilling。

**决策记录：**
- 调研只提供 evidence，不以行业惯例替代本项目决策；未知或不可核验接口不作为合同依据。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:28] 进展记录**

**工作内容：**
- Robert 批准研究后版本的 Q4 query/control contract 与 Q6 minimal model-visible output；已将 `Retrieval Result` 和 `Retrieval Audit Event` 写入 `CONTEXT.md`。

**结果：**
- Model-visible input 固定为恰好一个非空自然语言 `query`；未知字段、空白、类型错误与超长输入不得静默修复。Scope、paper IDs、`top_k`、filters、sort、pagination、search mode、content type、threshold 与 budgets 均由版本化 runtime policy 控制。
- Model-visible success payload 固定为 paper-ordered `paper_id + title + segments[{content_type,text}]`；不重复 rank，不发送 score、metadata、query echo、count、hash、provenance 或 provider-derived text。
- `year` 不进入 v1 核心 payload；只有后续 idea-quality validation 证明可重复收益后，才允许通过新版本加入。

**问题与状态：**
- ⏳ Empty-result、error exposure、audit atomicity/replay 与最终 ranking invariants 尚待 grilling。

**决策记录：**
- 模型上下文只承载影响 ideation 判断的 source-faithful evidence；runtime/audit 信息不因“可用”而默认暴露。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:34] 进展记录**

**工作内容：**
- Robert 批准 Q7 合法空结果、Q8 error exposure 与 Q9 audit atomicity/replay，并询问 `Retrieval Audit Event 写入` 的含义。

**结果：**
- 合法无匹配使用 `{"papers":[]}`；模型可在 call budget 内改写 query，但 Ideation Run 若从未获得非空 Retrieval Result，则禁止 finalize 并 fail closed。无 eligible content 属于 corpus/preflight error，不是 empty。
- 只有模型可修复的 `INVALID_QUERY`、`QUERY_TOO_LONG` 暴露最小 input error；boundary、corpus、ranking、audit 与 budget 错误由 controller 终止 run，完整细节只进 private audit。
- Retrieval Result 必须先生成 canonical payload/hash，并持久化可校验的 Retrieval Audit Event 后才能交给模型；audit 写入或校验失败即 fail closed。
- “写入”指持久化到该 run 的 Evidence Chain，不指发送给模型或打印控制台；物理路径由后续 run identity/evidence layout ticket 决定。

**问题与状态：**
- ⏳ 只剩 ranking allowed inputs、stable ordering 与明确移交给 ranking prototype 的参数边界待 grilling。

**决策记录：**
- 可审计性是释放检索结果的前置条件，不是运行结束后的补记。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:37] 进展记录**

**工作内容：**
- Robert 批准最后一轮 Q10 ranking allowed inputs、Q11 stable ordering 与 Q12 prototype handoff boundary；当前 design tree frontier 已清空，进入 shared-understanding 收口。

**结果：**
- Ranker 只能读取 normalized query、title、eligible Reference Content/Retrieval Segments，并仅用 `paper_id` 做稳定 identity/final tie-break、用 `content_type` 执行已批准的 eligibility/segmentation policy。
- 禁止 Target Paper、citation contexts、Workshop hidden text、previous query/result state、动态 metadata、跨 case state、remote API/provider model 等 undeclared inputs。
- Paper 以 score descending + stable `paper_id` 排序；segment 以 score descending + canonical content-item order + source start position 排序。Filesystem/hash-map/wall-clock 顺序禁止影响结果。
- Ranker、normalization、content-type handling、result/segment caps、segment budget、threshold、dependencies、latency 与 cross-platform determinism 明确交由后续 prototype 在真实 corpora 上比较；不得借此改变 scope、I/O schema 或 fail-closed contract。

**问题与状态：**
- ⏳ 等待 Robert 对完整 shared understanding 做最终确认；确认前不写 Resolution、不关闭 ticket、不更新 map。

**决策记录：**
- 当前 ticket 锁定 mechanism-independent contract，具体 ranking 机制和数值参数必须由最小公平实证选择。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:46] 进展记录**

**工作内容：**
- Robert 确认完整 shared understanding；已将合同写入 `Define the Scoped Literature Retriever contract` Resolution、设为 closed，并在 Wayfinder map 的 `Decisions so far` 添加 context pointer。

**结果：**
- 合同完整锁定 corpus binding、minimal query/result schema、Reference Content eligibility、paper-level deterministic ranking boundary、合法 empty、model-actionable input errors、controller-level fail-closed failures、private Retrieval Audit Event release gate 与 versioned prototype handoff。
- Tracker 自动校验通过：36 张 tickets、22 张 closed tickets、22 条 map decisions、6 张 frontier tickets；frontmatter、blockers、Resolution、closed-decision 对应关系与本 ticket/map 本地 Markdown links 全部一致。
- `Choose and calibrate local literature ranking` 已解除全部 blockers 并进入 frontier，仍为 unclaimed；未实现 retriever、未生成 corpus、未修改 raw dataset、未调用模型、未进入 downstream。
- `git diff --check` 通过。

**问题与状态：**
- ✅ `Define the Scoped Literature Retriever contract` 已完成 Wayfinder 收口；具体 ranker 与数值参数保留给后续 prototype。

**决策记录：**
- Retriever v1 只向模型暴露 query 与 source-faithful paper evidence；所有 scope、ranking controls 与 audit proof 留在 pinned runtime/private Evidence Chain。

**相关 Commit：**
- `2c336ed docs: define scoped retriever contract`

---

**[18:59] 进展记录**

**工作内容：**
- 核对并补交进入本会话前已 modified 的 `work-logs/sessions/2026-08-29_11-21.md`；确认其仅为 `Define the frozen corpus contract` 完成后的合法 frontier 推荐续记，没有混入未知代码或数据改动。
- 按 Robert 批准的方案，将新 project-owned `prototype` Skill 从 UI/logic 两分支扩展为 comparison/logic/UI 三分支，并用 `writing-for-agents` 校验 invocation pointer、信息层级、completion criteria 与规划/实现授权边界。

**结果：**
- 新增 `COMPARISON.md`，要求在算法、prompt、model/config 与 data-policy 选择前预注册 hypotheses、alternatives、held-constant inputs、metrics、thresholds、invariants 和 failure cases，运行最小公平对比并保留可 replay 的输入、命令、版本、outputs、hashes、延迟、成本与失败。
- `SKILL.md` 会将可度量的 “which works better” 问题路由到 comparative prototype；LOGIC/UI 仍保留，但 validated winner 只有在另行授权 implementation 后才进入 production。
- Prototype 可添加证明结论可信所必需的 fixtures/checks，但不做 production hardening；paid model/API run 保留事前估价与批准 gate，所有 planning workflow 在记录 human-approved decision 后停止。
- Frontmatter、OpenAI metadata、三条 branch pointers、全部相对 Markdown links、语义 guardrails 与 `git diff --check` 校验通过；`.DS_Store` 已由现有 `.gitignore` 排除。

**问题与状态：**
- ✅ 旧 session log 已补交；prototype Skill 已准备提交，未运行任何 prototype、模型或 downstream stage。

**决策记录：**
- Project prototype 不等同于 UI mockup；凡需要从多个技术方案中基于效果选型的 ticket，默认使用 evidence-driven comparative branch。
- Prototype 只提高决策保真度，不自动授予 production implementation 权限。

**相关 Commit：**
- `7c0c571 docs: record retriever ticket recommendation`
- `f5873bd add: add evidence-driven prototype skill`
