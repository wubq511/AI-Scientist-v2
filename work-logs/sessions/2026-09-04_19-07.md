# Session 记录 - 2026-09-04 19:07

**[19:07] Ticket 01（Prompt Profile seam）实施、双轴 review、修复与提交**

**工作内容：**
- 实施 `docs/wayfinder/ideation-prompt-neutralization/tickets/01` 全部 9 项 checklist，实现封闭、版本化 Prompt Profile seam：
  - 新模块 `ai_scientist/ideation/profiles.py`：registry 常量（in-code 两个 profile）+ `contract.PROMPT_PROFILE_REGISTRY_SHA256` hash pin；`resolve_profile`（自由文本/路径/fragment/未知 id 全拒）、`validate_profile_field`（封闭四键 pin 字段逐项校验）、`resolve_admission_profile`（legacy 恒 baseline + legacy 文档禁带 profile 字段）、三个 render 函数；registry document 在 import 时计算一次并缓存 bundle hash，避免每次 admission/resume 重复 canonicalize 全部模板。
  - `ml-baseline-v1` 字节级 golden 保持：system prompt SHA-256 `d2c000…62d` 与实现前 HEAD 的 `build_system_prompt()` 输出逐字节一致（生成/reflection 模板 golden 同样锁定）；`cross-domain-v1` 精确实现规格批准文本（multidisciplinary role、field-appropriate methods/feasibility、field-neutral Title/Abstract/Experiments、field-aware reflection），无 ML venue/algorithm/metric 硬编码。
  - Run Request/Admission schema 升至 v1.1.0，pin `{profile_id, contract_version, bundle_sha256, registry_sha256}`；admission 前（Step 1 validate）与 controller 构造时双重验证 registry/hash，之后才允许 model operation。
  - resume 只从 admission 重建 profile（不读 mutable default——测试把 default 换成另一 profile 后 resume 仍用 admitted profile）；拒绝 registry drift、bundle hash mismatch、profile substitution；legacy v1.0.0 恒解释为 `ml-baseline-v1`。
  - evidence validator 接受 v1.0.0/v1.1.0 双版本；legacy request 禁带 profile 字段；sanitized manifest 新增只含 id/version/hash 的 `prompt_profile` 段，`FORBIDDEN_KEY_PATTERNS` 增补 5 个模板键作纵深防御。
  - CLI `new-run` 新增必需 `--prompt-profile`；`--help` 无任何 model/provider/effort/tokens/prompt-text 覆盖旗标。
- 同步权威合同：024 修订注（prompt diff 围栏 + 明确「不是第四个 DI seam」）、pipeline spec 三处修订（safe-entry、Run Specification、sanitized export）、validation-matrix 升 v1.1（新增 VM-CONTRACT-024-04/05/06、VM-CONTRACT-023-04）、ticket 01 关闭、map.md 决议记录。
- 按 implement skill 用 /code-review 做双轴（Standards/Spec）并行 review，两个子代理的发现全部处置：
  - 硬违规修复：删除 `admission.py` 未用 `PromptProfile` import；删除无人引用的 `PROMPT_PROFILE_SCHEMA_VERSION` dead constant。
  - Spec 缺口补齐：新增 `test_profile_id_never_enters_transport_request_or_authorization`（Provider Attempt request.json 无 profile id/Authorization/Bearer）；新增 `test_legacy_request_cannot_inject_a_profile_field`；重写两个 resume tamper 测试——先重 pin chain 再触发，使失败真正来自 profile 校验而非外层 hash pin（原来 `ADMISSION_TAMPERED|PROMPT_PROFILE` 的宽松 match 掩盖了测试没测到目标逻辑）。
  - 脆弱 golden 测试重写：原 `exec(git show HEAD:controller.py 的 168:196 行切片)` 在本 diff 提交后会因行号漂移直接 SyntaxError；改为 exec 保留在 controller 里的 legacy 常量（字节等价已由一次性 HEAD 对照验证），并新增常量↔registry 一致性测试防漂移。
  - 判断类整理：registry 计算移到 import 时缓存；schema version 字面量改用 run_store 常量（消除双源）；sanitized key 统一为 `profile_contract_version`；`_CapturingStubTransport` 去除重复 append（曾导致 sent_requests 计数翻倍的假象）；`_workspace` 返回类型注解修正、辅助函数命名、`Ensure the plan is feasible…` 句子补齐 spec 批准的 feasibility 约束文本。

**结果：**
- ✅ 全量 `python -m pytest -q`：694 passed（新增 17 个 contract tests + 14 个双 profile 端到端 lifecycle tests；CLI→admission→controller→seal→validate→export→resume 全链，两个 profile 各自走通，零网络、零真实 provider request、零费用）。
- ✅ `compileall`、目标范围 Black、`git diff --check`、import/dependency 契约测试全绿。
- ✅ Profile 身份 hash：registry `658fcb02…822d`；`ml-baseline-v1` bundle `0eef0489…48793`；`cross-domain-v1` bundle `735eda9e…21992`。
- ✅ 生产默认 profile 未改变（仍 `ml-baseline-v1`）；`cross-domain-v1` 仅在显式选择时进入 governed comparison。未读取 credential 值、未调用 DeepSeek、实际支出 0.00 CNY。
- ✅ 双轴 review 完成：Standards 2 硬违规（均已修复）+ 若干 judgement calls（已按 repo 惯例取舍）；Spec 轴确认 baseline 字节一致、cross-domain 语义逐句对齐规格、无 scope creep 残留（echo profile id 属可接受 cosmetic）。

**问题与状态：**
- ✅ StubTransport `sent_requests` 计数翻倍之谜：根因是测试子类重复 append（transport 基类已记录），非 adapter 重试；已修复。
- ✅ resume tamper 测试首次重写后仍触发外层 HASH_MISMATCH：把 tamper 语义改为「只改 request.json 的 profile、admission 保留原 pin」，交叉校验（request pin ≠ admission pin）才真正生效。
- ✅ golden 测试的 HEAD 行号切片问题在提交前发现并重写，避免 commit 后测试自毁。
- ⏳ Ticket 02（blinded comparison reducer）成为 frontier；Ticket 03 被其阻塞。

**决策记录：**
- registry hash pin 放在 `contract.py`（与其它 policy hash 一致），registry document import 时物化并缓存——重复 SHA-256 全模板（每次 resolve 4+ 次）对 admission/resume 热路径是纯浪费，且常量不可变、无漂移风险。
- profile id 校验复用 `schema.stage_id` 模式而非新写一个 pattern：两者形状相同（小写连字符标识符），新写一个等价 regex 反而引入第二个校验面。
- `IDEA_GENERATION_PROMPT`/`IDEA_REFLECTION_PROMPT`/`build_system_prompt`/`build_tool_catalog` 保留为 canonical evidence（不再被 runtime 调用），既支撑 golden 测试，又避免「删除即失去 HEAD 对照锚点」。
- legacy 语义选择「v1.0.0 缺字段恒 = baseline、v1.0.0 带 profile 字段 = 注入即拒、v1.1.0 缺字段 = INVALID_SCHEMA」，三条都 fail closed，无任何路径能随未来默认值漂移。

**相关 Commit：**
- `9c2738d` feat: add closed versioned Prompt Profile seam for ideation runs