# Session 记录 - 2026-09-03 23:01

**[23:01] 进展记录**

**工作内容：**
- 响应用户指令 `/implement ticket 09`，完成 Ticket 09（Seal explicit non-success outcomes）的 TDD 交付：
  1. `tests/test_sealed_non_success_outcomes.py` 新建 13 项测试先红后绿（VM-CONTRACT-025-02、VM-CONTRACT-026-02、VM-LEAKAGE-02、VM-INTEGRATION-03、VM-FAULT-04）；
  2. `ai_scientist/ideation/controller.py`：FinalizeIdea gate 重排为固定优先级 hygiene → structure → grounding → duplicate，新增 `scan_payload_hygiene` 与版本化模式列表 `payload-hygiene-patterns-v1.0.0`（六组模式从 023/024 标识符格式推导，检索 paper_id 明确不命中）；新增 `_record_hygiene_hit`（hygiene-violation.json + `hygiene_hit` 事件）与 `_seal_failed_run`（terminal failed seal）；
  3. Terminal failed 封闭词汇三类 reason_kind：controller（hygiene/corrupt/backstop）、provider（022 taxonomy terminal 行）、retriever_evidence（020 boundary/audit 码）；未知 reason fail closed；
  4. `run()` 统一捕获 `IdeationInputError` 与 `ModelRoundError`：terminal disposition → failed seal；suspend 类上抛不 seal（ticket 10 边界测试固化）；
  5. `ai_scientist/ideation/run_store.py` 新增通用 `write_artifact`（模板路径 fail-closed 校验）承载 `artifacts/failures/backstop-violation.json`；
  6. 合同变更：run-level retrieval backstop 由抛 `RETRIEVAL_BACKSTOP_FAILED` 异常改为 seal terminal `failed`（025 批准的 terminal 语义），`tests/test_sealed_ideation_run.py` 对应测试同步更新；
  7. 交付证据文档 `docs/research/non-success-outcomes-evidence.md`，更新 ticket 09（状态 `review`）、map.md、AGENTS.md 运行说明。

**结果：**
- ✅ `tests/test_sealed_non_success_outcomes.py` 13 项全部通过；
- ✅ `tests/test_sealed_ideation_run.py` + `tests/test_model_fixable_actions.py` 21 项通过（backstop 测试按新合同更新）；
- ✅ 全量 `python -m pytest -q`：508 passed（495 存量 + 13 新增，无回归）；
- ✅ `compileall` 与 black（ideation 范围 23 文件）全部通过；
- ✅ CLI help baseline、import closure、依赖合同守卫测试通过；
- ✅ 全程确定性 fixture，零网络、零真实模型费用，未进入 downstream。

**问题与状态：**
- ✅ Ticket 09 实施与验证完成，状态流转 `review`，等待 Robert 验收；
- 初版实现的 `_execute_generation` 返回值被 `run()` 忽略导致 hygiene seal 后继续跑下一 generation（测试捕获），已修复为检查返回值并立即返回 sealed result。

**决策记录：**
- hygiene 命中后不产生任何 feedback 文本（模型不再见到该轮），violation report 只记录 pattern_id + span，避免把密文写进事件（026 terminal 语义 + 023 证据边界）；
- sealed failed reason 词汇封闭为三类而非放行任意 error code：controller 内部码 / 022 provider terminal 行 / 020 retriever-evidence 边界码，未知码 `INVALID_FAILURE_CODE` fail closed；
- backstop 从「异常逃逸」改为「seal failed」：025 规定 terminal failure 必须产生 seal，异常路径违反「每个 run 都有显式终局」的 spec story 45。

**相关 Commit：**
- `55537d0 feat: seal explicit non-success outcomes (ticket 09)`