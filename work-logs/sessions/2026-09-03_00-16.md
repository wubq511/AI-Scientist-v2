# Session 记录 - 2026-09-03 00:16

**[00:16] 进展记录**

**工作内容：**
- 按 `local-ranking-atomic-formal-003-preparation-contract-v1.0.md`（SHA `a08d7612…952c`）执行 formal 003 prepare-only
  阶段。基线核验：HEAD=`326f928`、branch 正确、tree 干净、合同 hash 一致、`8ea6443..HEAD` 的 `*.py` diff 为空、fresh
  formal root 事先不存在、四个 frozen prerequisite hash（两份 source bundle 文件+self、canary summary、smoke result）
  全部核对通过。
- 生成六组 current 族 preparation（r1-r3 × o1/o2；atomic v2.5 / transport v3.1、forced_submit_judgment_tool、
  deepseek-v4-pro/max、concurrency 4、max_tokens 16384、各 24 calls），临时目录重复 prepare 六组 byte-identical
  （未写入 formal root）。
- `snapshot-usage` 取得 read-only quota/model snapshot（本阶段唯一的 provider 请求：authenticated GET /usage + 公开
  model-list GET；未打印/复制/持久化 key）：rolling 7%、weekly 36%、monthly 42%，三 period 均 `status=ok`，
  `deepseek-v4-pro` 在 model list。Snapshot SHA `92238d54…cb7f`。
- 以 repo-relative POSIX 路径冻结 profile v2.2（SHA `6be3471c…762b`）：source_commit=`8ea6443`、budget 144 logical /
  576 physical、canary/smoke/usage/source 绑定全部核对；r1 首批 48/192 保持单独授权。
- 构建无 secret execution bundle：git archive HEAD 的 `prototypes/`+`tests/` + formal `input/` + 四个 byte-identical
  prerequisites + 6 个与 Windows lock hash 逐个核对的 win_amd64 wheels（httpx 链，离线保险）+ 自写 `verify-bundle.py`
  + canonical 逐文件 ledger（660 文件）。Archive SHA `d7acd549…8795`、1,777,314 bytes；Mac 解包 round-trip
  byte-identical；路径级扫描无 credential/config/.env/.git。
- Windows 传输与离线验证：archive 复制一次到既有 `D:\AI-Scientist-v2-workspace` 新子目录；`certutil` 核对 archive SHA
  一致；tar.exe 解包；`D:\python.exe` 3.13.7 `-B -X utf8` 跑 verifier 全绿（ledger 双 Pass、六组 preparation 语义校验、
  六组 preparation+profile 重新生成逐字节一致、bundle pristine）；verifier 输出回传至 `controller/`，SHA
  `6a0087e8…9174` 一致。
- 写 `controller/freeze-summary.json`（SHA `22283185…3d60`）：执行代码 SHA、全部关键 hash、文件数/大小、生成命令
  （credential 路径以占位符脱敏）、双平台/Python 版本、验证结果、deviation 记录；不含 secret/SSH identity/host/IP/
  用户绝对路径。
- 回填 15-22.md 的「待提交」为 `326f928`（时间戳与内容吻合）。

**结果：**
- ✅ Prepare-only 全部合同项完成；零 chat/completions/responses、零 execute-call、零 runner transport、零 judgment/vote。
- ✅ Mac：ambient 3.14.6 与 locked 3.13.7（`--require-hashes`）各 255/255 passed；targeted Ruff zero-error；full Ruff 恰
  7 个已披露 baseline；Black/compileall/diff-check 绿。
- ✅ Windows：Python 3.13.7 MSC/AMD64、UTF-8 mode、全程离线、未触碰 credential；verifier 五步全过。
- ⚠️ 两处环境缺陷被 Windows 首验暴露并已处理：① Windows MAX_PATH（260）导致 tar.exe 静默跳过 591 个深路径文件 → 改
  短前缀目录 + `--strip-components=1` 解包；② `verify-bundle.py` 用调用方 cwd 解析相对输入路径 → 修为 chdir 到
  bundle root（保证 profile 内嵌路径字符串与 Mac 冻结值逐字节一致）。两者导致 bundle v1 archive（`2afcac12…faab`）
  被 v2 取代；v1 从未通过验证，Windows 侧已删除。rmdir 无法清理的长路径残留用 robocopy /MIR 空目录镜像法清除。
- ⏳ Ticket 021 继续 open；prepare-only 证据交 Codex 独立复核，r1 live calls 未授权。

**决策记录：**
- Bundle 内嵌 6 个离线 wheels（httpx 链）作为保险：Windows 端实际已有 httpx 0.28.1 系统安装，wheels 未使用，但保留
  可无网络重建环境。
- Archive 不含 tests 以外的历史 outputs、不含 ai_scientist/ 下游栈：合同要求 source tree 无历史 outputs，formal 运行只
  需 prototypes/；tests/ 随基线入 bundle 供审计。
- SSH 连接身份按合同不落盘：端点从 Codex 历史 session 记录中恢复（用户确认该连接此前由 Codex 使用），报告与
  freeze-summary 只写「existing LAN Windows host / workspace 新子目录」类别信息。
- 因 v1 archive 缺陷重传 v2 属合同内的「先核对再验证」流程正常产出，作为 deviation 如实记录两个 hash。

**相关 Commit：**
- `1456e4f add: record formal 003 prepare-only completion`
