# Session 记录 - 2026-08-29 23:41

**[23:41] 进展记录**

**工作内容：**
- Robert 追问本地文献排序是否只有 BM25、local embedding 与 hybrid，并要求调查检索系统和学术文献系统中的其他技术路线。
- 按 Wayfinder 认领 `Choose and calibrate local literature ranking`，将 assignee 设为 `Robert`；未写 Resolution、未关闭 ticket。
- 完成 primary-source 调研并新增 `docs/research/local-literature-ranking-alternatives.md`，覆盖 exhaustive score-all、classical lexical/probabilistic models、query expansion、learned sparse、scientific embeddings、late interaction、cross-encoder、LTR/active learning、citation graph、fusion/diversification，以及 paper/segment aggregation；报告已作为 Evidence 链接回 ticket。

**结果：**
- 候选空间确认远超 BM25/dense/hybrid；Lucene、Terrier、Anserini/Pyserini、SQLite FTS5、PubMed Best Match、Semantic Scholar/SPECTER2 和 ASReview 提供了可核验的机制参考。
- 对每 case 仅 3–36 篇 approved references 的约束，调研建议把 exhaustive score-all 作为 architecture baseline，不使用 ANN 或可能漏召回的 first stage；这仍是 prototype 建议，不是 Robert 已批准的最终决策。
- 第一轮候选建议聚焦透明 lexical baseline、field-aware BM25、一个 QLD/DPH classical challenger、一个 pinned local bi-encoder、deterministic RRF、phrase/proximity ablation，以及 `max/top-m` paper aggregation；更重或依赖 qrels/feedback/forbidden metadata 的路线明确延期或淘汰。
- 报告含 51 个唯一 primary-source URL；机械可达性检查完成，Ponte–Croft 的过期证书镜像已替换为 CIIR 官方 PDF。`git diff --check`、ticket blockers 状态和 Evidence 本地链接检查通过。

**问题与状态：**
- ⏳ 调研完成，但尚无 frozen query set、独立 relevance judgments/qrels 和最终 segment 分布，因此不能从文献直接选择 ranker；尚未运行 prototype、调用模型或进入 downstream。
- ⚠️ Web research 首次批量搜索出现一次连接失败，重试后成功；一个论文镜像的 TLS 证书过期，已换成作者机构来源。

**决策记录：**
- 本轮只扩展并筛选 prototype 候选空间，不锁定最终 ranker；检索架构、模型依赖、参数与 evaluation 仍需 Robert 批准后以最小公平 prototype 实证。

**相关 Commit：**
- 无（当前基线：`97a0c48 add: define run identity and evidence layout`）

---

**[00:12] 进展记录**

**工作内容：**
- Robert 同意继续起草 `Choose and calibrate local literature ranking` 的比较 protocol；新增 `docs/prototypes/local-literature-ranking-comparison-protocol.md`，并把草案作为 Evidence 链接回 ticket。
- Protocol 预注册了 12 cases / 24 queries 的 development 与 blind-holdout 拆分、blind graded qrels、diagnostic fixtures、staged candidate matrix、relevance/determinism/dependency/latency 指标、simplicity promotion margins、失败条件、证据布局和审批点。
- 通过现有 SSH key 只读连接 Windows，确认其 16-core CPU、约 32 GiB RAM、`D:` 约 283 GiB 可用、Wi-Fi 6 当前协商速率 2.4 Gbps；据此把 Mac controller + Windows CPU executor、Windows full matrix + Mac finalist replay 写入草案。未保存 SSH 用户、IP、hostname 或 key 到 repository artifact。

**结果：**
- 比较协议已有可审阅草案，但明确标为 `Draft / pre-registered proposal`；ticket 保持 `status: open`，没有写 Resolution 或更新 map。
- `git diff --check`、protocol 本地链接、ticket open/assignee 状态和“命令尚不存在/不可执行”标记检查通过。
- 当前无需数据线；Wi-Fi 只承担一次性 immutable bundle/model copy 与小体积结果回传，正式 ranking 从 Windows 本地副本读取。

**问题与状态：**
- ⏳ 尚待 Robert 批准 case/query 与人工 judgment 规模、double-review、normalization、candidate/metric/resource gates。
- ⏳ Dense bi-encoder 的 exact model/revision/hash/license/input policy 仍为 `TBD`；未安装 Python 3.11 或任何 ranking/neural dependency，未下载模型、生成 qrels 或启动 benchmark。
- ⚠️ Windows 当前默认 Python 是 3.13；正式 prototype 需要 isolated Python 3.11 environment，但本轮没有修改远端配置。

**决策记录：**
- 当前只锁定 protocol 的草案结构，不把任何 candidate、参数或 winner 记为已批准决定；若 holdout 证据不能区分 finalists，必须记为 `inconclusive`。
- Full candidate matrix 在同一 Windows CPU environment 执行以保证 latency 公平；Mac 只运行轻量 smoke 与 finalists 的 cross-platform observable-determinism replay。

**相关 Commit：**
- 无（当前基线：`97a0c48 add: define run identity and evidence layout`）

**[07:05] 进展记录**

**工作内容：**
- Robert 批准本地文献排序 comparison protocol，并授权从第一性原理解决全部未决项；重新审视了原草案的 `CPU-only Python 3.11` 约束。
- 盘点 Mac（Apple M5、16 GiB）与已通过 SSH 连接的 Windows（Core Ultra 9 285H、32 GiB、Arc 140T 核显、`D:` 约 283 GiB free）的解释器、算力与存储条件，并核验 Python、PyTorch、Transformers、Intel XPU/DirectML 与四个 dense model 的官方资料。
- 将 `docs/prototypes/local-literature-ranking-comparison-protocol.md` 定稿为 approved v1.0，冻结 12 cases / 24 queries、Robert 延时盲复核、exact lexical normalization/formulas、pinned E5、resource/promotion/output-budget gates 和 Windows/Mac execution topology。
- 同步 `AGENTS.md`、README fork-scope note、Wayfinder runtime/safe-entry/audit tickets、map、历史 runtime audit、alternatives report 与 ticket 021 Evidence/Current state。

**结果：**
- Python 3.11 被重新定性为历史 compatibility test cell，而非架构要求；新 reference minor 为 Python 3.13，当前 protocol patch 固定为 Windows 已有、Mac 可由 `uv` 安装的 3.13.7，并记录 per-platform lock。
- CPU FP32 被保留为 mandatory portable reference path，但不永久禁止 accelerator。只有 CPU 违反 latency gate 时才独立测 XPU/MPS；可选 backend 必须达到至少 2 倍端到端加速、无 operator fallback、canonical payload 与 CPU 完全一致且额外环境不超过 1 GiB。DirectML 排除。
- Dense challenger 固定为 `intfloat/e5-small-v2@ffb93f3bd4047442299a41ebb6fa998a38507c52`，weight SHA-256 为 `45bfa60070649aae2244fbc9d508537779b93b6f353c17b0f95ceccb1c5116c1`；直接使用 Transformers pooling，不引入 `sentence-transformers`。
- Ticket 021 保持 open；尚未实现 harness、安装依赖、下载模型、生成 qrels 或运行 relevance benchmark，未选择最终 ranker，也未进入 downstream。

**问题与状态：**
- ✅ 已解决原 protocol 的 runtime、reviewer、normalization、dense model、margins、resource gates 与 output-budget 未决项。
- ⚠️ 初版 runtime 报告误把 Hugging Face Git blob ID 当作 weight SHA-256，且引用了已过时的 PyTorch Python-support prose；已用官方 LFS metadata 和当前安装页纠正。
- ⏳ 下一步需单独实现 throwaway harness、准备 approved cases/queries/qrels、生成锁文件并运行 development/blind holdout；实际 winner 仍待证据和 Robert 最终批准。

**决策记录：**
- Python minor 根据当前两台设备、维护周期和官方 wheel 共同选择 3.13，不因历史 README 固定 3.11；README 的 CUDA/3.11 内容明确标为 upstream full-pipeline instructions。
- 第一轮 neural candidate 选择 E5-small-v2 而非 BGE/MiniLM/SPECTER2，因为它以最小成本明确匹配 asymmetric query-to-passage/segment 任务；这只是 challenger identity，不是 relevance winner。
- v1 不设 scorer raw-score threshold；先用离散 `paper_cap={3,5}`、`segments_per_paper={1,2}`、`total_segment_cap=6` 校准 payload，避免比较不可比的 raw scores。

**相关 Commit：**
- 无（当前基线：`97a0c48 add: define run identity and evidence layout`）

---

**[07:44] 进展记录**

**工作内容：**
- 按 approved v1.0 protocol 实现 `prototypes/local_ranking/` throwaway harness：closed input/protocol/qrels schemas、repository-relative path 与 SHA-256 校验、normalization v1、IDF coverage/TF-IDF/BM25/DPH、field/aggregation/phrase ablation、paper/segment RRF、deterministic model payload、candidate child-process isolation、typed failure 与 immutable attempt evidence。
- 新增 tracked fixture、one-command replay 和 19 项测试，覆盖 normalization golden cases、公式值、tie-break、payload allowlist、RRF、qrels 完整性、forbidden metadata/content、missing dense model fail-closed、CLI evidence 与重复 attempt 拒绝。
- 同步 protocol 与 ticket 021 的实际实现状态、evidence layout、RRF segment-order 语义和 Windows execution topology。
- SSH 恢复后，在 Windows `D:\python.exe` 3.13.7 下传输最小 ZIP，完成 `compileall` 与 5-candidate lexical/RRF fixture replay；创建持久项目目录 `D:\AI-Scientist-v2-workspace`，并划分 `repo/`、`envs/`、`model-cache/`、`artifacts/`、`tmp/`。

**结果：**
- Mac ambient Python 3.14.6：`python -m pytest -q` 为 19 passed；临时 Python 3.13.7 reference cell：19 passed。`black --check`、`ruff check`、`compileall` 与 `git diff --check` 均通过。
- Mac Python 3.13.7 exact replay 成功；Windows transfer、compileall、fixture replay 均通过。两端 fixture 的 5 个 lexical/RRF candidates 均成功，harness 保持 `winner: null`，resource gate 明确为 `not_evaluated_until_environment_smoke`。
- Windows smoke 的随机临时子目录已删除；持久 workspace 及其五个边界目录保留。未安装 PyTorch/Transformers、未下载 E5、未生成真实 qrels、未运行 relevance comparison，也未进入 downstream。

**问题与状态：**
- ✅ Harness implementation、Mac reference fixture 和 Windows cross-platform fixture smoke 完成。
- ⏳ Environment/model bytes、fresh isolated locks 与 dense resource gates 尚未评估；approved 12 cases / 24 queries 和 Robert blind qrels 也尚未准备。
- ⚠️ 首次 Windows smoke 编排因 PowerShell UTF-16 diagnostic stream 在 Mac 端触发 `UnicodeDecodeError`，尚未传输或运行 fixture；检查无残留目录后改用 `cmd.exe + tar.exe`，随后成功。
- ⚠️ RTK 的 pytest wrapper 曾错误报告未发现 tests，因此验证立即回退到原始 `python -m pytest`；这是 wrapper 观测问题，不是测试失败。

**决策记录：**
- Fixture 只证明 harness 行为，不作为 ranker decision evidence；正式 split 仍强制 clean committed worktree、Python 3.13.7、resource measurement 与 complete qrels。
- RRF 的 paper 与 per-paper segment 都融合 rank，而不混合不同 scorer 的 raw scores；segment 同分回到 approved canonical source order。
- 当前未提交源码不复制到 Windows `repo/`；形成 clean commit 和 immutable manifest 后再同步，避免远端工作副本来源不明。

**相关 Commit：**
- 无（当前基线：`97a0c48 add: define run identity and evidence layout`）

---

**[12:30] 进展记录**

**工作内容：**
- 提交已完成的 local-ranking harness checkpoint：`f4e8f24 add: prototype local literature ranking`；随后生成并提交 29 日工作日报、formal-input readiness check、Windows/macOS Python 3.13 hash locks、pinned E5 model preparation CLI 与 3 项新增测试：`fdffb46 add: prepare ranking reference environments`。
- 盘点正式输入时 fail closed：仓库中 Approved Workshop / Approved Target Reference Corpus bundles 为 0，没有读取 raw Target/reference content，也没有伪造 12 cases / 24 queries 或 qrels。
- 从精确 commit 生成 ZIP（SHA-256 `0e38584fa8dcefece134f66b80ec302c82542cac853fe642029ffa4bb9491936`）同步到 Windows immutable repo directory；fresh Python 3.13.7 venv 已按 Windows hash lock 安装。
- 在 Windows 下载并校验 pinned `intfloat/e5-small-v2` 六文件 artifacts；manifest SHA-256 `a0459c80016d083d853c3c3d44c46754b793b005a8a84cca5e1fc2eb3f03bb04`，model files 134,410,262 bytes。
- 使用 library offline flags、`local_files_only=True`、CPU FP32、single thread，对 BM25 / E5 dense / RRF 连续运行两个独立 fixture attempts；每 attempt 包含 5 次 cold preflight 与每 query 30 次 warm replay。

**结果：**
- Mac ambient Python 与 Python 3.13.7 均为 22 tests passed；Black、Ruff、`git diff --check` 通过。
- 两次 Windows attempts 全部 candidate success，逐 candidate/逐 query canonical payload hashes 完全一致。
- E5 worst observed：cold p95 8.786 s、warm p95 29.641 ms、build 0.193 s、peak RSS 422,768,640 bytes、model 134,410,262 bytes、isolated environment 836,643,909 bytes；全部通过 protocol v1.0 gates。
- 新增 tracked evidence summary `docs/prototypes/local-ranking-windows-dense-smoke.md`；未选择 winner，未运行 development/holdout，未进入 downstream。

**问题与状态：**
- ✅ Windows CPU reference dense runtime、pinned artifact identity、两次独立 replay determinism 与 E5 complexity gates 已验证。
- ⏳ 正式 comparison 仍被 0 approved cases 阻塞；network-denied fixture、source-inclusive RRF resources 与（仅 dense 成为 finalist 时）Mac neural replay仍待完成。
- ⚠️ 首次 model/environment size 采集脚本因 Windows raw string 以反斜杠结尾而语法失败；改用正斜杠后成功，无远端写入或 run 影响。第一次跨 attempt 比较脚本也因本地变量尾部字符误报不同；修正并逐项比较后确认 hashes 一致。
- ⚠️ Diagnostic run root 不是 Git worktree，raw summary 的 git metadata 为 null；commit archive + ZIP hash 提供 source identity。Formal split 仍强制 clean Git worktree，不能沿用该例外。

**决策记录：**
- 不因 fixture E5 指标略高而选 dense：一个 synthetic case / 两条 query 不满足 blind qrels、strata 或 promotion rule。
- 不在 Mac 预装 neural stack：protocol 只要求 finalist replay；在 dense 尚未晋级前把计算留在 Windows，符合减轻 Mac 负担的目标。
- Library offline success 不冒充 OS network denial；保持该 gate open，且不在未经额外授权时修改 Windows Firewall。

**相关 Commit：**
- `f4e8f24 add: prototype local literature ranking`
- `fdffb46 add: prepare ranking reference environments`
