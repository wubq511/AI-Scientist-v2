# Session 记录 - 2026-09-02 22:12

**[22:12] 进展记录**

**工作内容：**
- 实现冻结合同 `local-ranking-atomic-formal-evidence-ledger-amendment-v2.3.2.md`（E1–E5 + §4.2/4.3/4.4/4.5 + §6
  proofs），基线 `3c56550`。只允许改 7 个文件，全程无 live call、未动 frozen artifacts、未碰 credential。
- 先红后绿：55 个新负例全部先以最终形态写在旧代码上跑红——14 个 smoke E1 负例与 3 个 judge 负例（current 族两个
  record 入口缺 `--preparation-root` 仍 PASS、runner resume 不重验 input evidence）为 wrong-PASS 红（DID NOT
  RAISE），其余为旧签名缺 `preparation_root` 形参的 TypeError 红；`recovers_completed` 为双向通过的回归守卫。
- `atomic_judge.py` 集中持有 shared current-tool evidence policy（版本族常量、7+8 成功证据文件集、5 文件失败 base
  集、transport/stream 附加文件、ATOMIC_MAX_TOKENS、SMOKE_CALL_COUNT、RETRY_POLICY 单源）；实现 §4.2 成功执行全量
  校验（精确文件集+逐文件 hash+HTTP 恰 200+SSE headers+有序 timestamps+identity/usage/diagnostics+SSE 重建逐字节
  比对）、§4.3 失败执行四类分派（transport/HTTP/non-SSE/extractor，metadata 与 error/evidence 互恰）、§4.4 输入
  绑定（current 族必选 `--preparation-root`、spent 族传入即拒、record 时复制 manifest/prompt/request 三文件进
  `input-evidence/`、attempt outcome 升 v2.6 携带 `preparation_manifest_sha256`）、replay `_validate_attempt` 按
  manifest 族分派 attempt 版本并以 record 同强度重放（E5 闭合）。
- `atomic_opencode_go.py` 删掉本地副本改为 import 复用；`validate_smoke_call` 重写为绑定检查后调 shared §4.2
  policy（错误码 `INVALID_SMOKE_EVIDENCE`）；`execute_call` writer 字节未动。
- `atomic_runner.py` 的 `_preflight_resume` 与 `run_orientation` 透传 `preparation_root`；CLI 两个 record 子命令加
  `--preparation-root`。
- 修绿过程中修复三个测试侧问题：`_validate_tool_failure_evidence` 误用 10-file 精确并集改为 shape+unknown+subset
  分派；`_tool_execution`/`_failed_execution` 只对 current manifest 做真实 preparation binding；一个 replay 负例
  只 resync 了 result files map，补全 receipt files/双 hash/attempt 双 hash 及 evidence 目录 receipt.json 副本后
  只剩语义检查触发预期错误码。
- 旧测试 helper 升级：旧 minimal `record_attempt`/`_failed_attempt` 在 tool-transport 族委托新全证据 helper，legacy
  族保留 minimal 路径（pairing 负例的五个错误码逐一核对不变）；两个 drifted-receipt 直调点补 `preparation_root`；
  `test_new_writers_emit_only_tool_transport_schema_versions` 断言升 attempt v2.6。
- README 更新 record-attempt CLI（`--preparation-root`）、版本族段落（current→attempt v2.6、canary 读 v2.5）与
  §4.2/4.3/4.4 账本段落；ticket 021 追加 v2.3.2 完成段。
- Lint 修一处根因：`_legacy_request` 的 re-export 与 PLC0414 冲突，改为测试直接从 atomic_judge 导入（唯一定义
  处），删除 atomic_opencode_go 的冗余 re-export；同时删掉该模块不再使用的 `MAX_PHYSICAL_ATTEMPTS` 与
  `SUBMIT_JUDGMENT_TOOL_CHOICE` import（各自的消费者本就直接从 atomic_judge 导入）。

**结果：**
- ✅ 55 个新负例全部先红后绿；全量 tests 251 passed（ambient Python 3.14.6 与 locked Python 3.13.7 venv
  `--require-hashes` 双解释器各 251/251）。
- ✅ Targeted Ruff（5 个改动 .py）zero-error；full Ruff（`prototypes tests`）恰剩 7 个已披露 untouched baseline；
  Black/compileall/`git diff --check` 绿。
- ✅ 真实 replay（输出全在 mktemp，`git status artifacts/` 干净）：canary smoke-001 通过 shared-policy validator；
  canary `call-021` attempt-1 record 重放 byte-identical 且 frozen ledger `_validate_attempt` 重放通过，attempt
  SHA-256 仍为 `5d620f2807f584071d444dc842811f2ea266f43e1811c44d926814e73ff75252`；v2.2 r1/o1 25 个真实 attempt
  全部可读、resolve 停在 RETRY_REQUIRED/call-021；legacy v2.1 transport diagnostic-only；current preparation 重复
  prepare 全部 bytes 一致；proof 5 三 hash（tool schema `fa2cea71…`、prompt `4d8b409e…`、request `7445fd80…`）原样。
- ⏳ Fresh `pro-max-calibration-003-tool-output` 仍未授权；按合同停止并交回 Codex 独立复核。

**决策记录：**
- `_validate_tool_failure_evidence` 不用精确并集而用「shape + unknown 拒绝 + base 子集 + extra 分派」：合同允许
  transport 与 extractor 附加文件互斥出现，精确并集会把合法单附加形态误杀。
- 旧 minimal 测试 helper 不删除而按族委托：legacy pairing 负例的错误码稳定性依赖族门先于 presence 检查触发，保留
  minimal 路径是最小改动。
- `_legacy_request` 不留 re-export：唯一消费者是本任务可改的测试文件，从唯一定义处导入消除 F401/PLC0414 双重
  冲突，不加 noqa 绕过标记。

**相关 Commit：**
- 5a915d0 fix: close atomic formal evidence ledger
- 待提交（本 session log）
