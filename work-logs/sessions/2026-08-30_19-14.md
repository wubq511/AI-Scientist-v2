# Session 记录 - 2026-08-30 19:14

**[19:14] 进展记录**

**工作内容：**
- 作为隔离 query-author 会话继续 ticket 021：只读取 `inputs-007/approvals/input-approval-001/query-author-packet/`（manifest + 12 份 Workshop，读前逐文件 SHA-256 校验全 OK），未读取 Target、corpora、references、qrels 或 ranker outputs。
- 起草前重读 approved v1.1 protocol §5.2/§7.1 与 harness `schema.py`/`normalization.py`，确认 query 硬约束：每 case 恰 2 条（`broad`/`focused`）、英文、trim 后 1–256 Unicode scalar values、query_id 安全模式且 per-case 排序、全局唯一。
- 为 12 个 cases 各起草 1 条 broad + 1 条 focused query（共 24 条）；focused 只取 Workshop abstract 可见的机制/方法/限制维度，不含 Target identity、held-out contribution、自创缩写或答案式措辞；全部 ASCII、≤194 scalars。
- 冻结可审计 manifest：`artifacts/local-ranking-prototype/query-authoring-v1/attempts/queries-001/query-manifest.json`（gitignored private 区），复用 harness `canonical_json_bytes`/`write_json_once`（canonical sorted JSON、O_EXCL write-once、0o600）。内容含：每条 query 的 text SHA-256、scalar 数、harness normalized text/tokens、逐条 authoring basis、author/reviewer/rewrite policy、visibility boundary 声明、packet manifest 与 v1.1 protocol 的 SHA-256 绑定、validation 结果。
- 回填 `2026-08-30_18-59.md` 两条「待提交」→ `24fa354 add: calibrate ranking length policy`（19:02 提交，时间戳 + 内容双重吻合，覆盖两条目的研究与 probe 工作）。

**结果：**
- 24 条 query 全部通过 harness `normalize_query`（非空 tokens、≤256 scalars）；manifest SHA-256 为 `aa9875353b6d16ab24eabd525c2620074737bbe70b4feb5a3481d2d3471cb8e1`。
- 绑定 hash 交叉验证一致：packet manifest `aca55098…51ef8`、v1.1 protocol `23d9b6d0…f91c`，均与既有 readiness/preparation 文档记录相同。
- 第二个 fresh-process 独立审计 7 项全 PASS：canonical bytes round-trip、packet 绑定、12 份 workshop hash 与 packet/磁盘一致、text hash/scalar/id/kind、normalized 字段可精确重算、12 cases/24 queries 全局唯一、6+6 split。

**问题与状态：**
- ✅ 24 queries 已冻结为 `query-draft-001`，status 为 `draft_pending_robert_approval`：Robert 在同一可见边界内批准或改写；任何改写冻结为新 attempt（如 `queries-002`），本文件不再编辑。
- ⏳ 后续阻塞项不变：controller materialize 180 个 formal segments + query exact E5 token-length 验证（256-scalar 上限使 512-token 超限不可能，manifest 中已注明由 controller 步骤执行），然后生成 Robert blind qrels form。

**决策记录：**
- Query manifest 落在 private gitignored 的 `artifacts/local-ranking-prototype/query-authoring-v1/attempts/queries-001/`，沿用 `<step>/attempts/<id>` 不可变 artifact 约定，而不是写入已被 hash 绑定的 `approvals/input-approval-001/` 目录（避免事后变动审批 sidecar 目录）。
- Manifest 逐条记录 normalized text/tokens 与 authoring basis，使 Robert 批准复核与后续 controller materialization 均可仅凭 hash 审计，无需重读 Workshop 内容。

**相关 Commit：**
- 待提交
