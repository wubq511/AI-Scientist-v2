# Session 记录 - 2026-08-30 09:08

**[09:08] 进展记录**

**工作内容：**
- 解决 wayfinder ticket [Define the minimal runtime dependency contract]（034）。先向 Robert 说明 ticket 意图，确认开工后认领，用 grilling 分两轮问了 5 个决策问题（文件形态、锁定策略、三类遗留包处理、清单枚举时机、dev 工具归属），Robert 全部按推荐采纳。
- 决策内容：`requirements.txt` 变为本 fork canonical runtime 最小集；现有 29 包全量清单改名 `requirements-upstream.txt` 保留并标注仅服务 legacy 代码；dev 工具独立 `requirements-dev.txt`；三文件全部精确 pin，hash 防篡改由证据层 per-platform lock 承担；最小集 = 新 ideation 路径实际 import 闭包、随使用方同生同灭；`requests` 作 transitional 声明、`tiktoken` 不声明（后续删未使用 import）、downstream-only 包永不进最小集；本 ticket 只定规则，确切包清单由实现 ticket 随代码生成。
- 将 Resolution 写入 ticket 034 并关闭，map 的 Decisions so far 追加一行 gist。CONTEXT.md 未新增术语（依赖文件属实现层关注点，非领域语言）。

**结果：**
- ticket 034 关闭，map 更新，commit `06a0602`。未修改任何 runtime code 或 `requirements.txt`（本 map 期间仅限规划）。

**问题与状态：**
- ✅ 已解决。工作区仍有其他 session 遗留的未跟踪文件（`prototypes/local_ranking/environments/`、`work-logs/daily/2026-08-29.md`），未纳入本次提交。

**决策记录：**
- 选「交换主次」（最小集占用 `requirements.txt` 文件名）而非另建独立最小文件：fork scope 止于 ideation，canonical 文件名应表达真实 runtime 契约，且 `AGENTS.md` 安装命令语义不变。
- 锁定选精确 pin 而非 `--require-hashes`：hash 是 per-platform 的，防篡改职责归证据层，声明层保持人可读。
- 不现在钉死包清单：新 runtime 代码尚不存在，钉清单是假确定性；决策钉在稳定的规则与流程上。

**相关 Commit：**
- `06a0602` add: define minimal runtime dependency contract
